<?xml version="1.0" encoding="utf-8"?>
<!--
    v1.8.8 "Atlas" (Workstream H): the initial (loading) layout the launcher shows
    for the Resume widget until Glance composes the real content. Kept minimal — a
    branded placeholder so the widget never flashes blank.
-->
<TextView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FF221C4A"
    android:gravity="center_vertical"
    android:padding="12dp"
    android:text="@string/app_name"
    android:textColor="#FFFFFFFF"
    android:textStyle="bold" />
