<?xml version="1.0" encoding="utf-8"?>
<!--
    PLAY-FLAVOR MANIFEST OVERLAY.

    RustyNES is permanently open-source and income-free: there is no advertising,
    no in-app purchase, and no tracking. This overlay therefore declares nothing
    ad-related and no advertising-id permission. The `play` flavor differs from
    `foss` only in the non-monetization Google Play services it links (Play Games,
    Cast, Integrity, in-app update/review, cloud save), none of which need a
    manifest node here. Kept as an empty overlay so the flavor split stays explicit.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
</manifest>
