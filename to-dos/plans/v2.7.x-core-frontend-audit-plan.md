<!-- SPDX-License-Identifier: GPL-3.0-or-later -->
# v2.7.x — the core and frontend audits, closed one finding at a time

The first of three minor lines between v2.6.23 "Pulse" and the SuperStation One
core at **v3.0.0** ([ADR 0041](../../docs/adr/0041-hardware-release-is-v3.0.0.md)).
It acts on two of the four audit reports in
[`docs/audits/`](../../docs/audits/README.md): the **core** audit and the
**frontend** audit. Both are oracle-only; nothing in this line changes the
bitstream.

**Scope in one sentence:** make the emulator safe against every untrusted input
it accepts, stop it losing save data, close the mapper gaps the audit located,
harden the desktop, web and mobile hosts, and then — last, and only where it
measures — make it faster.

**Work from the reports, not only from this plan (maintainer instruction, 2026-09-23).** The table below compresses each finding to a clause. For this line that means [`core-audit-report.md`](../../docs/audits/core-audit-report.md) and [`frontend-audit-report.md`](../../docs/audits/frontend-audit-report.md). The reports in [`docs/audits/`](../../docs/audits/README.md) carry the mechanism, the exact locations, the failure scenario and a remediation sketch. Read a finding's section in its report before triaging it, and record the verdict in that report's ledger. The reports are still AI-written guidance, not truth: the calibration and the triage rule below apply to everything taken from them.

## How every finding is handled

The audits are AI-written. Their calibration found the core and frontend reports
accurate at the cited lines far more often than not, and still wrong on some
numbers, on impact, and in several recommended fixes. So nothing in this line is
applied as written:

1. **Triage** it into the ledger ([`core`](../../docs/audits/core-disposition.md),
   [`frontend`](../../docs/audits/frontend-disposition.md)) with evidence.
2. **Pin** a test that fails on `main` and show it failing.
3. **Fix** with the smallest correct change, updating the subsystem doc in the
   same PR.
4. **Gate:** fmt; clippy for every feature combination including
   `retroachievements`; wasm clippy both ways; `cargo doc` with `-D warnings`;
   `cargo test --workspace --features test-roms`; the `no_std` cross-build; and
   **AccuracyCoin 144/144 and nestest 0-diff re-run**, never assumed. A change that
   touches the core or a snapshot format shows byte-identity on the existing
   goldens.

A fix that collides with a standing rule goes to the maintainer instead of being
applied. For this line that means NEVER LAUNDER, the performance bar, and
off-by-default features.

## The releases

| Version | Theme | Contents |
| --- | --- | --- |
| **v2.7.0** | Re-plan, triage, untrusted input | The ADR 0041 re-plan (this document and its siblings) and every ledger populated with verdicts. Core IMP-01/02/03: range checks on PPU `spr_count`, APU duty/step/decay/dac, and BlipBuf's floating-point parameters, plus `checked_add` in `SectionIter`. **A `cargo fuzz` target over snapshot restore**, which should find these crash classes on its own before the fixes land. `#![forbid(unsafe_code)]` on the five chip crates. The suspected **pulse-1 negate** mute (core ledger T-01) pinned by a unit test written from the wiki text, and fixed if the test fails |
| **v2.7.1** | Save data survives | `sram()` on Bandai FCG, Taito X1-005, TxSROM / TQROM (forwarded to the inner MMC3) and Multicart 15. `write_atomic` for the FDS, RetroAchievements, movie and export writes (SEC-06). A `.bak` of a corrupt `config.toml` before defaults overwrite it (CON-04). **`// Provenance:` headers** on `m085_vrc7.rs`, `m099_vs_system.rs` and `m244_cne_decathlon.rs`, with `NOTICE` and `docs/originality-and-provenance.md` entries, reviewed by the maintainer before merge (the audit's "sanitize" is rejected; see core ledger §6.2). **As built:** a sixth board (BMC-FK23C, mapper 176) found by `battery_sram_exposed.rs`, which builds every mapper number rather than trusting the audit's list; SEC-06 covered every bare user-data write in the frontend, not only the three the audit named; and `provenance_record_audit.rs`, which fails if a header and its §1 row disagree |
| **v2.7.2** | Mapper gaps | MMC5 multi-bank PRG-RAM; MMC1 SUROM/SXROM 512 KiB PRG; Namco 163 `$C000-$DFFF` nametable banking; open bus at `$6000-$7FFF` on boards with no RAM there; `has_hardwired_mirroring` on the fixed-mirroring boards; the Sachen `$4101` open-bus latch. Each is pinned to a test ROM or a commercial-ROM screenshot golden, with the dumps staying in the gitignored `tests/roms/external/`. **As built:** see [the v2.7.2 plan](v2.7.2-bankroll-plan.md#as-built): MMC5 moved to the 64 KiB superset after a commercial-ROM regression, the open-bus fix covered 205 board variants, and five boards gained their documented WRAM |
| **v2.7.3** | Desktop and web | DESK-01..08: egui texture deltas uploaded before swapchain acquire (as `detached.rs` already does), CPAL stream reconnect, the underrun threshold floored at the host period, the **three-way `KeyM` default** (microphone, menu bar, P2 Select), fast-forward wakeup coalescing, the canvas CSS, blob-URL revocation, badge-cache clearing. SEC-01..05: PNG dimension clamp and the `u32` size multiply, a Lua memory limit and an uncatchable budget abort, a scheme / address allowlist and size cap on script HTTP, and an asynchronous A/V recorder stop. CON-01/03/05. **FE-01 (maintainer, 2026-09-23): desktop battery saves.** The desktop frontend persists no cartridge battery RAM. `Nes::sram()` is read only by the libretro core (RetroArch's `.srm`) and, for checks, the test harness. Four rules for the fix, from review on #549: **(1) gate on the battery bit.** `sram()` is not "battery RAM": NROM, for one, returns its 8 KiB PRG-RAM whatever the header says. Load and write only when the cartridge's `has_battery` is set, or a volatile cart gains a save it never had. **(2) Key the file by `Nes::rom_sha256()`**, the SHA-256 of the full ROM file bytes including the header, which is the key save states already use: `<data_dir>/battery/<hex>.sav`. **(3) Dirty tracking is a mechanism to design, not a phrase.** The core has no SRAM dirty latch, and the emulation thread owns the `Nes` behind the shared lock. The frontend therefore takes a periodic copy under a brief lock, compares it with the last bytes it wrote, and writes through `write_atomic` only on a difference, plus unconditionally at unload, ROM switch and exit. "Clean" means equal to the last successful write. A core-side dirty flag would be an API change and is decided in v2.7.3. **(4) Test first:** run a battery game, let it save, reload the ROM, and require the save back. A no-battery cart must leave no `.sav` **As built:** see [the v2.7.3 plan](v2.7.3-hearth-plan.md#as-built): all 16 items plus FE-01 fixed; the audit's DESK-05 example and SEC-04's buffering half refuted by measurement; DESK-02 had a fourth clash; and a coroutine budget bypass (FE-02) was found while fixing SEC-03 |
| **v2.7.4** | Mobile, all tiers | The Rust bridge first: `catch_unwind` on every JNI and C-ABI entry, graphics-handle lifetime safety, lock-free input, the ROM size cap, battery-SRAM export and import, and teardown that does not join a network thread on the UI thread. Then Kotlin (surface lifecycle, pause and AudioFocus, I/O off the main thread, `AtomicFile`, recomposition, the render-loop wait) and Swift (a 60 Hz display link, audio-session recovery, dynamic rate control, background tasks, gesture deferral, autorelease pools, thermal state) **As built:** see [the v2.7.4 plan](v2.7.4-pocket-plan.md#as-built): MOB-03 was worse than reported (mobile builds aborted on every panic; a new `release-mobile` profile unwinds), MOB-08 was a core defect every host had, battery saves were proven on the Android emulator, and the iOS-only Rust is now type-checked in CI. IOS-06, IOS-07, IOS-09 and FE-03 refuted; MOB-06 deferred to v2.7.5 |
| **v2.7.5** | Performance and API hygiene | IMP-04..07 and IMP-12 and the scheduler hotspots, **each A/B-measured on the same runner: kept only above 3% and byte-identical, otherwise recorded as rejected**. `#[deprecated]` on the 22 dead `rustynes_cpu::Bus` methods and on `ApuBus` (removal is decided at v2.9.0, per ADR 0041). The doc drift the core audit found **As built:** see [the v2.7.5 plan](v2.7.5-tally-plan.md#as-built): twelve performance proposals closed, one adopted (`drain_all` keeps its capacity, -0.89% where it applies): ten measured in v2.7.5 (the combined ceiling was zero), IMP-12 on its v2.3.1 measurement, the phase-match fold by reasoning; 18 dead bus methods and `ApuBus` deprecated (5 of the audit's 22 were live); the doc drift fixed; MOB-06 blocked on UniFFI. The core and frontend ledgers are closed |

## Things known before starting

- **The performance items have prior evidence against them.** `docs/performance.md`
  already measured `#[inline]` on the hot bus functions as a **+0.60% regression**,
  and puts all of `status.rs` at **0.7%** of the profile, which caps what
  branchless flags can win. That is why they are last and why each needs its own
  A/B. Recording one as rejected is a result, not a failure.
- **The three provenance files quote Mesen2 source expressions.** That makes them
  derivation statements, whatever the audit calls them. The fix adds attribution;
  it removes nothing.
- **The pulse-1 finding is a suspicion, not a result.** It came from reading
  `rustynes-apu/src/pulse.rs:160-174` against the vendored wiki. The sibling's RTL
  follows the wiki, so if the oracle is wrong the co-simulation ladder has a blind
  spot here too; v2.8.2 adds the gate that closes it.
- **Mobile needs the maintainer.** CI compiles Kotlin, and an Android emulator runs
  here; runtime behaviour on real devices and anything Swift does not run here. The
  maintainer runs a per-platform device checklist before the v2.7.4 PR merges, and
  anything left unverified is labelled so in its release notes.

## Out of scope for this line

The libretro and RTL audits (v2.8.x); anything that changes the bitstream; the
features deferred past v3.0.0 (FDS on the FPGA, expansion audio, save states,
cheats, PAL, the other mapper families, the DE10-Nano second board).
