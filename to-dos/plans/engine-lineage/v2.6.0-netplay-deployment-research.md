# v2.6.0 netplay deployment-gap investigation (read-only)

Findings recorded below; this was a research task, no edits to the codebase.

## Component states
- signaling.rs: COMPLETE pure relay protocol + server-side bookkeeping (no I/O). WebSocket/JSON wire.
- signaling_server.rs example: COMPLETE runnable tokio/tungstenite WS server (plain WS, needs TLS proxy).
- mesh_net.rs: COMPLETE native N-peer UDP mesh (loopback-verified).
- stun.rs: COMPLETE client encode/decode + HolePunch FSM. No hardcoded server; docs recommend google STUN.
- wasm_netplay.rs: BrowserNetplay defined + structurally complete handshake BUT never instantiated/wired into App. STUN hardcoded google. signaling_url is a param, no config/UI input.
- No Dockerfile / compose / coturn config anywhere in repo.
- No docs/ deployment guide.

## Code work left (S/M/L)
- Public STUN config default: S
- Signaling URL config field + plumb: S
- Wasm lobby UI + wire BrowserNetplay into App: M-L
- docs deploy guide: S
- Docker/compose for signaling: S-M
