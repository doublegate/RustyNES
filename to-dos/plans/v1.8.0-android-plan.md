# RustyNES — Plan: v1.8.0 "Android"

> **RELEASE REPLAN (maintainer, 2026-06-23): the Google Play *production* launch is
> deferred to v2.1.0.** This document stays the authoritative **engineering** spec
> for the Android app and the v1.8.x line keeps shipping as **GitHub-sideload**
> builds (the v1.8.9 batch + the batched on-device pass continue). But the **Play
> production launch** — the old "Workstream P" maintainer task — is **held until
> after v2.0.0 "Timebase"**: the Android app is **finalized across v2.0.1–v2.0.4**
> on the v2.0.0 core and **launched jointly with iOS at v2.1.0**. The PGS /
> Play-Integrity / Chromecast features stay **default-off behind their build flags**
> until that joint launch. See
> [`v2.0.x-mobile-finalization-plan.md`](v2.0.x-mobile-finalization-plan.md) for the
> full phasing / scheduling.

> **Scope LOCKED by the maintainer (2026-06-17):** architecture = **Hybrid** (thin Rust core
> via JNI/UniFFI + the existing `wgpu` renderer on an Android `SurfaceView` + a Jetpack Compose
> shell; a beta.1 winit+wgpu+egui `android-activity` spike proves first-boot). MVP = **focused**:
> core + video/shaders + audio + touch/gamepad + save-states/rewind/TAS. **DEFER netplay,
> RetroAchievements, and Lua** to a follow-up point release. A shared **`rustynes-mobile`**
> bridge crate is factored here and reused by the v1.9.0 iOS plan.

> **MONETIZATION MODEL REVISED (maintainer, 2026-06-23): ad-supported, $3.99.** The
> `rustynes-monetization` scaffold has been harvested into the repo as the **chosen
> v2.1.0-launch monetization** — an **ad-supported** freemium model (**AppLovin MAX** ads on
> the free tier, removed by a **RevenueCat** `premium` entitlement) with a one-time **"Full
> Version / Remove Ads" ($3.99)** unlock — a deliberate override of the **$2.99-no-ads** model
> locked below. The shared policy core now lives at **`crates/rustynes-monetization`**
> (`AdPolicy`, 13 tests); the staged app shells at `crates/rustynes-monetization/shells/`; the
> design/compliance docs at `docs/monetization/`. The three persistence gates are unchanged;
> what changes is **ads + the $3.99 price + RevenueCat** (vs direct Play Billing). The freemium
> layer still ships **default-off behind `PLAY_BUILD`** and is flipped on at the **v2.1.0**
> joint launch (the "v1.8.8 Play debut" timing in this plan + the harvested docs is superseded
> by [`v2.0.x-mobile-finalization-plan.md`](v2.0.x-mobile-finalization-plan.md)). The
> implemented `LicenseManager` ($2.99) remains the current sideload behavior. The block below
> is the prior locked decision, kept for context.

> **Monetization LOCKED by the maintainer (2026-06-19):** ship on the **Google Play Store**
> as a **free download with a one-time in-app "Full Unlock" purchase priced at $2.99 USD**
> (a managed/non-consumable IAP via Play Billing — the modern single-listing freemium model,
> NOT two separate paid/free listings). The **free tier is a time-limited demo**: an **8-minute
> play session per launch** (10 → 8 in v1.8.1), and with **save-states, save-on-background/resume,
> and on-cart battery-backed SRAM persistence all disabled**. Everything else (full emulation
> accuracy, video, audio, input, pause/fast-forward, the whole ROM library) is **identical to the
> paid tier** — the demo is feature-complete except the three persistence gates + the session timer.
> Purchasing the Full Unlock removes the timer and enables save-states / resume / SRAM. See the
> "Monetization / licensing (Workstream M)" section below.
>
> **Play-launch timing — UPDATED by the maintainer (2026-06-20):** **do NOT publish to Google
> Play until v1.8.8 is complete and posted on GitHub (with release notes), immediately before
> starting v1.8.9.** (Superseded timing: v1.8.6 — moved out to v1.8.8, the release that lands the
> bulk of the user-facing enhancement + polish features, so the Play debut presents the most
> polished build.) v1.8.0–v1.8.7 are **GitHub-Releases / sideload-only** and ship **full-featured**
> (no demo limit) — Play Billing cannot transact on a non-Play build anyway, so a demo gate off-Play
> would just strand sideload users. Mechanism: gate the freemium logic (the session timer + the
> three persistence locks + the Billing client) behind a **`PLAY_BUILD` switch** (a `play` product
> flavor / `BuildConfig` flag, default **false**). Sideload/dev/GitHub builds → `PLAY_BUILD=false`
> → unlimited; the Play AAB → `PLAY_BUILD=true` → freemium. The freemium code (verified end-to-end
> in v1.8.0 + the v1.8.1 R8-strip confirmation) stays in place, dormant, until the v1.8.8 Play
> launch flips it on. Wire the `PLAY_BUILD` flavor in **v1.8.2** so every interim sideload release
> is already full-featured.

Research backing: [`research/v1.8.0-research-android.md`](research/v1.8.0-research-android.md).

## Context

RustyNES is a pure-Rust workspace: a `#![no_std]`+alloc cycle-accurate chip stack
(`rustynes-{cpu,ppu,apu,mappers,core}`) + a `std` desktop frontend `rustynes-frontend`
(winit + wgpu + cpal + egui), a WebAssembly build (winit/wgpu + a lightweight canvas embed,
with a deterministic Pointer-Events **touch overlay**), rollback netplay, RetroAchievements,
TAS, save-states/rewind/run-ahead, Lua scripting, and a composable shader stack. It ships at
**v1.5.0 "Lens"** with **v1.6.0 "Studio"** in development; this plan slots **after the
v1.6.0+v1.7.0 desktop maturity** as the first **platform** (not accuracy) release.

**v1.8.0 ports RustyNES to a complete, shippable Android app.** The pure-Rust core compiles to
AArch64 and produces **bit-identical** framebuffer+audio on ARM — so the entire emulator value
(accuracy, shaders, save-states, TAS) ports for free; only the *host shell* (lifecycle,
rendering surface, audio sink, touch UI, file access, packaging) is new. The accuracy bar is
held by the existing chip stack and host CI; nothing in this release touches it.

## Hard contract (unchanged; applies to everything below)

- **Determinism:** same seed+ROM+input ⇒ **bit-identical** framebuffer+audio. The chip stack +
  the emitted-frame/sample contract are **never touched**, so **AccuracyCoin 100% (139/139)** +
  the oracles stay green on **host** CI (Android CI only proves the build links + an instrumented
  smoke boot). The core is platform-independent → a state saved on desktop loads on Android and
  a `.rnm` TAS replays identically, so **desktop⇄Android netplay cross-play stays valid**.
- **All Android work lives in new host crates** — a shared `rustynes-mobile` bridge + a
  `rustynes-android` platform crate + a Gradle module — exactly as `rustynes-frontend` and the
  wasm path are separate hosts over the same core. The chip stack stays `#![no_std]`+alloc;
  `unsafe` is confined to the JNI/surface glue (each with a `// SAFETY:` note).
- **NEVER bundle commercial ROMs.** All ROM/SRAM/save-state I/O goes through the Storage Access
  Framework (document picker + **persistable URI grants**); the app collects nothing by default.
- New mobile pacing/throttle knobs are **frontend-only** — run-ahead's speculative frames are
  already rolled back and never reach save-states/TAS/RA, so auto-throttling cannot change the
  persistent timeline.

## Architecture (LOCKED: hybrid, with a winit spike)

**Ship target — Option (c) Hybrid.** Keep `wgpu` rendering the NES image (full shader stack
intact) onto an Android `SurfaceView`/`SurfaceHolder` whose `ANativeWindow` is handed to wgpu.
The core + render/audio loop run on a dedicated native thread (the Android analogue of the
desktop `emu-thread`); **Jetpack Compose** owns all chrome (top bar, settings, SAF ROM picker,
touch overlay, save-state manager). Touch/gamepad events publish into the existing lock-free
`SharedInput` and are read at the **same late-latch a keypress uses** — so TAS/netplay/rollback
see them identically and there is **no new determinism surface** (the exact pattern the wasm
touch overlay already proved).

**beta.1 spike — Option (a).** Compile the existing winit+wgpu+egui `App` to Android via
`android-activity` (the `wasm-winit` path is the structural precedent). This is the fastest proof
the core renders + sounds on real ARM, and winit 0.30's `resumed`/`suspended` already models the
surface-loss contract. The egui debugger panels can survive as an optional in-surface power-user
overlay, so the spike's reuse is not wasted; the shippable app then graduates (a)→(c) by lifting
the wgpu surface + core loop into `rustynes-android` and replacing the egui chrome with Compose.

**Rejected:** Option (b) pure-Compose-no-wgpu — discards the NTSC/CRT/Bisqwit shader stack +
the 8:7-PAR/overscan/letterbox blit + the wgpu present path (the v1.0–v1.5 rendering crown
jewels) for Material polish; an XL rewrite at lower fidelity.

### Shared `rustynes-mobile` bridge (factored here, reused by v1.9.0 iOS)

A new platform-agnostic crate that owns the **typed control surface** over the core — load ROM
(from a byte buffer, never a path), set input (the `Buttons` mask per port), tick/run-frame,
borrow the current framebuffer, save/load state, query status — generated via **UniFFI** so the
Kotlin (Android) and Swift (iOS) bindings come for free and type-checked. Platform crates
(`rustynes-android`, and v1.9.0's `rustynes-ios`) wrap it with only the thin, hot, hand-rolled
glue UniFFI can't express: handing the native surface handle (`ANativeWindow` / `CAMetalLayer`)
to wgpu, and the audio sink. This keeps the hand-rolled `unsafe` JNI/FFI surface tiny and shares
~all of the binding layer across both mobile targets.

## STANDING directive — continual research + opportunistic improvement

When implementing any workstream below, **re-mine the web + the Rust-on-Android ecosystem**
(winit/wgpu/cpal/android-activity/cargo-ndk/UniFFI release notes, Play policy updates) as needed
— the ecosystem moves fast. And make any **beneficial enhancement or measured performance/battery
optimization** encountered en route even if not explicitly listed here, provided it respects the
hard contract (determinism-neutral, core untouched, gated where it changes shipped behavior).

## Workstreams

Lettered, each independently shippable as a beta; cumulative on `main` via `feat/*` PRs.

### A — Foundation & build  → beta.1
Stand up `rustynes-mobile` (UniFFI control surface) + `rustynes-android` (`jni` 0.21 hot glue) +
a Gradle module. Toolchain: **NDK r27 LTS** (verify 16 KB page alignment — a Play requirement for
Android 15+ regardless of NDK), Rust targets `aarch64-linux-android` (ship) + `x86_64-linux-android`
(emulator/CI) + optional `armv7-linux-androideabi`, built with **`cargo-ndk`** invoked from Gradle
via **`cargo-ndk-android-gradle`** → `.so` in `jniLibs/` → **AAB** (NOT `cargo-apk`: deprecated +
APK-only, can't publish to Play). `minSdk 26` (AAudio floor), `targetSdk 35` (Play mandate since
2025-08-31). CI: GitHub Actions mirrors `release.yml` — build arm64+x86_64, `gradle bundleRelease`,
upload the AAB; the determinism gate stays on host CI. **Effort: L. Determinism-safe** (new host).

### B — Rendering + video filters  → beta.2  *(SHIPPED via AGSL; wgpu deferred)*
**Decision (2026-06-19):** ship the render path as the per-frame RGBA `Bitmap` blit (proven
performant — the NES is light) and deliver the **GPU shader experience via an AGSL `RuntimeShader`**
applied over the Compose render path (`Modifier.graphicsLayer { renderEffect = … }`): None /
Scanlines / CRT (soft-scanline + RGB aperture mask + vignette), an "FX:" cycle control + persisted
choice. `RuntimeShader` is API 33+, so it's feature-detected (`videoFiltersSupported`) and the plain
blit is the always-on fallback. Post-processing only — the emulated framebuffer/audio is untouched,
so determinism holds. **This is the pragmatic, Compose-native, low-risk path to the user-facing
shader goal; it avoids the native-surface rearchitecture.**

The **full wgpu-on-`SurfaceView` native renderer** (reusing the desktop WGSL NTSC/CRT/Bisqwit stack,
`ANativeWindow`→wgpu via `raw-window-handle`, the `surfaceDestroyed`/`surfaceCreated` surface-loss
lifecycle, Choreographer pacing) is the documented **deeper-architecture follow-up** — worth it for
shader-stack parity + max perf/battery, but not required to ship a polished v1.8.0. **Determinism-
safe** either way (presentation-only). **Effort: AGSL = S (done); native wgpu = L (deferred).**

### C — Audio  → beta.3  *(nearly free)*
**`cpal 0.18` (the current workspace pin) has a native AAudio backend at API 26**, so `audio.rs`
(the lock-free SPSC ring + allocation-free callback + 4-tap Hermite ±1% DRC resampler) works
**unchanged** — the single biggest de-risk. Add: AAudio `PerformanceMode::LowLatency` (2-burst
buffer, MMAP where supported), a mobile DRC latency-target default (mobile bursts are spikier —
bump headroom like the desktop >75 Hz rule), stream pause/resume on `onPause`/`Suspended`, and
audio-focus handling (`AudioManager`) forwarded to mute/pause. **Effort: S. Determinism-safe** —
the resampler is timing-only; the core's emitted samples never depend on wall-clock feedback.

### D — Input: touch + gamepad + haptics  → beta.3
**Reuse the deterministic wasm touch-overlay design** (`wasm_touch.rs`): D-pad/A/B/Start/Select +
selectable target port (P1–P4 Four Score) + Power Pad mat, drawn by Compose (Option c) or egui
(spike), funnelled into the `Buttons` mask → `SharedInput` → `EmuCore::latch`. Hardware gamepads:
Android `InputDevice` + `KeyEvent`/`MotionEvent` (`gilrs` does NOT cover Android) → the same mask,
via `GameActivity`'s `InputIterator` or the Compose `Activity`'s `dispatchKeyEvent`/
`onGenericMotionEvent` over JNI. Light haptics on touch-press via `Vibrator`/`VibratorManager`.
**Effort: M (touch + gamepad), S (haptics). Determinism-safe** — all input converges on the one
late-latched `Buttons` mask, identical to wasm touch.

### E — Storage / SAF + lifecycle  → beta.4  *(retires Risk #3)*
ROM import via the **Storage Access Framework** (`ACTION_OPEN_DOCUMENT`, optional
`ACTION_OPEN_DOCUMENT_TREE` for a library view) with **persistable URI grants**
(`takePersistableUriPermission`) so ROMs survive reboot; bytes read via `ContentResolver` → handed
to `Nes::from_rom` (the wasm byte-picker is the closest precedent — no path, raw bytes). Save-states
/SRAM/settings in app-private `Context.filesDir` (the Android analogue of the desktop `ProjectDirs`):
keep the existing **`.rns` format + `<rom-sha256>/slot-N.rns`** layout (byte-identical, just a
different root → cross-device portability). Save-on-background (`onPause` → SRAM + auto-state) +
the desktop `emu-thread`/`Arc<Mutex<EmuCore>>` model (native `pthread` host — a better fit than
single-threaded wasm). **Effort: M–L. Determinism-safe** (format unchanged).

### F — Compose shell + feature-port  → beta.5
Native Material 3 shell: top bar, settings (DataStore or `toml` in `filesDir`), save-state manager,
ROM library/recents on content URIs, the touch-overlay layout. Port the feature matrix —
**ports clean:** core, audio, video+PAR+overscan, NTSC/CRT/Bisqwit shaders + ShaderStack,
save-states/rewind/run-ahead, TAS `.rnm` (SAF import/export), per-game DB, custom palettes
(SAF), HD-pack (SAF tree, off-by-default). **Re-map:** clipboard screenshot → Android
share-sheet/MediaStore. **DROP:** `rustynes help`/ratatui (terminal-only). **DEFERRED (per the
locked MVP):** Lua (mlua *does* cross-compile with the NDK — feature-gate for a later increment),
RetroAchievements (rcheevos C cross-compiles — needs a Compose OAuth/login UI), netplay (UDP
compiles but mobile NAT/CGNAT needs the TURN story that's already a deferred desktop item).
**Effort: L. Determinism-safe.**

### G — Play Store / signing / legal  → rc.1  *(Risk #2 is partly non-engineering)*
Enroll in **Play App Signing** (Gradle `signingConfig` upload key); AAB build + 16 KB-alignment
check + targetSdk 35 in CI; IARC content rating (an empty emulator shell = Everyone/PEGI 3; the
*user's* ROMs aren't the app's content); data-safety = "no data collected" for the MVP. Emulators
**are allowed** on Play (RetroArch/Dolphin/PPSSPP ship there) given **no bundled ROMs / no
downloader** — compliant by design. Mitigate the Aug-2025 Play-Protect "ROM-based systems"
language + the 2026–27 developer-verification rollout by (1) preparing dev verification early and
(2) shipping a **guaranteed sideload/F-Droid + GitHub-Releases channel** so the project never
depends solely on Play (also the home for the egui-debugger power-user build). **Effort: M;
approval/calendar risk is outside our control.**

### M — Monetization / licensing (freemium $2.99 unlock)  → rc.1
A **single free Play listing** with a **one-time, non-consumable in-app purchase** ("Full
Unlock", product id `full_unlock`, **$2.99 USD**) via the **Play Billing Library** (`com.android.
billingclient:billing-ktx`, v7+). NOT a paid app and NOT a second "demo" listing — Play's modern
guidance is one free app + an unlock IAP. Pieces:

- **`Entitlement` / `BillingManager`** (new Kotlin): on start, connect `BillingClient`, query the
  `full_unlock` product + the user's purchases, and expose a `@Volatile isUnlocked` the shell
  reads. A purchase must be **acknowledged** (`acknowledgePurchase`) or Play auto-refunds in 3
  days. Entitlement is cached locally (DataStore) for offline launches but is **re-verified
  against Play** on each connect (the cache is a convenience, not the source of truth). No
  server: a non-consumable IAP is owned forever and restored via `queryPurchasesAsync` (handles
  reinstall / new device / "restore purchases").
- **Demo gating (the three locks + the timer), all enforced in the shell, core untouched:**
  1. **10-minute session timer** — a monotonic per-launch budget; on expiry, pause the emulator
     and show a non-dismissable "Demo time's up — Unlock full version for $2.99" sheet with the
     Buy + Restore buttons. Resets on next launch (per-session, not per-day).
  2. **Save-states disabled** — the Save/Load slot buttons are hidden/disabled in demo.
  3. **Save-on-background + auto-resume disabled** — `onPause` writes no `auto` state and a ROM
     never auto-resumes in demo.
  4. **Battery-backed SRAM not persisted** — the on-cart save RAM is never written to disk in
     demo (a battery save inside the snapshot is fine in-session, but nothing survives close).
  When `isUnlocked` flips true (purchase or restore), all four lift immediately, no restart.
- **UI:** an always-visible "Unlock ($2.99)" affordance in demo; a "Restore purchase" entry; a
  subtle "Demo — N:MM left" indicator. After unlock, these disappear.
- **Determinism-safe / accuracy-safe:** every gate is a *host* policy over persistence + pacing;
  the emulated framebuffer/audio for a given input is **byte-identical** in demo and paid (the
  demo is the same emulator, just without the persistence features + a session clock). The core,
  the `.rns` format, and AccuracyCoin are untouched.
- **Maintainer-manual (can't be CI-self-certified):** create the `full_unlock` managed product
  in the Play Console, the pricing ($2.99) + tax, a license-tested account, and the closed-track
  upload to exercise the real purchase flow (Play Billing can't be fully tested on a sideloaded
  build). A **`BuildConfig`/debug override** (`debug.force_unlocked` / `debug.force_demo`) lets the
  gating + timer + UI be verified on-device without Play. **Effort: M.**

### Perf / thermal / battery  *(cross-cutting, in B/C/F)*
Perf is **not** the gate (the NES is light: ≤2 ms/frame headless, ample on modern arm64) —
**thermals/battery** are. Default run-ahead **off or 1** on Android (keep the budget-throttle
hysteresis), a smaller default rewind window, Choreographer vsync pacing (no battery-draining
busy-spin), and respect `PowerManager.OnThermalStatusChangedListener` to back off
run-ahead/fast-forward. **Determinism-safe** — all frontend pacing/scheduling knobs.

## Release train

| Beta | Workstreams | Arch | Gate / risk retired |
|---|---|---|---|
| **beta.1 — First boot** | A (foundation/build) | (a) spike | core+wgpu+cpal/AAudio render+sound on real ARM + emulator |
| **beta.2 — Surface & lifecycle** | B | (a→c) | **Risk #1** surface loss; emu headless across the gap; Choreographer pacing |
| **beta.3 — Audio + input + touch** | C + D | (c) | audio lifecycle/latency; input determinism (touch + gamepad → one mask) |
| **beta.4 — Storage + SAF + states** | E | (c) | **Risk #3** no-bundled-ROM/SAF + persistable grants; `.rns` cross-device |
| **beta.5 — Compose shell + feature port** | F | (c) | native feel + parity (shaders/TAS/palettes); RA/netplay/Lua marked deferred |
| **rc.1 — Store/legal/signing + monetization** | G + M | (c) | **Risk #2** Play policy (partial) + signing + 16 KB align + AAB in CI; **freemium $2.99 unlock + 10-min demo gating** |
| **v1.8.0 release** | docs (`docs/android.md`), CHANGELOG, `rustynes-mobile`+`rustynes-android`+Gradle committed, signed AAB on a closed track + sideload APK | (c) | full host-CI gate suite green; AccuracyCoin 100%/139 |

## Critical files / crates

- **New:** `crates/rustynes-mobile/` (UniFFI control surface, shared with iOS), `crates/rustynes-android/`
  (`jni` 0.21 surface/audio glue), `android/` Gradle module (Compose shell), `docs/android.md`.
- **Reused verbatim:** the chip stack; `rustynes-frontend`'s `gfx.rs`/shaders, `audio.rs`/`resampler.rs`,
  `wasm_touch.rs` (touch template), the `emu-thread`/`SharedInput`/late-latch model, the `.rns`
  save-state format + SHA-256 slot keying, the pacing matrix.
- **Build/tooling:** `cargo-ndk` (bbqsrc), `cargo-ndk-android-gradle` (willir), NDK r27 LTS, AGP
  current, AAB; `android-activity` 0.6+ (`game-activity`) for the spike; `jni` 0.21 + **UniFFI** ~0.28+;
  `wgpu` 29 (Vulkan/GLES), `winit` 0.30, `cpal` 0.18 (AAudio); Jetpack Compose (Material 3, Activity
  Result API for SAF, DataStore, `Vibrator`, Choreographer).

## Reuse (don't rebuild)

The byte-identical core; the entire wgpu render + shader pipeline; the cpal/Hermite-DRC audio
engine (AAudio backend is automatic at API 26); the deterministic touch-overlay design from wasm;
the `SharedInput`/late-latch input contract; the `emu-thread` decoupling; the `.rns` format. Only
the chrome + host glue (lifecycle, surface handoff, SAF, packaging) is genuinely new — and it's
better done the Android-idiomatic way than faked inside egui.

## Verification (every beta + finals)

- **Host CI (unchanged, authoritative):** `cargo test --workspace --features test-roms` →
  AccuracyCoin 100% (139/139), oracles byte-identical, nestest 0-diff; fmt/clippy/rustdoc/`no_std`.
- **Android CI:** NDK + targets install, `cargo-ndk` arm64+x86_64 build, `gradle bundleRelease`
  links, an **instrumented emulator smoke test boots + loads a CC0 test ROM**, 16 KB-alignment check.
- **Cross-platform determinism:** a save-state written on desktop loads on Android; a `.rnm`
  recorded on desktop replays bit-identically on Android (and vice-versa).
- **Manual (on-device):** rotate/background/lock/multi-window survive without crash/black-screen;
  low-latency audio + audio-focus; touch + a hardware gamepad both drive P1; SAF ROM/library +
  persistable grants survive reboot; save-on-background; each shader/preset; a TAS replay.

## Maintainer decisions (researched defaults; flag to override)

1. **Bindings:** **UniFFI** for the typed surface + raw `jni` only for the surface/audio hot path
   *(default: yes — minimises hand-rolled `unsafe`)*.
2. **Activity backend:** `GameActivity` (better input/IME) vs a plain Compose `Activity` hosting a
   `SurfaceView` *(default: plain Compose `Activity` for cleaner Compose integration; revisit if
   input plumbing argues for GameActivity)*.
3. **ABIs:** arm64-only ship vs arm64+armeabi-v7a *(default: arm64-only — smallest AAB; x86_64 for
   emulator/CI regardless)*.
4. **NDK pin:** r27 LTS vs r28/r29 (16 KB-by-default) *(default: r27 LTS, Rust's supported LTS)*.
5. **Distribution posture:** Play-first + sideload backup, or F-Droid/sideload-first treating Play
   as best-effort *(default: Play-first with a guaranteed sideload/F-Droid channel)*.
6. **Lua on Android:** ship native mlua (cross-compiles) vs defer scripting from the mobile build
   *(default: defer per the locked MVP; feature-gate for a later increment)*. **RESOLVED in v1.8.6:**
   native `mlua` shipped — `rustynes-script`'s `ScriptEngine` was made `Send` (mlua `send` +
   `Arc`/atomic newtypes) so the UniFFI bridge can hold it; host/CI-verified, on-device pending.

## Out of scope / deferred (documented, not built in v1.8.0)

Netplay on mobile (NAT/CGNAT/TURN — already a deferred desktop item), RetroAchievements
(Compose login UI over the cross-compiled rcheevos), Lua scripting, and the egui debugger as a
first-class mobile surface (kept only as an optional sideload power-user overlay). These land in the
v1.8.x completion roadmap below — **Lua + RetroAchievements + LAN/direct-IP netplay arrive in v1.8.6**
(see that section); only **CGNAT/TURN netplay** + the egui-debugger overlay remain deferred past it
(into the post-v1.8.6 tail).

---

# Post-1.8.0 — the v1.8.x Android completion roadmap

> **Status:** **v1.8.0 "Android" SHIPPED** (the MVP above — verified on a Galaxy Z Fold 7) and
> **v1.8.1 SHIPPED** (demo trimmed to **8 minutes**; the debug Full-Unlock override confirmed
> **R8-stripped from the release AAB** — 0 occurrences in `classes*.dex`). **The freemium $2.99
> Full Unlock is built + verified but NOT yet live: the Google Play launch is held to v1.8.8
> (the user-facing enhancement + polish release), posted on GitHub with release notes before v1.8.9
> starts, per the maintainer (2026-06-20).** v1.8.0–v1.8.7 distribute via GitHub Releases / sideload,
> full-featured (freemium gated behind `PLAY_BUILD`, off until v1.8.8). This roadmap sequences
> **v1.8.2 → v1.8.8** to finish the Android build to **desktop feature parity** + the polish wave,
> in the optimised
> order for development +
> releases. Each point release is independently shippable, additive/off-by-default where it changes
> shipped behaviour, and keeps the hard contract (core byte-identical on ARM; AccuracyCoin 100%
> (139/139) on host CI).
>
> **Progress (2026-06-20):** v1.8.2–v1.8.5 SHIPPED; **v1.8.6 is IN DEVELOPMENT** on
> `feat/v1.8.6-connectivity` (Lua + RetroAchievements landed host/CI-verified; LAN/direct-IP netplay
> being delivered) — **not yet released**. The **Google Play launch has been moved out to v1.8.8**
> (it was the v1.8.6 capstone): the Play debut now rides the "Atlas" platform-excellence + library
> release, so the store listing presents the most polished build. After v1.8.6, the Android line
> continues through a short **post-v1.8.6 / v1.8.7 "Connectivity completion"** tail (full CGNAT/TURN
> netplay, the on-device sign-offs), then **v1.8.8 "Atlas" — which carries the Play production launch
> (flip `PLAY_BUILD` on + publish the freemium AAB), posted on GitHub first with release notes
> before v1.8.9 begins** (see the v1.8.8 section). The platform focus then moves to **v1.9.0 iOS**
> (`rustynes-mobile` reused — now connectivity-complete, so iOS inherits Lua/RA/netplay) and
> **v2.0.0 "Timebase"**.

## Optimised release order (rationale)

Sequenced by **user-facing impact × dependency order × risk**: fix the biggest gameplay pain first
(touch input is currently single-touch — a real bug), then deepen the shell (settings/state), then
the high-effort/high-risk native renderer, then the power-user feature port, then connectivity.
**The Google Play launch is held to v1.8.8** (the user-facing enhancement + polish release, posted
on GitHub with release notes before v1.8.9 starts) per the maintainer's updated timing (2026-06-20);
v1.8.0–v1.8.7 distribute via **GitHub Releases / sideload** and ship **full-featured** (freemium
behind `PLAY_BUILD`, off until v1.8.8). Play-listing growth assets are still *prepared* across the
early releases so the listing is ready when the build is.

| Release | Theme | Headline | Effort | Distribution |
|---|---|---|---|---|
| **v1.8.2** ✓ shipped | **Input & the virtual controller** | **Multi-touch** + a `rustynes.svg`-styled **virtual NES controller**, responsive to both Z Fold 7 displays; haptics; remapping; layout/size/opacity. **Adds the `PLAY_BUILD` flavor** so interim builds are full-featured | M | sideload |
| **v1.8.3** ✓ shipped | Settings, save-state depth, **casting**, onboarding & controller polish | A Material-3 Settings sheet (theme / filter / mute / haptic-intensity / per-mode controller size+opacity); **multi-slot save-states + a manager**; the **"MENU" pill** toggle; **cast gameplay-only to a TV/external display** (`Presentation` API; Z Fold landscape-cover when closed); **first-run welcome + ROM-legal dialogs**; an **About** dialog; per-game config; Play-listing assets *prepared* | L | sideload |
| **v1.8.4** ✓ shipped | wgpu `SurfaceView` renderer + shader stack | The **full native wgpu render path** (Vulkan/GLES) reusing the desktop **WGSL NTSC/CRT/Bisqwit** stack (via the shared **`rustynes-gfx-shaders`** crate); surface-loss lifecycle; Choreographer pacing; the **native-audio JNI hot path** (samples drained as bytes, no per-frame `List<Float>` boxing). Retires the deferred Workstream **B-native** | L | sideload |
| **v1.8.5** ✓ shipped | Power-user feature port | **TAS `.rnm`** record/play; **compressed `.zip` ROMs** + **custom `.pal` palettes**; the **per-game DB** (remembered video filter); **HD-pack** loading + compositing (via the new shared **`rustynes-hdpack`** crate) | L | sideload |
| **v1.8.6** *in dev* | Connectivity & scripting | **Lua** (native `mlua`; `ScriptEngine` made `Send`) ✓ · **RetroAchievements** (Compose login over the new shared **`rustynes-ra`** crate; `RaClient` made `Send`) ✓ on-device-pending · **Netplay** = **direct-IP / LAN-only** (reuses `UdpTransport`/`RollbackSession`); CGNAT/**TURN deferred**. *(The Google Play launch was decoupled from this release and moved to v1.8.8 — 2026-06-20.)* | L + M | sideload |
| **post-v1.8.6 / v1.8.7** | **Connectivity completion** | Full CGNAT/**TURN** netplay (stand up + verify the `deploy/` relay + ICE/room-codes/`RelayTransport`); the on-device Lua/RA/netplay sign-offs; gamepad remap/turbo | M | sideload |
| **v1.8.8** *planned* | **"Atlas": platform excellence + library + the Google Play launch** | **targetSdk 36 / adaptive Window-Size-Class layouts** (Play 2026-08-31 mandate, Android-16 large-screen); **Material You** + per-app **i18n** (desktop EN/ES catalog); a **box-art library** (folders/favorites/search/`THM ` thumbnails/batch import); **cloud saves** (PGS v2 Saved Games/Snapshots, `.rns` sync + conflict policy); **Play Games achievements/leaderboards** (distinct from RA); **screenshot + clip capture/share**; **Android TV / Leanback** + d-pad; **quick tile / shortcuts / widget / PiP**; **accessibility** (TalkBack / switch-access / colorblind) — all **host-only, no shared-core Rust** · **then flip `PLAY_BUILD` on, publish the freemium AAB to Play, and post the GitHub release (with notes) before v1.8.9 starts** | L | **Google Play** + sideload |
| **v1.8.9** *planned* | **"Workshop": power-user & creator tools** | The desktop crown jewels on touch: **Mesen2-class debugger** (panels/hex/RAM-search/breakpoints/watchpoints/event-heatmap, `debug-hooks`-gated, off the Play build); **TAStudio piano-roll**; **movie import** (`.fm2`/`.bk2`/`.fcm`/`.fmv`/`.vmv`); the **HD-Pack Builder** (over `rustynes-hdpack`); **audio depth** (20-band EQ / panning / Schroeder-reverb / crossfeed / per-channel / **NSF player**); **shader breadth** (hqNx/xBRZ / CRT preset bank / constrained `.slangp`+`.cgp`); **Game Genie + raw-RAM cheats + DB**; **symbol / `.dbg` source-map** debugging — all **additive / off-by-default**, byte-identical default build | XL | **Google Play** + sideload |
| cross-cutting | i18n (reuse the desktop catalog — landed in v1.8.8), accessibility (TalkBack labels / scaling / colourblind themes — v1.8.8), opt-in crash reporting | — | — | — |

## v1.8.2 — Input & the virtual controller  *(the priority increment)*

Two coupled deliverables: **multi-touch input** and a **stylised virtual NES controller** that the
touch regions are derived from, both **resizing + remapping correctly across the Z Fold 7's two
displays**.

### A — Multi-touch (fixes the single-input bug)

**Root cause.** The current overlay gives each on-screen button its own
`pointerInput { detectTapGestures { onPress … } }`. `detectTapGestures` tracks a *single* gesture, so
pressing A while holding Right (or two D-pad directions for a diagonal) drops one input — only one
on-screen button registers at a time.

**Fix — one container-level multi-pointer handler.** Replace the per-button gesture detectors with a
**single `pointerInput` on the controller container** that runs an `awaitPointerEventScope` loop,
and on every `awaitPointerEvent()` recomputes the *entire* `Buttons` mask from **all currently-pressed
pointers**: for each `PointerInputChange` with `pressed == true`, hit-test its `position` against the
button regions and OR in every region it falls inside; then `controller.setButtons(0, mask)` and
`change.consume()`. This is the RetroArch-style overlay model and it gives, for free:

- **simultaneous presses** (A + B, A + a direction, Start + Select),
- **D-pad diagonals** (a finger on the cross's corner falls inside both the Up and Right regions —
  the regions are allowed to overlap at the diagonals so ↖↗↙↘ register as two bits),
- **slide / roll** off one button onto another without re-tapping (handled because the mask is
  recomputed from live positions every event, not latched per-down),
- correct **late-latch** semantics (the mask still flows through the existing `SharedInput`/late-latch
  path, so TAS/netplay/determinism are unchanged — this is purely a host input-collection change).

Reuse the deterministic wasm touch-overlay region model (`wasm_touch.rs`) for the hit-test math.

### B — The virtual NES controller (styled per `assets/RustyNES_Icon/rustynes.svg`)

Render an on-screen **NES-001 control pad** matching the controller drawn by the icon generator's
`nes_controller()` (`assets/RustyNES_Icon/make_icon.py`) — the same one in `rustynes.svg`:

- **Body:** light-grey rounded-rectangle shell (`#cfcdc6` face, `#a7a299` shadow side, `#6f6e69`
  edge key-line, corner radius ≈ 0.14·h), with a dominant **dark-charcoal face plate** (`#37373a`,
  `#1d1d1f` border, `#525256` inner bevel).
- **D-pad (left):** a recessed dark square well (`#27272a`) with the black **cross** (`#161618`
  base + a top-light gradient, `#26282d` centre hub + a `#4a4d53` highlight).
- **Select / Start (centre):** a light-grey pill housing (`#c6c4bf`, `#7d7c77` stroke) with two dark
  pills (`#2e3138` + a `#474b53` top catch-light) and **red** `SELECT` / `START` labels (`#e60012`).
- **A / B (right):** a light-grey housing with two dark ring-wells (`#26282c`) and **concave
  bright-red** buttons (`#9a1c1c` rim → a dished red face with a `#ff9a90` lower catch-light), **red**
  `B` / `A` labels below. (NES layout: **B left, A right**.)
- **Logo:** the red "racetrack" capsule outline (`#e60012`, **no wordmark** — matches the icon).
- Two medium-grey accent bars (`#9a9a98`) across the upper-centre of the plate.

**Implementation.** Add a **controller-only export** mode to `make_icon.py` (emit just
`nes_controller(...)` to a standalone SVG) and convert it to an **Android `VectorDrawable`** (the
faithful, low-effort path); render it behind the touch layer. Pressed buttons get a lit/inset
variant (brighten the red dish / darken the D-pad arm) driven by the live mask, so the art reacts to
the multi-touch state. (Alternative: draw it directly with a Compose `Canvas` from the same geometry
constants — keep both options open; the VectorDrawable is the default.)

### C — Responsive sizing + hit-region remap for BOTH Z Fold 7 displays  *(maintainer requirement)*

The controller art **and** its touch regions must **resize and remap together** for the active
display, so they can never desync:

- **Single source of truth.** Derive every button region from the **measured controller bounds**
  (`BoxWithConstraints` / `onGloballyPositioned`) as fractions of the rendered controller rect — the
  exact fractions `nes_controller()` uses (D-pad centre at `left+0.205w, cy+0.07h`; A/B at
  `left+0.805w ± 0.068w`; Select/Start at `cx ± 0.064w`; arm half-extents `0.245h × 0.088h`; button
  radius `0.118h`; etc.). Because the regions are computed from the *same* rect the art is drawn into,
  a resize moves both in lockstep.
- **Per-posture layouts, recomposed on fold/unfold.** The activity already absorbs the
  `screenSize`/`smallestScreenSize` config change without a restart, and the Compose layout is
  responsive; key off the active size class:
  - **Cover (folded, ~1080×2520 portrait):** a compact controller pinned low, sized for **one-handed
    / two-thumb** reach (button clusters toward the bottom corners), screen above.
  - **Inner (unfolded, ~1968×2184, near-square):** a **larger** controller with the D-pad and A/B
    clusters spread to the **outer edges** for a held-in-two-hands tablet grip, with more vertical
    room for the NES image. Optionally a side-by-side (controller flanking the screen) landscape
    variant.
  - A **size / opacity / horizontal-offset** setting (per posture) so players can tune reach; the
    hit regions follow the rendered size automatically.
- **Verification:** on-device, fold/unfold mid-game and confirm (a) the controller rescales without a
  restart, (b) a press in the *new* layout hits the right button (regions tracked the resize), and
  (c) multi-touch + diagonals work on both displays.

### D — Polish

Light **haptics** on press (`Vibrator`/`VibratorManager`, a short tick; user-toggleable);
**hardware-gamepad remapping** + the on-screen layout sharing the one `Buttons` mask; an optional
**turbo/autofire** toggle per face button (reuse the desktop turbo model). **Effort: M.
Determinism-safe** — all input converges on the one late-latched mask.

### E — The `PLAY_BUILD` flavor (interim sideload builds are full-featured)

Per the updated Play-launch timing, wire a **`play` product flavor / `BuildConfig.PLAY_BUILD`
flag** (default **false**) and gate all freemium logic on it: the 8-minute session timer, the three
persistence locks (save-states / save-on-background+resume / battery-SRAM), and the `LicenseManager`
Billing client only engage when `PLAY_BUILD == true`. The default sideload/GitHub/dev build →
`PLAY_BUILD=false` → **unlimited, full-featured**; the freemium code stays compiled but dormant. This
keeps every interim (v1.8.2–v1.8.7) sideload release fully playable while the Play AAB (flipped on at
the **v1.8.8** launch) stays freemium. **Effort: S.**

### Shipped — actuals & deltas (as built, 2026-06-20)  **[DONE]**

What actually landed for v1.8.2 vs. the plan above, including mid-development corrections (most found
by on-device testing on the Z Fold 7):

- **A (multi-touch):** built as one container-level `awaitPointerEventScope` loop in
  `VirtualController.kt`. **Delta:** the first cut recomputed the mask from each event's `changes`,
  which **dropped held fingers when a 3rd touched down** (3+ simultaneous presses failed) — fixed by
  **tracking every pointer by `PointerId` in a map** and recomputing from the full set. Plus a
  `try/finally` mask-reset on gesture-cancel/disposal and `synchronized` touch/key combine (from the
  #160 review).
- **B (controller art):** a `Canvas` drawing the NES-001 pad from the `make_icon.py`
  `nes_controller()` geometry/palette, with pressed-state lighting.
- **C (responsive):** `BoxWithConstraints` + an explicit width (centered) so it resizes/remaps across
  the cover/inner displays.
- **D (haptics):** **Delta:** Compose `LocalHapticFeedback(TextHandleMove)` was **not felt on the Z
  Fold 7** (too subtle / system-gated) → switched to the **system `Vibrator` + `EFFECT_CLICK`** with
  the `VIBRATE` permission. (Gamepad remap + turbo: still TODO, carried forward.)
- **E (`PLAY_BUILD`):** shipped as a `BuildConfig` boolean (default false); all freemium gated on it.
- **App icon (added scope):** the launcher icon became the **real RustyNES artwork** (adaptive,
  **62 %-inset** foreground so the Samsung squircle keeps the corner peripherals — a fix after the
  initial 72 % inset clipped them). The **icon generator was also re-run** so **"Rusty" matches "NES"
  in size** (nudged right ~1/8 char), refreshing `assets/RustyNES_Icon/`, the Android mipmaps, and the
  README logo.

**v1.8.1 actuals** (shipped): demo trimmed **10 → 8 min**; the debug Full-Unlock override confirmed
**R8-stripped from the release AAB** (0 occurrences in `classes*.dex`). Released (tag `v1.8.1`).

## v1.8.3 — Settings, save-state depth, casting, onboarding & controller polish

> **Scope grew (maintainer, 2026-06-20):** v1.8.3 is still unreleased (no PR cut), so the items
> originally pencilled for v1.8.4 onboarding/casting are pulled **into v1.8.3**. Sub-sections marked
> **[DONE]** already shipped on `feat/v1.8.3-settings-states`; **[TODO]** are the added scope.

### Already landed on the branch  **[DONE]**

- **Material-3 Settings sheet** (`Settings.kt`, a `ModalBottomSheet` opened from the menu bar):
  **Light/Dark/System theme** (applied at the Compose theme root, default System), a **video-filter
  picker** (None/Scanlines/CRT, replacing the FX cycle button), **mute**, a **haptics** toggle, and
  **controller size + opacity** sliders — all persisted in `SharedPreferences`, applied live.
- **Save-state manager** (`States.kt`): **four explicit slots** keyed by ROM SHA, each with its
  last-saved time + **Save/Load/Delete** (`SaveStateStore` gained `lastModified`/`delete`/
  `USER_SLOTS`). The `auto` resume slot is unchanged. (`THM ` thumbnails: deferred to a follow-up.)
- **The "pill" menu toggle** (`VirtualController.kt`): the controller's red racetrack logo carries a
  wordmark and is a tap target; the **control bar is hidden by default** and the pill shows/hides it.
- **Controller** decluttered control bar (Open / States / Reset / Pause / Fast-forward / Settings),
  centered `BoxWithConstraints` width so the size slider can exceed 100%.
- **Bot-review hardening** (from the #160 review, applied here since this branch owns the code):
  `try/finally` clears the touch mask on gesture-cancel/disposal (no stuck buttons); `synchronized`
  `setTouchMask`/`onKey` (no touch/key race); allocation-free draw pass (inlined SELECT/START + A/B,
  remembered `Paint`, `Float` `hypot`).

### Added scope (this batch)  **[TODO]**

**A — Screen casting / external-display output (gameplay-only to the TV).** Let RustyNES send **only
the gameplay framebuffer** to a cast/secondary display while the controller + menu stay on the
phone. The mechanism is Android's **`Presentation` + `MediaRouter`/`DisplayManager`** (NOT system
screen-mirroring): `DisplayManager.getDisplays(DISPLAY_CATEGORY_PRESENTATION)` (or
`MediaRouter.getSelectedRoute(ROUTE_TYPE_LIVE_VIDEO).presentationDisplay`) surfaces a secondary
display, and a `Presentation` (a Dialog bound to that `Display`, inflated with the display's own
`Context`/density) renders **independent** content there — exactly the "different content on the
external screen" case, unlike mirroring which duplicates the whole phone screen. Coverage by
transport:
  - **Wired** (USB-C DisplayPort/HDMI), **Samsung DeX**, **Miracast / Wi-Fi "Cast"/"Smart View" when
    exposed as a *secondary* display**, and generic wireless displays → all appear as presentation
    displays and work directly with the `Presentation` path.
  - **Chromecast (Google Cast)** → native rendering via the old **Cast Remote Display API was
    removed by Google (~2019)**, so true Chromecast output now needs a **Cast SDK web Receiver** that
    we stream frames to — heavier; flagged as a **stretch / v1.8.6** item. For v1.8.3 we target the
    `Presentation`-display transports (the common Z Fold cases: Smart View-as-display / Miracast /
    DeX / wired) and, where the system only offers **mirroring**, fall back to mirroring the phone
    screen as-is (documented limitation: controller is visible on the TV in that fallback).
  - **AirPlay** → Apple-proprietary, **no native Android API**; documented as unsupported (a
    third-party-receiver hack is out of scope).
  - **Z Fold 7 posture behaviour while a presentation display is active:** **closed** (cover) → the
    cover switches to **landscape**, showing the **controller only** (rotated/resized to fit the
    landscape cover), gameplay only on the TV; **open** (inner) → the inner screen keeps **gameplay +
    controller**, and *also* renders the **gameplay-only** `Presentation` to the TV (mirror of the
    framebuffer, not the screen). **Never** cast the controller or the (pill-unhidden) menu bar in
    either posture. Implemented in `rustynes-android` host glue (a `Presentation` subclass that draws
    the same `ImageBitmap`/`Surface` the on-phone path already produces — determinism-neutral,
    presentation-only). Adds a **Cast/route button** to the menu bar via `MediaRouteButton` (or a
    custom route picker) and a `DisplayManager.DisplayListener` to react to attach/detach.

**B — Pill wordmark "M E N U".** Change the red racetrack pill text from "RustyNES" to **`M E N U`**
(same red `#E60012` bold face as SELECT/START). **Make the pill taller** top-to-bottom so the glyphs
can be the **same size as SELECT/START** while staying centered left-right and **not intersecting**
the oval outline.

**C — "RustyNES" wordmark above SELECT/START.** In the grey plate area **between the top of the red
SELECT/START labels and the white border above them**, add a **"RustyNES"** wordmark in the **icon
generator's font/colour** (the Press Start 2P red wordmark `make_icon.py` bakes — reuse the same face
+ `NINTENDO_RED`), sized to the **width of the white SELECT/START housing below** and centred.

**D — Controller size 25–110 % with true overflow, ticks & quick-tap.** Above 100 %, the controller
**actually extends past the screen edges** (sides off-screen), and the **touch hit-regions remap with
the size across the whole 25–110 % range** (they already derive from the measured Canvas size — verify
at the extremes). Change the slider **minimum from the current 0.6 to 0.25 (25 %)**; add **tick marks
+ quick-tap snap points at 25/50/75/100/110 %** (Material `Slider` `steps`/custom), while still
allowing a **free drag to any value between**. **Haptic feedback** fires when a tick value is hit
(on tap-to-set and when the drag crosses 25/50/75/100/110 %).

**E — Per-screen-mode controller size/opacity (remembered + smart-switched).** Keep **separate,
independently-remembered** size + opacity for **three modes**: **closed/outer (cover)**,
**opened/inner**, and **screencast/landscape controller**. Switch the active values automatically as
the posture/cast state changes. In Settings, **prefix the "Controller" size/opacity labels with the
current mode in parentheses** (e.g. "(Inner) Controller size"). Persistence keys per mode.

**F — Haptic intensity Off / Low / Medium / High.** Replace the on/off haptics toggle with a
**4-level** selector; map to `VibrationEffect` amplitudes/predefined effects (e.g. Off → none, Low →
`EFFECT_TICK`, Medium → `EFFECT_CLICK`, High → `EFFECT_HEAVY_CLICK` / a stronger one-shot). Drives
both the button-press tick and the size-slider tick (D).

**G — Resizing the controller must NOT move the gameplay window.** Decouple the layout so changing
controller size (any mode) **leaves the gameplay display fixed** — the controller grows/shrinks
(and overflows at >100 %) without reflowing or shifting the framebuffer view (e.g. overlay the
controller / give the gameplay pane a stable measured area independent of controller size).

**H — First-run onboarding dialogs.** On the **first launch only**: (1) a **welcome** dialog — a
brief intro + "tap the **pill** to open the menu bar" — with a **Continue…** button; then (2) a
**legal/ROM-ownership** dialog — play only ROMs you own a physical copy of; RustyNES ships **no**
commercial ROMs; responsibility is the user's — with a **Close** button **and** a **"Do not show
again"** checkbox; ticking it + Close suppresses both on future launches (persisted flag).

**I — "About" menu item.** Make the **last menu-bar item** an **About** entry opening a dialog that
shows the **RustyNES icon** + the **desktop project's About text** (reuse the desktop About copy), with
a Close button.

**J — Related enhancements (research-driven).** A landscape controller layout for the cast/cover case
(reuse the same Canvas geometry, rotated/relaid for a wide aspect); a `DisplayManager.DisplayListener`
+ posture/`WindowInfoTracker` watcher to drive A/E switching; persist the cast route; the per-mode
indicator also on the cast button; verify immersive/edge-to-edge on the landscape cover; keep the
`THM ` save-state thumbnails as a fast-follow. Research Cast SDK receiver feasibility for true
Chromecast (likely v1.8.6).

**Plus the original v1.8.3 carry-overs:** per-game `<rom>.json` config overlay (desktop H4 format);
recents/library polish (rename/remove/last-played); **Play-listing assets** *prepared* (phone +
foldable + tablet screenshots, feature graphic, descriptions, content-rating/data-safety). **Effort:
L** (was M; casting + onboarding + per-mode state grew it). Determinism-safe (all host/UI;
presentation output is a second view of the same framebuffer).

> Casting research: Android **`Presentation`** renders *independent* content on a secondary display
> (vs. mirroring) and `DisplayManager.DISPLAY_CATEGORY_PRESENTATION` enumerates suitable displays —
> [Presentation (Android Developers)](https://developer.android.com/reference/android/app/Presentation),
> [DisplayManager](https://developer.android.com/reference/android/hardware/display/DisplayManager).

## v1.8.4 — wgpu `SurfaceView` renderer + the desktop shader stack  **[SHIPPED]**

Retired the deferred **B-native**: lifted `wgpu` rendering onto an Android `SurfaceView` (Vulkan/GLES),
handing the `ANativeWindow` to wgpu via `raw-window-handle`, and **reused the desktop WGSL shader
stack** (LMP88959 NTSC/PAL, CRT, the **Bisqwit composite NTSC**, the 8:7-PAR/overscan/letterbox blit)
for parity beyond the AGSL CRT/scanline post-pass. **As built, the shared WGSL was extracted into a
new `rustynes-gfx-shaders` crate** (`feat(gfx-shaders): share the Bisqwit NTSC WGSL with the Android
renderer`) so the desktop frontend and the Android renderer compile one copy — the first of the v1.8.x
"extract-to-a-shared-crate" moves (later `rustynes-hdpack`, `rustynes-ra`). The keystone is the
**surface-loss lifecycle** (drop the `Surface` on `surfaceDestroyed`, recreate on `surfaceCreated`;
creating before resume panics) with the emu thread running headless across the gap; pacing via
**Choreographer**. Per-filter shader-tuning sliders + a phone-DPI NTSC default landed too. The
`Bitmap`+AGSL path stays as the API-<33 / fallback renderer. **Also shipped here:** the **native-audio
JNI hot path** — samples drained **as bytes**, dropping the per-frame `List<Float>` boxing. **Effort:
L. Determinism-safe** (presentation-only).

## v1.8.5 — Power-user feature port  **[SHIPPED]**

Ported the remaining desktop creator features through SAF: **TAS `.rnm`** record/play (the format is
cross-platform), **compressed `.zip` ROM loading** + **custom `.pal` palettes**, the **per-game DB**
(persists the per-game video filter), and the **HD-pack** loader + compositor. **As built, the HD-pack
subsystem was extracted into a new shared `rustynes-hdpack` crate** (`refactor(hdpack): extract HD-pack
subsystem into the rustynes-hdpack crate`) and taught to **load from a `Read + Seek` reader / raw
bytes** so the Android SAF (no file paths) can feed it — the bridge gained `load_palette` + Bisqwit
NTSC-data methods. All output-only / off-by-default, so the shipped emulation stays byte-identical.
**Effort: L.**

## v1.8.6 — Connectivity & scripting

> **Status (2026-06-20):** IN DEVELOPMENT on `feat/v1.8.6-connectivity` — **NOT released**. Build
> version is already `1.8.6` (`versionCode 10806`); `PLAY_BUILD` stays **false** on the branch.
> Maintainer sequence for the three deferrals: **Lua → RetroAchievements → Netplay**. Lua and RA
> have landed (host/CI-verified; on-device sign-off pending); netplay is being delivered
> **direct-IP / LAN-only** with CGNAT/TURN traversal **deferred** (see below).
>
> **The Google Play launch was decoupled from this release and moved to v1.8.8** (the user-facing
> enhancement + polish "Atlas" release), per the maintainer (2026-06-20) — so the Play debut presents
> the most polished build. v1.8.6 ships **sideload / GitHub-Releases only**, full-featured
> (`PLAY_BUILD=false`). The launch mechanics are described in the v1.8.8 section.

The three locked-MVP deferrals — each additive / off-by-default; the core stays byte-identical on ARM:

**Lua scripting — DONE (host/CI-verified; on-device pending).** `mlua`'s vendored Lua 5.4 C
cross-compiles cleanly via the NDK. The blocker was that the desktop `ScriptEngine` was `!Send`
(`Rc<RefCell<…>>`/`Rc<Cell<…>>` internals), which the UniFFI `Send + Sync` bridge object cannot hold.
Fixed by making `rustynes-script`'s `ScriptEngine` **`Send`** — enable mlua's **`send`** feature and
replace the `Rc<RefCell>`/`Rc<Cell>` interior state with `Arc<Mutex>`/atomic newtypes (a sub-agent
spike proved this tractable). The bridge exposes `load_script` / `unload_script` / `drain_script_log`
plus a `post_frame_script` hook (`crates/rustynes-mobile/src/lib.rs`); the Android UI is a SAF `.lua`
picker + an on-screen log overlay. **On-device live execution is deferred** (the test phone was
disconnected this cycle).

**RetroAchievements — DONE (host-verified; on-device pending).** The frontend's `RaSession` was
extracted into a new shared std crate **`rustynes-ra`** (the same extract-to-a-shared-crate move as
v1.8.5's `rustynes-hdpack` / `rustynes-gfx-shaders`), so both the desktop frontend and the mobile
bridge consume one RA implementation. rcheevos's `RaClient` is `!Send` (raw C `rc_client` pointers),
resolved with an audited **`unsafe impl Send for RaClient`** — the `rc_client` callbacks are
synchronous on the driving thread and the bridge `Mutex` serializes all access (the invariant is
documented at the `unsafe impl`). The bridge adds the full `ra_*` surface + a `post_frame_ra` tick +
a hardcore **`load_state` gate** (`MobileError::HardcoreBlocked` — a hardcore session refuses
`load_state` but still allows in-session play). The Android UI is a Compose login screen + a toast
unlock HUD + a `.rap` progress sidecar. **TLS cross-compiles cleanly with NO OpenSSL** —
`ureq` + `rustls` + `ring` (the RA HTTP path), which sidesteps the OpenSSL-on-NDK cross-compile pain.
**On-device login round-trip + unlock toasts + sidecar persistence are deferred** (no device this
cycle).

**Netplay — direct-IP / same-LAN only for v1.8.6; CGNAT/TURN DEFERRED.** The mature GGPO-style
rollback core (`rustynes-netplay`: `UdpTransport` in `connection.rs` + `RollbackSession<T>::advance`
in `session.rs`) has **no `!Send` barrier and cross-compiles to Android as-is**, so a host-and-join
*direct-IP / same-Wi-Fi-LAN* session is the tractable v1.8.6 scope. The Kotlin frame loop branches to
an `np_advance_frame` entry point — **netplay REPLACES `runFrame`**, because rollback re-runs frames
(speculative advance + rollback-and-resimulate); the bridge cannot just emit one frame per call in a
netplay session. Determinism + cross-play hold (a state saved on desktop loads on Android; a `.rnm`
replays identically), so **desktop ⇄ Android cross-play stays valid**.

**The CGNAT / cross-NAT TURN story is explicitly DEFERRED** (carryover — see the post-v1.8.6 roadmap).
The relay infrastructure **already exists in-repo** — `deploy/` is a turn-key Docker bundle (coturn
STUN/TURN `turnserver.conf` + a signaling-server image + a Caddy TLS proxy + `.env`-driven config),
and `rustynes-netplay` already has the `stun` (RFC 5389 binding-request) + `signaling` (N-peer mesh
offer/answer/ICE) + `mesh_net` machinery. What is **unrun** is the *ops* step: a **hosted, running
relay** with a domain + TLS + a live **cross-NAT** session over it. That is a **maintainer ops / cost
decision** (recurring TURN hosting + bandwidth + abuse surface), exactly as `docs/netplay-webrtc.md`
§5 records the browser path's relay as "deployment-ready, NOT verified — the maintainer's manual run."
So v1.8.6 ships LAN/direct-IP netplay and defers internet/CGNAT netplay to the relay being stood up.

After Lua + RA + LAN netplay land, Android is at near-desktop parity (the remaining gaps are the
deferred TURN traversal, the on-device sign-offs, and the egui debugger — which stays an optional
sideload power-user overlay, never a first-class mobile surface).

**The Google Play launch is decoupled from this release.** Per the maintainer's updated timing
(2026-06-20), Play publication now rides **v1.8.8 "Atlas"** (the user-facing enhancement + polish
release), posted on GitHub with release notes before v1.8.9 starts — so the store debut presents the
most polished build, not the connectivity build. v1.8.6 ships **sideload / GitHub-Releases only**,
full-featured (`PLAY_BUILD=false`). The freemium wiring (verified end-to-end in v1.8.0/v1.8.1) stays
in place, dormant. The launch steps (flip `PLAY_BUILD`, create the `$2.99` `full_unlock` product,
Play App Signing, closed-track → production) are detailed in the **v1.8.8** section below.

**Effort: L** (the connectivity workstreams; the Play-launch effort moves to v1.8.8).

### Late UI addition — the "Close ROM" toggle  **[DONE]**

The idle/ROM-loaded UI's primary button became an **Open ⇄ Close toggle** (`MainActivity.kt`'s
`closeRom()`): with a ROM running it reads **Close** and unloads the cart (persisting RA progress on
the way out, returning to the idle screen); with no ROM it reads **Open** (the SAF picker). A
host/UI-only change — determinism-neutral.

## Post-v1.8.6 — connectivity completion + on-device sign-off  *(carryover)*

> v1.8.6 brings Android to **near**-desktop parity, but it is not the literal end of the Android line
> the earlier draft implied. A handful of completion items remain — most gated on hardware the test
> cycle lacked (a connected phone; two networked devices) or on a maintainer ops/cost decision (TURN
> hosting). These ship as a small **v1.8.7 "Connectivity completion"** point release (or fold into
> the v1.8.6 finals if the hardware/relay are available before the cut). All are additive / host-side;
> the core stays byte-identical on ARM and AccuracyCoin holds 100% (139/139) on host CI.

**1. Full CGNAT / cross-NAT netplay (TURN).** Promote v1.8.6's direct-IP/LAN netplay to internet play:
stand up + verify the existing `deploy/` relay (coturn STUN/TURN + the signaling server + the Caddy
TLS proxy), wire **ICE candidate exchange** (reuse `rustynes-netplay::signaling` / `stun` /
`mesh_net`) + a **room-code matchmaking** flow into the mobile bridge, and add a **relay-backed
transport** so a session falls back to the TURN relay when hole-punching fails (the desktop/browser
path's documented `TURN relay (RFC 8656)` fallback, `docs/netplay-webrtc.md` §2.1/§3). Then run a
**live cross-NAT test** between two real networks. **Framed as a maintainer ops / cost decision** —
recurring TURN hosting + bandwidth + abuse surface — exactly the carryover `docs/netplay-webrtc.md`
§5 already tracks for the browser path; solving it here solves it once for desktop, browser, Android,
and (later) iOS.

**2. On-device verification sign-offs** (the items the disconnected-phone cycle deferred): live **Lua**
script execution + log overlay; the **RetroAchievements** login round-trip + unlock toasts + `.rap`
sidecar persistence + the hardcore `load_state` gate; and **end-to-end netplay** (needs two networked
devices — both LAN/direct-IP and, once #1 lands, cross-NAT). Until then the plan honestly records
these as **host/CI-verified; on-device sign-off pending**.

> **On-device verification is BATCHED, by maintainer decision (2026-06-20).** Rather than
> gate each interim sideload release on a device pass, the maintainer ships v1.8.6 → v1.8.7 →
> v1.8.8 to **GitHub Releases** on the strength of the host-CI gates (AccuracyCoin 139/139,
> the netplay loopback/mock proofs, the per-feature APK builds), and then does **one
> exhaustive on-device verification pass covering ALL deferred sign-offs across v1.8.6–v1.8.8**
> (Lua / RA / controllers / the controller-aware UI / LAN + room-code netplay / Chromecast,
> etc.). That pass runs **after the v1.8.8 "Atlas" GitHub release is published** and **before
> the Google Play Store production promotion** — so the Play launch is the first release gated
> on a green on-device pass, while the sideload channel stays continuously shippable. The
> live cross-NAT test (#1) folds into the same pass once the `deploy/` relay is hosted.

**3. Remaining Android polish.** Gamepad **remapping** + **turbo/autofire** (carried from v1.8.2 D) —
the small input-polish item that fits the connectivity-completion release. The larger deferred-feature
items are **re-homed into the two follow-on feature releases** below: save-state **`THM ` thumbnails**
and the **library/box-art** work land in **v1.8.8 "Atlas"** (WS C); the **Cast SDK web-Receiver** path
for true Chromecast gameplay output (the v1.8.3 J carryover — Google removed the legacy Cast Remote
Display API, so true Chromecast needs a Cast Receiver we stream frames to) folds into **v1.8.8**'s
capture/cast surface; and the optional **on-device debugger** (long reserved as the "egui debugger
sideload overlay") is **superseded by the native Compose Mesen2-class debugger in v1.8.9 "Workshop"**
(WS A, `debug-hooks`-gated, off the Play build) — a better mobile surface than an embedded egui panel.

**4. Play launch — moved to v1.8.8.** The Google Play debut is **no longer part of this tail**: per
the maintainer (2026-06-20) it now rides **v1.8.8 "Atlas"** (the user-facing enhancement + polish
release), posted on GitHub with release notes before v1.8.9 starts. v1.8.7 stays **sideload /
GitHub-Releases only** (`PLAY_BUILD=false`, full-featured). The full launch mechanics (flip
`PLAY_BUILD`, the `$2.99` `full_unlock` product, Play App Signing, closed-track → production
promotion, and keeping the sideload channel alongside) are in the **v1.8.8** section below.

After v1.8.7 the Android build is **feature-complete on the byte-identical core** (AccuracyCoin
139/139) and connectivity-complete (TURN netplay verified), distributed **sideload / GitHub-Releases
only** (the Play launch is still pending — it lands in v1.8.8). Two further feature releases —
**v1.8.8 "Atlas"** (which **carries the Google Play launch**) and **v1.8.9 "Workshop"** — then close
the remaining **desktop→mobile gap** and deliver **Android-platform excellence** before the platform
focus moves to **v1.9.0 iOS** (`rustynes-mobile` reused — see the iOS cross-reference below) and
**v2.0.0 "Timebase"**. Both are **FUTURE / planned (not released)**; both keep the hard contract —
additive / off-by-default where they touch shared code, the core byte-identical on ARM, AccuracyCoin
100% (139/139) on host CI.

> **Sequencing rationale (why Atlas before Workshop).** v1.8.8 "Atlas" goes **first** because its
> work is the highest **Play-store value immediately after launch** and partly **deadline-driven**:
> (a) **targetSdk 36 / Android 16 adaptive layouts** are a hard Play mandate — new submissions must
> target API 36 by **2026-08-31**, and Android 16 already *ignores* orientation/aspect-ratio
> restrictions on large screens (sw ≥ 600 dp) for API-36 apps, with Android 17 removing the opt-out
> entirely — so the foldable/tablet adaptive pass is non-optional, not a nicety; (b) **cloud saves +
> Play Games achievements + a rich box-art library + screenshot/clip sharing** are the retention and
> store-ranking levers that pay off most right after the freemium listing goes live; and (c) it is
> mostly **idiomatic-Android / Compose host work** over the *existing* bridge — **no new shared-core
> Rust** and **no new on-device-touch-UX research problem**. v1.8.9 "Workshop" goes **second** because
> the creator/power tools are **lower commercial urgency**, are **additive / off-by-default** (so
> there is no Play-deadline pressure), and carry the genuinely **hard mobile-UX research** (a Mesen2-
> class debugger and a TAStudio piano-roll re-imagined for touch) — best tackled once the platform
> shell, adaptive layouts, and large-screen input are already solid (the debugger/TAStudio panels
> *want* the v1.8.8 expanded-width layouts and gamepad/keyboard plumbing). Power-user tools also draw
> a smaller audience, so sequencing them after the mass-market platform polish maximises the
> post-launch growth curve.

## v1.8.8 — "Atlas": Android-platform excellence + library & content **+ the Google Play launch**

> **FUTURE / planned — not released.** Thesis: **make RustyNES feel like a first-class modern
> Android app, not a desktop port** — adaptive across phone/foldable/tablet/TV, with a rich game
> library, cloud-synced saves, Play Games achievements, and native capture/sharing. Every item is a
> **host / Compose / Play-services** change over the *existing* `rustynes-mobile` bridge; **no new
> shared-core Rust** and **no new determinism surface** (saves/library/captures are all views of, or
> wrappers around, the already-byte-identical `.rns`/framebuffer/SRAM the core emits). Distribution:
> **Google Play + sideload** (PGS/cloud/Billing features feature-detected so the `PLAY_BUILD=false`
> sideload build degrades gracefully).
>
> **This is the Google Play launch release** (moved here from v1.8.6 by the maintainer, 2026-06-20):
> because the Play debut presents the most polished build, the freemium **$2.99 Full Unlock** goes
> live on top of the Atlas polish wave — adaptive layouts, library, cloud saves, capture/sharing.
> **Sequencing within the release:** land + verify the Atlas workstreams (A–I) **first**, then run
> the **Play launch (Workstream P)** as the capstone — **post the v1.8.8 GitHub release (with release
> notes) before starting v1.8.9**. v1.8.0–v1.8.7 remain sideload-only (`PLAY_BUILD=false`,
> full-featured); v1.8.8 is the first release to flip `PLAY_BUILD=true` for the Play AAB.

> **How this section is organised (read first).** Because this is **the Play launch**, the v1.8.8
> detail is intentionally the **most exhaustive in the document** — longer + more technically specific
> than the rest of the plan combined (the maintainer asked for this explicitly). The workstreams are
> grouped into three bands: the **platform-foundation band (J → K → A)** — perf/startup, modern-UX,
> adaptive layouts — which is **engineering that protects Android-vitals + the store ranking and must
> land first**; the **native-feel + content band (B–I)** — Material You/i18n, library, cloud, capture,
> TV, quick-surfaces, accessibility; and the **launch band (L → P)** — Play quality/integrity then the
> Play production capstone. Within the release: **J, K, A, then L's integrity wiring** are the hard
> floors (a janky / crashy / non-adaptive / un-attested build endangers the listing); **B–I** are the
> polish wave; **P** is run dead-last after everything else verifies. Every research claim below carries
> an inline official source URL; the load-bearing dates/thresholds were independently re-verified
> (2026-06-20). All of this is **host / Compose / Gradle / Play-Console / NDK-build** work over the
> *existing* `rustynes-mobile` bridge — **no new shared-core Rust, no new determinism surface.**

| WS | Theme | Headline | Effort |
|---|---|---|---|
| **J** | **Performance, startup & app-size (the vitals floor)** | **Baseline + Startup Profiles** (`androidx.baselineprofile` plugin + a `:baselineprofile` Macrobenchmark module → ~20–30% faster cold start, fewer frozen frames/ANRs); **App Startup** consolidation + deferred Billing/Cast/bridge init off the cold path; **Compose-perf** pass (strong-skipping already default, `ImmutableList` ROM/state lists, lazy `key`/`contentType`, compiler metrics in CI); **R8 full-mode + resource-shrink** with constructor-aware UniFFI/JNA/JNI keep rules; **frame pacing** of the wgpu `SurfaceView` to the exact NES rate via `Surface.setFrameRate` + `AChoreographer` VSync + ADPF; **AAudio/Oboe** 48 kHz low-latency/exclusive tuning; **ADPF + thermal governor**; **NDK release** (`SYMBOL_TABLE` symbols to Play, LTO/strip, 16 KB verified). Measurable targets, Macrobenchmark-gated | L |
| **K** | **Modern Android UX (edge-to-edge, predictive back, splash, M3 Expressive)** | **Edge-to-edge** (`enableEdgeToEdge()` + `WindowInsets.safeDrawing`, now *enforced* at SDK 35+ — no opt-out at 36); **predictive-back** (`OnBackPressedCallback`/`PredictiveBackHandler`, `enableOnBackInvokedCallback`; required before the 36 bump since `onBackPressed` stops firing); the **Splash Screen API** (`core-splashscreen`, hold-until-first-frame); a **Material 3 / Expressive** theming + motion pass; **adaptive + themed/monochrome** launcher icon; **task-description** recents card; richer **haptics** | M |
| **A** | **Adaptive layouts + targetSdk 36** | Bump **targetSdk/compileSdk 36** (Play **2026-08-31** mandate); drive the whole UI off **Window Size Classes** (`currentWindowAdaptiveInfo().windowSizeClass`, compact/medium/expanded/large/x-large) so phone, the Z Fold's two displays, tablets, and free-form/desktop/ChromeOS windows all lay out correctly; foldable-posture awareness (`WindowInfoTracker`); an **expanded two-pane** (`NavigableListDetailPaneScaffold`: library rail beside the player); decide the **`appCategory="game"`** large-screen-override posture; renderer-side NES pillarboxing so any aspect ratio is correct | M |
| **B** | **Material You / theming + i18n** | **Dynamic color** (`dynamicLightColorScheme`/`dynamicDarkColorScheme`, API 31+, brand fallback below) folded into K's theme; per-app **in-app language** (`AppCompatDelegate.setApplicationLocales` / framework `LocaleManager`, API 33+, `locales_config.xml`) wiring the desktop **i18n catalog** (English + Spanish, ADR 0023) into the Compose strings | M |
| **C** | **Game library / content** | A **box-art library** — folders / favorites / full-text search / recents / last-played / metadata, **`THM `-style save-state thumbnails** (carries the v1.8.3 / post-v1.8.6 thumbnail item), batch **SAF tree import** (`ACTION_OPEN_DOCUMENT_TREE` + persistable grants); a per-game tile grid keyed by **ROM SHA-256** (reuse the per-game DB); user-supplied / locally-derived art only (no bundled/network art) | L |
| **D** | **Cloud save sync (Play Games Saved Games)** | Optional **PGS v2 Saved Games / Snapshots** sync of the `<rom-sha256>/slot-N.rns` states + SRAM, **per-ROM-slot snapshot units**, with a documented **conflict-resolution** policy (last-write-wins by in-game frame count + a manual picker on true conflicts); cross-device + reinstall restore; **desktop⇄Android `.rns` portability** as a first-class cloud feature | L |
| **E** | **Play Games achievements / leaderboards (optional, distinct from RA)** | A thin **PGS v2 achievements + leaderboards** layer for *platform* milestones (ROMs played, hours, library size) — **NOT** a substitute for the RetroAchievements integration shipped in v1.8.6; the two coexist (RA = per-game retro sets; PGS = app-level Play UX); PGS v2 auto-sign-in also anchors D's cloud account | M |
| **F** | **Capture & sharing** | **Screenshot** (the framebuffer → `MediaStore` Pictures, the desktop clipboard-screenshot's Android re-map) + **gameplay clip recording** (`MediaCodec`/`MediaRecorder` over the render surface) → Android **share-sheet**; both gameplay-only (never the controller/menu, reusing the v1.8.3 cast framebuffer-isolation) | M |
| **G** | **Android TV / Leanback + d-pad** | The **Leanback** launcher entry (already in the manifest) + **full d-pad navigation** of the library/menus (no touch required); the gamepad is already P1, so a TV box + controller is a complete experience; TV banner + the TV app-quality tier (overscan-safe margins, large focus targets) | M |
| **H** | **Quick-tile / shortcuts / widget / PiP** | A **Quick Settings tile** (`TileService`) + dynamic **app shortcuts** (jump to recent ROMs) + a **resume** home-screen widget (`androidx.glance`); **picture-in-picture** (`enterPictureInPictureMode`, 8:7-PAR aspect) so a game keeps running in a corner when backgrounded (gameplay-only PiP surface, controller hidden) | M |
| **I** | **Accessibility** | **TalkBack** content-descriptions on every control (the virtual pad, menus, library), **switch-access** focus order, scalable UI respecting the **system font scale** + the desktop **high-contrast / Okabe-Ito colorblind** themes brought to Compose | M |
| **L** | **Play quality & integrity** | **Play Integrity API** (replaces the now-defunct SafetyNet; `LICENSED` + `PLAY_RECOGNIZED` + `MEETS_DEVICE_INTEGRITY` as the **anti-tamper layer over Billing** — Billing stays the entitlement source of truth); **in-app update** + **in-app review** APIs; an **opt-in crash/vitals** path; a **pre-launch-report** + **Android-vitals** clean-up pass to clear the bad-behavior thresholds (crash < 1.09% / ANR < 0.47% / per-device < 8% / the 2026 wake-lock rule) before promotion | M |
| **P** | **Google Play launch (the release capstone)** | Flip the **`PLAY_BUILD`** flavor on for the Play AAB (freemium **$2.99 Full Unlock** + the 8-minute demo activate; sideload stays `PLAY_BUILD=false` / full-featured); create the **`full_unlock`** managed product at **$2.99** in the Play Console; enroll in **Play App Signing**; finalize IARC rating + data-safety (no data collected) + the store listing (assets prepared from v1.8.3); the **12-tester / 14-day closed test** (personal-account gate) → **license-tested** purchase-flow verification → **promote to production**; **post the v1.8.8 GitHub release with release notes** before v1.8.9 begins | M *(mostly non-engineering)* |

### Per-workstream detail

**J — Performance, startup & app-size (the Android-vitals floor).** This is the **single most
important engineering workstream of the release**: the Play store ranks + gates on Android vitals, so
a fast, jank-free, small build is not polish — it is what keeps the listing discoverable. Critical
framing: Baseline Profiles / R8 / App Startup optimise only the **Kotlin/Java/ART (DEX) layer** (the
Compose shell, Activity/`SurfaceView` startup, JNI/UniFFI marshalling, SDK init) — they do **not**
touch the already-native Rust `.so` or wgpu throughput; that side is governed by frame pacing + ADPF
+ NDK build below. Items:

- **Baseline + Startup Profiles.** Add the `androidx.baselineprofile` Gradle plugin (**v1.4.1**) and a
  dedicated **`:baselineprofile`** `com.android.test` module (`targetProjectPath = ":app"`, a
  `ManagedVirtualDevice` `aosp` image, `useConnectedDevices = false`); in `:app` add
  `baselineProfile(project(":baselineprofile"))` + `androidx.profileinstaller:profileinstaller:1.4.1`.
  Write a `BaselineProfileRule.collect(includeInStartupProfile = true) { startActivityAndWait(); /*
  uiAutomator launch→first-ROM-frame journey */ }`, binding TTFD past the **async JNI ROM load** with
  `ReportDrawnAfter {}` / `FullyDrawnReporter`. `automaticGenerationDuringBuild` on in **release CI**;
  AGP compiles the rules to `assets/dexopt/baseline.prof` (< 1.5 MB); **Startup Profiles**
  (`startup-prof.txt`) reorder the launch classes into the first `classes.dex` (DEX-layout opt, on by
  default in **AGP 8.3+**) for a further **15–30%** over Baseline Profiles alone. Reported real-world
  wins: ~**20% faster cold start** (up to **40%** on low/mid-tier), ~**30% fewer ANRs**, fewer P95
  frozen frames. Sources: <https://developer.android.com/topic/performance/baselineprofiles/overview>
  · <https://developer.android.com/topic/performance/baselineprofiles/dex-layout-optimizations> ·
  <https://android-developers.googleblog.com/2024/12/reddit-improved-app-startup-speed-using-baseline-profiles-r8.html>.
- **App Startup + deferred SDK init.** Consolidate initialisers under one
  `androidx.startup:startup-runtime:1.2.0` `InitializationProvider`; keep **Play Billing**
  `startConnection()` and **Cast** `CastContext.getSharedInstance()` **off `Application.onCreate()`**
  (lazy, on first foreground / the relevant screen — Billing's `BillingClient` and Cast's
  `CastContext` are both designed to init lazily), and defer the **bridge `.so` load + ROM-DB / save
  scan** to `Dispatchers.IO`. Signal `reportFullyDrawn()` / Compose `ReportDrawnAfter {}` **after the
  first emulated frame**. Stay well under the Play excessive-startup thresholds — **cold ≥ 5 s / warm
  ≥ 2 s / hot ≥ 1.5 s** are the bad-behaviour lines.
  <https://developer.android.com/topic/performance/vitals/launch-time>
- **Compose performance.** **Strong-skipping is already default** (Kotlin ≥ 2.0.20), so the wins are:
  wrap the ROM-browser / save-state / library lists in **`ImmutableList`**
  (`org.jetbrains.kotlinx:kotlinx-collections-immutable`) so they read as stable, give every
  `LazyColumn`/`LazyVerticalGrid` a stable **`key`** + **`contentType`**, defer per-frame overlay
  reads with lambda-modifiers (`Modifier.offset { }` / `drawBehind { }`) + `derivedStateOf`, and turn
  on the **Compose compiler metrics/reports** (`composeCompiler { reportsDestination/metricsDestination
  }`) on a release build in CI to catch any non-skippable composable.
  <https://developer.android.com/develop/ui/compose/performance/stability/fix>
- **R8 full-mode + resource shrink.** R8 full mode is **default-on since AGP 8.0** (no flag needed);
  keep `isMinifyEnabled` + `isShrinkResources = true` + `proguard-android-optimize.txt`. **Gotcha
  (AGP 9.x `strictFullModeForKeepRules`): a bare `-keep class Foo` no longer keeps constructors** — so
  the **UniFFI / JNA / JNI** keep block must spell out `{ <init>(...); }` and use the Mozilla
  application-services JNA ruleset (`-keep class com.sun.jna.** { *; }` + the descriptor-class native
  keep + `Signature`/annotation attributes). `.so` files are never shrunk by R8; only the class
  holding the `native` methods must be kept.
  <https://developer.android.com/topic/performance/app-optimization/keep-rule-examples> ·
  <https://github.com/mozilla/application-services/blob/main/proguard-rules-consumer-jna.pro>
- **Frame pacing of the wgpu `SurfaceView`.** Swappy/AGDK frame-pacing is C/C++ for GL/Vulkan and is
  **not a wgpu drop-in**, so do what Swappy does by hand: call
  **`Surface.setFrameRate(60.0988f, FRAME_RATE_COMPATIBILITY_DEFAULT)`** with the *exact*
  master-clock-derived NES NTSC rate (not `60` — Android 15 defaults games to 60 Hz otherwise), pace
  the loop on the **VSync timestamp** via `AChoreographer_postVsyncCallback` (API 33+) /
  `Choreographer.postFrameCallback`, advance core state by elapsed time on the dedicated emu thread,
  and enable **`VK_GOOGLE_display_timing`** on the Vulkan backend to kill judder on 90/120 Hz panels.
  Gate releases on Macrobenchmark **`FrameTimingMetric` P99 `frameOverrunMs ≤ 0`**.
  <https://developer.android.com/media/optimize/performance/frame-rate> ·
  <https://developer.android.com/games/optimize/display-refresh-rate-change>
- **AAudio / Oboe low-latency.** Pin the native-audio hot path (from v1.8.4) to the recommended
  config: **Oboe** → AAudio with **`LowLatency`** + **`Exclusive`** (MMAP) + `Usage::Game` + Float
  PCM, **no `setSampleRate`** (use the device-native **48 kHz** and resample the APU output to 48 kHz
  in the Rust core — 44.1 kHz via AAudio costs ~160 ms vs ~20 ms), buffer = 2× `framesPerBurst`, the
  data-callback draining the existing lock-free ring only (no alloc/lock/IO in the callback), with
  `LatencyTuner` auto-correcting underruns. Target ~20 ms round-trip.
  <https://developer.android.com/games/sdk/oboe/low-latency-audio>
- **ADPF + thermal governor (battery + sustained perf).** Open one **`PerformanceHintManager`** hint
  session over the emu + render thread IDs with the NES frame budget as the target
  (60.0988 Hz ≈ **16.64 ms**), `reportActualWorkDuration` **per frame** (the SoC then ramps to hit the
  deadline and drops clocks on early frames — a real battery win), `setPreferPowerEfficiency(true)` on
  API 35+. Extend the **existing thermal fast-forward backoff** into a real governor:
  `getThermalHeadroom()` / `addThermalStatusListener`, shedding the GPU NTSC/Bisqwit filter →
  resolution → frame-skip before `SEVERE`, with a user toggle. No foreground service is needed (run in
  a normal Activity, pause emulation on `onStop()`, `FLAG_KEEP_SCREEN_ON` during play — a screen-on
  flag, **not** a partial wake lock, which dodges the **March 2026** wake-lock vitals rule).
  <https://developer.android.com/games/optimize/adpf> ·
  <https://developer.android.com/topic/performance/vitals/excessive-wakelock>
- **NDK release build.** On **NDK r29** the `.so` is **16 KB page-aligned by default** (the Nov 2025
  Play gate is satisfied with no flags — verify in CI with `check_elf_alignment.sh` /
  `bundletool dump config … | grep PAGE_ALIGNMENT_16K`). Release Rust profile: `opt-level = 3`,
  `lto = "fat"`, `codegen-units = 1`, `panic = "abort"`; upload **`debugSymbolLevel = "SYMBOL_TABLE"`**
  native symbols to Play for readable crash stacks (`FULL` can blow the ~300 MB cap for a Rust core).
  Ship the **AAB** (already arm64-only release) and keep `language { enableSplit = false }` so the
  in-app EN/ES picker can't hit a missing-resource crash.
  <https://developer.android.com/guide/practices/page-sizes> ·
  <https://developer.android.com/build/include-native-symbols>

Verifiable: **host CI / Macrobenchmark** for the measurable numbers (cold-start delta `None()` vs
`Partial(BaselineProfileMode.Require)`, frame-overrun P99, alignment); **on-device / AVD** for the
felt result. Bridge: **none** (the one core-touching item — APU resample to 48 kHz — is a
frontend-resampler concern, determinism-neutral). **Effort: L.**

**K — Modern Android UX (edge-to-edge, predictive back, splash, Material 3 / Expressive).** The
behaviours a 2026 user expects from a native app, several of which are now **mandatory** under
targetSdk 35/36:

- **Edge-to-edge (enforced).** Apps targeting SDK 35+ draw edge-to-edge by default on Android 15+, and
  **at API 36 the temporary opt-out is gone** — so call **`enableEdgeToEdge()`** in `onCreate` and
  consume insets with `WindowInsets.safeDrawing` / `Modifier.safeDrawingPadding()` / the M3
  `Scaffold(contentWindowInsets = …)`. The old `Window.setStatusBarColor`/`setNavigationBarColor` are
  deprecated/ignored for gesture nav; system bars are transparent. The NES surface must letterbox
  inside `safeDrawing` so it never sits under the status/nav bar or a display cutout.
  <https://developer.android.com/develop/ui/compose/layouts/insets> ·
  <https://developer.android.com/about/versions/15/behavior-changes-15>
- **Predictive back (required before the 36 bump).** For apps targeting **API 36 on Android 16+**,
  `Activity.onBackPressed` is no longer called and `KEYCODE_BACK` is no longer dispatched — so migrate
  every back handler to **`OnBackPressedDispatcher` + `OnBackPressedCallback`** (always fires) /
  Compose **`PredictiveBackHandler { progress -> … }`** / `BackHandler`, set
  `android:enableOnBackInvokedCallback="true"`, and drive enablement via `isEnabled`. The adaptive
  `…PaneScaffold` navigators (WS A) already wire predictive-back for list↔detail.
  <https://developer.android.com/guide/navigation/custom-back/predictive-back-gesture>
- **Splash Screen API.** Add **`androidx.core:core-splashscreen`** (`installSplashScreen()` before
  `super.onCreate()`, `setKeepOnScreenCondition { }` to hold during the bridge/ROM-DB load) so the
  Android-12+ system splash uses the RustyNES icon/brand instead of a blank window.
  <https://developer.android.com/develop/ui/views/launch/splash-screen>
- **Material 3 / Expressive theming + motion.** Everything is in `androidx.compose.material3:material3`.
  **Version reality (so we ship the right thing):** Material 3 **Expressive** (Google I/O 2025 —
  spring-physics `MotionScheme`, expressive shapes/typography, ~15 new/updated components like
  `ButtonGroup`/`FloatingToolbar`/`SplitButton`/wavy progress) is **not fully in the `1.4.0` stable
  line** (those APIs moved to the **`1.5.0-alpha`** line behind `@ExperimentalMaterial3ExpressiveApi`).
  **Plan: ship the stable `1.4.x` M3 set** (updated type scale, shapes, `MotionScheme`) for the launch;
  opt into specific `1.5.0-alpha` Expressive components **only** where the polish is worth an
  experimental opt-in. Either way, apply a real theme + typography + motion pass so the shell reads as
  Material 3, not ported desktop chrome.
  <https://developer.android.com/jetpack/androidx/releases/compose-material3> ·
  <https://m3.material.io/blog/building-with-m3-expressive>
- **Adaptive + themed launcher icon.** Ship an `<adaptive-icon>` with a **`<monochrome>`** layer so the
  Material You **themed icon** works on API 33+ (108×108 dp canvas, 66 dp safe zone), plus a recents
  **`setTaskDescription`** card and a richer **haptics** pass (`View.performHapticFeedback` /
  `LocalHapticFeedback` constants — no `VIBRATE` permission, automatic device fallback — over the
  current `VibrationEffect` path, gating any primitives with `arePrimitivesSupported()`).
  <https://developer.android.com/develop/ui/views/haptics/haptic-feedback>

Verifiable: structurally in **host CI**; the felt behaviour (back animation, splash, insets, themed
icon, haptics) is **on-device / AVD**. Bridge: **none**. **Effort: M.**

**A — Adaptive layouts + targetSdk 36.** Bump `targetSdk`/`compileSdk` to **36** (the Play
**2026-08-31** mandate; new submissions and updates after that date must target Android 16 — phones
and tablets; Wear/TV lag one level at 35). Under API 36, Android **ignores
`screenOrientation` / `resizeableActivity` / `minAspectRatio` / `maxAspectRatio` and runtime
`setRequestedOrientation()` on large screens** (smallest width ≥ **600 dp** — tablets, the Z Fold
inner display, free-form / desktop / ChromeOS windows), the temporary opt-out
(`PROPERTY_COMPAT_ALLOW_RESTRICTED_RESIZABILITY`) is **removed at API 37**, and there is a **`games`
exception** (`android:appCategory="game"` keeps the manifest orientation/aspect honored). **Decision
(maintainer to confirm):** even though tagging `appCategory="game"` would *exempt* RustyNES from the
override, the plan still **embraces adaptive layouts** — it is the larger store-ranking + quality win
on tablets/foldables and future-proofs API 37 — and lets the **wgpu renderer pillarbox the NES image**
at any aspect ratio rather than relying on a manifest orientation lock. Replace the current ad-hoc
`configChanges` posture handling with **`WindowSizeClass`** (via `currentWindowAdaptiveInfo().windowSizeClass`
from `androidx.compose.material3.adaptive:adaptive` **1.2.0**; width breakpoints compact < 600 /
medium ≥ 600 / expanded ≥ 840 / large ≥ 1200 / x-large ≥ 1600) as the **single layout driver**, add
foldable-posture awareness (`WindowInfoTracker` / `FoldingFeature` for tabletop/book postures), and an
**expanded two-pane** layout via **`NavigableListDetailPaneScaffold`** (the library/recents rail beside
the player) for tablets, the Z Fold inner display, and free-form windows. Keep the activity surviving
config changes so the emu thread + GL surface persist. Deps: `androidx.compose.material3.adaptive:adaptive(-layout/-navigation):1.2.0` + `androidx.window:window:1.5.1`.
Verifiable in **host CI only structurally** (the Kotlin compiles against compileSdk 36); the adaptive
behaviour is **on-device / emulator-AVD** (foldable + tablet + TV + free-form AVDs). Bridge: **none**.
Sources: <https://developer.android.com/about/versions/16/behavior-changes-16> ·
<https://developer.android.com/develop/ui/compose/layouts/adaptive/use-window-size-classes> ·
<https://developer.android.com/develop/ui/compose/layouts/adaptive/list-detail>. **Effort: M.**

**B — Material You + i18n.** Fold **dynamic color** (`dynamicLightColorScheme`/`dynamicDarkColorScheme`,
API 31+, falling back to the brand palette below, guarded by `Build.VERSION.SDK_INT >= S`) into K's
Material 3 theme so the shell adopts the system wallpaper palette where available. Wire **per-app
language** via `AppCompatDelegate.setApplicationLocales` / the framework `LocaleManager` (API 33+, with
the AppCompat back-compat shim) and a `locales_config.xml`, and bring the **desktop i18n string catalog
(English + Spanish, ADR 0023)** into the Compose string resources — the desktop already proved the
catalog; Android consumes the same translations. Note: because the AAB keeps
`language { enableSplit = false }` (WS J), the in-app picker can switch to a locale whose resources are
guaranteed present. Bridge: **none** (the catalog is host strings; the core emits no UI text).
<https://developer.android.com/develop/ui/compose/designsystems/material3>. **Effort: M.**

**C — Game library / content.** Grow the recents list into a **box-art library**: a tile grid keyed
by **ROM SHA-256** (reuse the per-game DB from v1.8.5), with **folders, favorites, full-text search,
last-played, and metadata**, plus **`THM `-style save-state thumbnails** (the carried thumbnail item
from v1.8.3 / the post-v1.8.6 tail — render the framebuffer to a small bitmap at save time, store
beside the `.rns`). Add **batch import** over a SAF **`ACTION_OPEN_DOCUMENT_TREE`** with persistable
grants (the storage model is already in place; this scans a folder and registers every NES/`.zip`
entry). Box art is **user-supplied / locally-derived only** (no bundled or network-fetched art — the
no-bundled-content posture holds). Bridge: at most a tiny `rom_header`/`rom_sha` helper if not already
exposed; otherwise host-only. **Effort: L.**

**D — Cloud save sync (Play Games Saved Games).** Add **optional** PGS v2 **Saved Games / Snapshots**
sync of the per-ROM `<rom-sha256>/slot-N.rns` states + on-cart SRAM. Use the documented Snapshots
pattern: **divide saves into independently-updateable units** (per-ROM-slot snapshots, not one
monolith) to minimise IO, and ship an explicit **conflict-resolution** policy — default
**last-write-wins keyed on the in-game frame count baked into the `.rns`**, with a **manual picker**
surfaced on a true divergent conflict (the Snapshots API hands both states to a resolver). Because the
`.rns` format is **platform-independent**, this also makes **desktop⇄Android save portability** a
first-class cloud feature, not just a manual copy. Feature-detected behind `PLAY_BUILD` / Play-services
availability; the sideload build keeps purely-local saves. Bridge: **none** (operates on the existing
`.rns` bytes; the core is untouched, so accuracy/determinism hold). **Effort: L.**

**E — Play Games achievements / leaderboards (optional, kept distinct from RA).** A thin **PGS v2
achievements + leaderboards** layer for **app-level / platform milestones** (number of ROMs played,
total play time, library size, first cloud sync, etc.). This is **deliberately separate from the
RetroAchievements integration shipped in v1.8.6** — RA is the per-game retro-achievement community
service (`rustynes-ra`, hardcore mode, `.rap` sidecars); PGS is the native Google Play games-UX layer
(the Play Games app's achievement/leaderboard surface). They **coexist and never collide**. PGS sign-in
performs **platform authentication at launch** (PGS v2 auto-signs-in), so this also gives a clean
account anchor for D's cloud sync. Feature-detected; absent on sideload. Bridge: **none**. **Effort: M.**

**F — Capture & sharing.** Re-map the desktop **clipboard-screenshot** to Android: write the current
**gameplay framebuffer** (the same `ImageBitmap`/surface the renderer already produces) to **`MediaStore`
Pictures** and offer the **share-sheet**. Add **gameplay clip recording** — `MediaRecorder`/`MediaCodec`
encoding the render **surface** (the wgpu `SurfaceView` from v1.8.4 or the Bitmap path) to an MP4 → share.
Both are **gameplay-only** (controller + menu excluded), reusing the v1.8.3 cast framebuffer-isolation so
the capture is the clean NES image. Determinism-neutral (read-only of the presented frame). Bridge:
**none**. **Effort: M.**

**G — Android TV / Leanback + d-pad.** Add a **Leanback** launcher `<intent-filter>` + a TV banner so
RustyNES appears on Android TV / Google TV home, and make the **entire library + menu d-pad-navigable**
(focus order, no touch dependency) — the hardware gamepad already drives P1, so a TV box + a controller
is a complete sit-down experience. Honor the TV app-quality tier (overscan-safe margins, large focus
targets). Pairs naturally with A's expanded layout. Bridge: **none** (input already converges on the one
late-latched mask). **Effort: M.**

**H — Quick-tile / shortcuts / widget / PiP.** A **Quick Settings tile** (`TileService`) to jump
straight into the last ROM; **dynamic app shortcuts** (long-press launcher → recent ROMs); a small
**resume** home-screen widget; and **picture-in-picture** (`enterPictureInPictureMode` with an
`aspectRatio` matching the 8:7-PAR NES image) so a game keeps running in a floating window when the user
multitasks — the **gameplay-only** surface (no controller/menu in the PiP), reusing F's/cast's framebuffer
isolation. PiP needs the activity in a resumed render state, which the v1.8.4 surface-loss lifecycle
already models. Bridge: **none**. **Effort: M.**

**I — Accessibility.** Add **TalkBack** content-descriptions to every interactive surface (the virtual
controller buttons, menu bar, library tiles, settings), a sensible **switch-access** focus traversal,
respect the **system font scale** in the Compose typography, and bring the desktop **high-contrast +
Okabe-Ito colorblind** themes (the v1.5.0 accessibility set) into the Compose theme. Improves the Play
listing's accessibility signals and broadens reach. Bridge: **none**. **Effort: M.**

**L — Play quality & integrity.** The non-feature engineering that protects the listing and the
freemium revenue. Run before P (the integrity wiring gates the production purchase flow):

- **Play Integrity API (anti-tamper over Billing).** **SafetyNet Attestation was fully turned down in
  January 2025** — the modern replacement is the **Play Integrity API** (`com.google.android.play:integrity:1.6.0`).
  **Key nuance:** `accountDetails.appLicensingVerdict == "LICENSED"` proves the user got the binary
  from Play, but it is **not a purchase record** — for the **$2.99 Full Unlock** the **source of truth
  stays Play Billing** (`queryPurchasesAsync` + server-side verification, already built in
  `Billing.kt`/`LicenseManager`). Use Play Integrity as the **anti-tamper / anti-piracy layer** that
  confirms a genuine, uncompromised binary (`PLAY_RECOGNIZED` + `MEETS_DEVICE_INTEGRITY` +
  `LICENSED`) before honoring or restoring the unlock; prefer the **Standard** request (warmed
  `prepareIntegrityToken`, replay-protected, few-hundred-ms) and **evaluate verdicts server-side**
  (don't cache/trust on-device). Feature-detected behind `PLAY_BUILD` (sideload skips it). Reported
  ~80% reduction in unauthorized usage. Sources:
  <https://developer.android.com/google/play/integrity/overview> ·
  <https://developer.android.com/google/play/integrity/verdicts> ·
  <https://developer.android.com/privacy-and-security/safetynet/deprecation-timeline>.
- **In-app update + in-app review.** Add **in-app updates**
  (`com.google.android.play:app-update(-ktx):2.1.0` — flexible for normal updates, immediate reserved
  for a critical fix; **2.1.0 is the minimum for targeting Android 14+**) and **in-app review**
  (`com.google.android.play:review(-ktx):2.0.2`, `ReviewManagerFactory` → `requestReviewFlow` →
  `launchReviewFlow`, triggered sparingly after a satisfying session — no CTA button, respect the
  quota). Both feature-detected (no-op on sideload). Sources:
  <https://developer.android.com/guide/playcore/in-app-updates> ·
  <https://developer.android.com/guide/playcore/in-app-review>.
- **Opt-in crash/vitals.** The cross-cutting **opt-in crash reporting** item from the release table —
  a privacy-respecting (off by default, explicit consent) crash/ANR reporter so post-launch
  Android-vitals regressions are diagnosable. Keep it **opt-in** to preserve the "collects nothing by
  default" data-safety posture.
- **Pre-launch-report + Android-vitals pass.** Before promoting, run the **pre-launch report** (the
  automated Robo crawl on real Firebase Test Lab devices — auto-runs on closed/open uploads, surfaces
  stability/perf/accessibility/security findings) and do a vitals clean-up pass against the **exact
  bad-behaviour thresholds**: user-perceived **crash rate < 1.09%**, **ANR rate < 0.47%** (input not
  handled within 5 s — never block the UI thread on the native bridge), **per-device < 8%** (one bad
  SoC family can trip a store warning even with a healthy global average), and the **partial-wake-lock
  rule enforced from March 2026** (which J's no-foreground-service / `FLAG_KEEP_SCREEN_ON` design
  already avoids). Because RustyNES would list under **Games**, its 60 fps emulation clears the games
  slow-session gate (Play steers users away from games that can't present a frame within 50 ms / hit
  ~20 fps) easily. Sources:
  <https://support.google.com/googleplay/android-developer/answer/9844487> ·
  <https://developer.android.com/topic/performance/vitals> ·
  <https://developer.android.com/topic/performance/vitals/anr>.

Verifiable: the integrity/update/review wiring compiles in **host CI**; the verdict round-trip, the
pre-launch report, and the vitals dashboard are **Play-Console-gated** (maintainer ops — needs the
linked Cloud project + a server endpoint for verdict decryption). Bridge: **none**. **Effort: M.**

**P — Google Play launch (the release capstone; moved here from v1.8.6, 2026-06-20).** This is the
release where RustyNES debuts on the **Google Play Store** — held to v1.8.8 by the maintainer so the
store listing presents the most polished build (the Atlas polish wave above). Run it **last** in the
release, after Workstreams **J, K, A, then B–I, then L** land + verify (the perf/UX/adaptive floor and
the integrity wiring must be in place before the production purchase flow). The freemium wiring (the
session timer + the three persistence locks + the Play Billing `LicenseManager`) was built and verified
end-to-end back in v1.8.0/v1.8.1 and has sat dormant behind `PLAY_BUILD=false`; first confirm that
wiring still holds in `android/app/build.gradle.kts` + the `Billing.kt`/`LicenseManager` Billing code,
then:

1. Flip the **`PLAY_BUILD`** flavor on for the Play AAB (the freemium **$2.99 Full Unlock** + the
   8-minute demo become active); sideload / GitHub-Releases builds stay `PLAY_BUILD=false` /
   full-featured. This is the **first** release to ship `PLAY_BUILD=true`.
2. Create the **`full_unlock`** managed (non-consumable) product at **$2.99** in the Play Console;
   enroll in **Play App Signing** with the upload keystore (you hold the upload key, RSA ≥ 2048;
   Google holds the app signing key); finalize the **IARC content-rating** questionnaire + the
   **data-safety** form (no data collected — the opt-in crash reporter from L is declared accordingly
   if shipped) + the store listing assets (prepared from v1.8.3, refreshed with the Atlas
   adaptive/library/capture screenshots).
3. **Testing-track ladder.** Internal (≤ 100 testers, live in minutes) for the smoke pass → **closed
   testing**, which also satisfies the **personal-account production gate** if it applies (accounts
   created after 2023-11-13 must run a closed test with **≥ 12 testers continuously opted-in ≥ 14
   days** — the number was **lowered from 20 to 12 in Dec 2024**; org accounts are exempt). Run the
   **pre-launch report** (WS L) and a **license-tested** verification of the real purchase + Full-Unlock
   flow (Play Console license-test accounts get a test purchase) → **promote the reviewed release to
   production** with a **staged rollout %**, keeping the guaranteed sideload / GitHub-Releases channel
   alongside. Source:
   <https://support.google.com/googleplay/android-developer/answer/14151465>.
4. **Post the v1.8.8 GitHub release with release notes** (the public changelog for the Atlas wave +
   the Play debut) — and do this **before starting v1.8.9 "Workshop."**

Bridge: **none** (Play-Console + Gradle-flavor + Billing host work). **Effort: M** (mostly
non-engineering). **Maintainer ops (account-gated, can't be CI-self-certified):** the Play Console
product/listing/rating/data-safety setup, Play App Signing enrollment, the linked Cloud project +
verdict-decryption endpoint for Play Integrity (L), the 12-tester closed test, and the production
promotion.

### Verification (v1.8.8)

- **Host CI (authoritative, unchanged):** the whole workspace still builds + AccuracyCoin 100%
  (139/139) — **no shared-core change**, so this is a free pass by construction; the Android crates
  cross-compile (arm64 + x86_64), the Kotlin compiles against compileSdk 36, the `.so` is **16 KB
  page-aligned** (`check_elf_alignment.sh` / `bundletool … | grep PAGE_ALIGNMENT_16K`), and the
  Compose **compiler metrics** report no unexpected non-skippable composable (J).
- **Macrobenchmark (host-runnable, device/AVD-backed; the J number gates):** cold-start
  `StartupTimingMetric` with `CompilationMode.None()` vs `Partial(BaselineProfileMode.Require)` shows
  the Baseline-Profile delta; `FrameTimingMetric` **P99 `frameOverrunMs ≤ 0`** during a ROM run; the
  TTFD trace section covers the async JNI ROM load (J).
- **On-device / AVD (manual):** a fast cold start + a smooth Compose menu (J); edge-to-edge insets,
  predictive-back animation, the splash hold, the themed icon + haptics (K); foldable + tablet + TV +
  free-form AVDs each get a correct adaptive layout + a working two-pane (A); dynamic color + a Spanish
  locale switch (B); library folders/search/favorites + thumbnails + a batch tree import (C); a
  cloud-save round-trip across two signed-in devices with a forced conflict (D); a PGS achievement
  unlock visible in the Play Games app (E); a screenshot to the gallery + a recorded clip shared (F);
  full d-pad library navigation on a TV AVD (G); the quick tile, a shortcut, the widget, and PiP (H);
  TalkBack reads every control + a font-scale + colorblind-theme pass (I); an in-app update + in-app
  review prompt + a Play-Integrity verdict round-trip (L).
- **Graceful degradation:** the sideload (`PLAY_BUILD=false`, no Play services) build hides D / E / the
  Integrity + update/review paths (L) and keeps everything else; J's perf/UX/adaptive work (J/K/A/B/C/
  F/G/H/I) is present in both flavors.
- **Play launch (P):** the `PLAY_BUILD=true` AAB activates the freemium timer + the three persistence
  locks; the `full_unlock` $2.99 purchase removes the timer and enables save-states / resume / SRAM,
  with Play Integrity (L) confirming a genuine binary before honoring the unlock and Billing as the
  entitlement source of truth; the **pre-launch report** is clean and **Android vitals** are inside the
  bad-behaviour thresholds; a **12-tester / 14-day closed test** + a license-tested run confirms the
  real purchase + Full-Unlock flow before the production promotion. The GitHub release (with notes) is
  posted before v1.8.9 begins.

### Critical files / crates (v1.8.8)

- **Android (Kotlin, mostly new):** `Library.kt` (box-art grid / folders / search), `CloudSync.kt`
  (PGS Snapshots), `PlayGames.kt` (PGS sign-in / achievements / leaderboards), `Capture.kt`
  (screenshot + clip), `Tv.kt`/Leanback manifest entries (the `LEANBACK_LAUNCHER` filter already
  exists), `QuickTile.kt`/`Shortcuts.kt`/`Widget.kt`, PiP wiring in `MainActivity.kt`; `Integrity.kt`
  (Play Integrity verdict request), `PlayUpdates.kt` (in-app update + review), `Crash.kt` (opt-in
  reporter); `Theme.kt` (edge-to-edge + Material 3 + dynamic color + splash), `Pacing.kt` /
  `NesSurfaceView.kt` (`setFrameRate` + `AChoreographer` + ADPF), `Thermal.kt` (governor extension);
  `Settings.kt` (language picker, theme, accessibility, cloud toggle, integrity/crash opt-ins); the
  **adaptive refactor of `MainActivity.kt`** to `WindowSizeClass` + `enableEdgeToEdge` +
  predictive-back; `proguard-rules.pro` (constructor-aware UniFFI/JNA/JNI keeps).
- **Gradle / build (J/K/A/P):** `android/app/build.gradle.kts` (compileSdk/targetSdk **36**, the
  `androidx.baselineprofile` plugin + `baselineProfile(project(":baselineprofile"))` + profileinstaller,
  the adaptive/window/glance/tv/integrity/app-update/review/splashscreen/appcompat/collections-immutable
  deps, `bundle { language { enableSplit = false } }`, `release { ndk { debugSymbolLevel =
  "SYMBOL_TABLE" } }`, the `PLAY_BUILD` flip to `true` for the AAB); a **new `:baselineprofile`
  `com.android.test` module** + a `:macrobenchmark` module; the Rust release profile (LTO/strip) for
  the `.so`. New deps: `androidx.compose.material3.adaptive:adaptive(-layout/-navigation):1.2.0`,
  `androidx.window:window:1.5.1`, `androidx.core:core-splashscreen:1.0.1`, `androidx.activity:activity`
  (edge-to-edge / predictive back), `org.jetbrains.kotlinx:kotlinx-collections-immutable`,
  `com.google.android.gms:play-services-games-v2`, `androidx.tv:tv-*` (Leanback),
  `androidx.glance:glance-appwidget`, `com.google.android.play:integrity:1.6.0`,
  `com.google.android.play:app-update(-ktx):2.1.0`, `com.google.android.play:review(-ktx):2.0.2`,
  AppCompat for per-app locales.
- **Play launch (P) / maintainer ops (non-code):** the existing `Billing.kt`/`LicenseManager` (confirm
  the `full_unlock` $2.99 managed product + Play App Signing), the Play Console listing / IARC /
  data-safety setup, the linked Cloud project + a server endpoint for Play-Integrity verdict
  decryption, and the 12-tester closed test. No new shared-crate work.
- **Shared crates / bridge:** **none required** (host-only release). If a small `rom_metadata`
  (header / SHA) helper is convenient for C, add it to `rustynes-mobile` as a pure read-only getter —
  determinism-neutral. The one core-adjacent item (the APU 48 kHz resample for J's AAudio path) lives
  in the frontend resampler stage, not the synthesis core, so determinism / byte-identity hold.

### v1.8.8 success criteria / Play-launch checklist

The gate to promote to production. **Sequencing (maintainer, 2026-06-20):** v1.8.8 ships to **GitHub
Releases first** (the sideload AAB + APK + notes, on the host-CI gates), **then** the single **batched
on-device verification pass covering all deferred v1.8.6–v1.8.8 sign-offs** must go green, and **only
then** is the **Google Play production promotion** made. So the order is **v1.8.8 GitHub release →
on-device pass → Play launch** — the GitHub release precedes the device pass, which precedes Play. Group
it as **must-pass before production** vs **should-pass for quality**:

**Must-pass (blocks production):**

- **The batched on-device verification pass is green.** Sideload the published **v1.8.8 GitHub release**
  and verify ALL deferred sign-offs across **v1.8.6–v1.8.8** on a real device: Lua exec + log overlay;
  RA login round-trip + unlock toasts + `.rap` sidecar + the hardcore `load_state` gate; hardware
  controllers (wired + BT, the d-pad-HAT/analog fix, remapping, P1–P4) + the controller-aware UI
  (auto-hide + max-fit resize + the Guide/Start+Select navigable menu); LAN + room-code netplay (incl.
  live cross-NAT once the `deploy/` relay is hosted, #1); and the flagged Chromecast spectator path.
  This is the **first release gated on a green device pass** — the sideload channel shipped through
  v1.8.7 on host-CI alone (see the Post-v1.8.6 batched-verification note). (all WS + the v1.8.6/v1.8.7 carryovers)

- **targetSdk / compileSdk = 36**, the AAB builds + is signed (Play App Signing enrolled), the `.so` is
  16 KB page-aligned, and the in-app EN/ES picker works (`language { enableSplit = false }`). (A/B/J)
- **Edge-to-edge + predictive-back migrated** — no `onBackPressed`-only handlers remain (they stop
  firing at API 36); insets handled so nothing sits under the system bars. (K)
- **Android vitals inside the bad-behaviour thresholds:** user-perceived **crash < 1.09%**, **ANR <
  0.47%**, **per-device < 8%**, no excessive partial wake-lock flag (the March 2026 rule). The native
  bridge never blocks the UI thread (5 s ANR line). (J/L)
- **Pre-launch report clean** (no new crashes/ANRs on the Robo crawl across real Test Lab devices). (L)
- **Freemium flow verified end-to-end** on a license-test account: the 8-minute demo timer + the three
  persistence locks under `PLAY_BUILD=true`, the **$2.99 `full_unlock`** purchase clears them, restore
  works on reinstall, and **Play Integrity** confirms a genuine binary before honoring the unlock with
  **Billing as the entitlement source of truth**. (P/L)
- **Store listing complete:** IARC content rating, data-safety form ("no data collected" — opt-in
  crash reporter declared if shipped), screenshots/feature graphic refreshed with the Atlas
  adaptive/library/capture shots, ≤ 500-char release notes. (P)
- **Personal-account production gate** (if applicable): a **12-tester / 14-day continuous** closed test
  passed before applying for production. (P)
- **AccuracyCoin 100% (139/139)** still green on host CI — guaranteed by construction (no shared-core
  change), but re-confirmed.

**Should-pass (quality / ranking levers, not hard blockers):**

- **Cold-start** comfortably under the 5 s bad-behaviour line, with a measurable Baseline-Profile delta
  (`StartupTimingMetric`, `None()` vs `Partial(Require)`); Macrobenchmark **`FrameTimingMetric` P99
  `frameOverrunMs ≤ 0`** during a ROM run; Compose metrics show no unexpected non-skippable composable.
  (J)
- **Adaptive layout correct** on a foldable + tablet + free-form AVD (two-pane at expanded width). (A)
- **Accessibility** — TalkBack reads every control, font-scale + colorblind themes respected. (I)
- **Graceful sideload degradation** — the `PLAY_BUILD=false` build hides D/E/L's Play-only paths and
  stays full-featured.
- **AAudio ~20 ms round-trip**, ADPF + thermal governor active, themed icon + richer haptics + splash
  + in-app update/review wired. (J/K/L)
- **Post the v1.8.8 GitHub release with release notes** (the public changelog for the Atlas wave + the
  Play debut) — **before** v1.8.9 "Workshop" begins, per the maintainer's standing rule.

### Notes to re-verify near launch (the few facts with a 2026 moving target)

- The literal **"Aug 31, 2026 / API 36"** target-API string is blog-confirmed but may still trail in
  the canonical Play reference page — re-check at submission time.
- **Material 3 Expressive** stable-vs-alpha split (the `1.4.x` line lacks most expressive components;
  they land across the `1.5.0` line) — re-check the current Compose BOM before pinning expressive
  widgets vs the stable M3 set.
- The **Compose / adaptive / window / Play-services / Integrity / Oboe / NDK** versions cited above are
  the mid-2026 stable picks; bump to current stable at implementation time (don't regress).

## v1.8.9 — "Workshop": the desktop power-user & creator tools, on touch

> **FUTURE / planned — not released.** Thesis: **bring RustyNES's desktop creator/power-user crown
> jewels — the Mesen2-class debugger, the TAStudio piano-roll, the HD-Pack Builder, the full
> shader/EQ/audio-depth stack, movie-format import, and cheats — to mobile, re-imagined for touch and
> a large screen.** Everything here is **additive / off-by-default** and **gated like the desktop
> equivalents** (`debug-hooks` / `emu.write`-class gates), so the shipped emulation stays
> **byte-identical** and there is **no Play-deadline pressure**. Most of the Rust already exists in
> shared/desktop crates; the new work is the **Compose touch UI** + a handful of **read-only or
> already-gated bridge getters/setters**. This is the release that makes a tablet + the Z Fold inner
> display a genuine **TAS / homebrew-dev workstation**.

| WS | Theme | Headline | Effort |
|---|---|---|---|
| **A** | **Mesen2-class debugger (touch)** | Compose **CPU / PPU / APU register panels**, a **hex editor** + **RAM search**, **breakpoints / R-W-X watchpoints** (expression/conditional), the **event-viewer heatmap** + per-scanline trace + the PPU/HD-pack inspectors — re-laid for touch + the expanded layout; **gated behind `debug-hooks`**, off the Play build by default | XL |
| **B** | **TAStudio piano-roll (touch)** | A **touch TAStudio** — the piano-roll input editor, frame-advance, branches/markers, `.rnmproj` — reusing the desktop TAStudio model + the v1.8.5 `.rnm` engine; the `tastudio.*` Lua API rides along (Lua already shipped in v1.8.6) | L |
| **C** | **Movie-format import/export** | Import **`.fm2` / `.bk2` / `.fcm` / `.fmv` / `.vmv`** and export `.fm2`/`.bk2` (hash-verified) via SAF — the desktop importers cross-compile unchanged; a Compose movie browser | M |
| **D** | **HD-Pack Builder (touch)** | The **HD-Pack Builder** (ADR 0017) on mobile — capture tiles, author a pack, export — over the shared **`rustynes-hdpack`** crate already on Android (v1.8.5); a touch authoring UI | L |
| **E** | **Audio depth** | The desktop **audio-depth** stack on Android: **20-band EQ**, **stereo panning / Schroeder-reverb / crossfeed**, **per-channel mixing**, **output-device picker**, and the **NSF / NSFe player** (a music-player UI, no ROM) — all frontend DSP, determinism-neutral | M |
| **F** | **Shader/filter breadth** | Beyond the v1.8.4 WGSL stack: **hqNx / xBRZ** scalers, the **CRT preset bank**, and a **constrained `.slangp` / `.cgp` import** (the desktop shader ecosystem) on the wgpu `SurfaceView` renderer | M |
| **G** | **Cheats + Game Genie** | A Compose **cheat manager** — **Game Genie** codes + **raw-RAM (GameShark-style)** cheats + the **code DB**, persisted per-ROM (the desktop cheat model, already in `rustynes-core`) | M |
| **H** | **Symbol / source-map debugging** | Load **`.sym` / `.mlb` / `.nl`** symbol files + **ca65/cc65 `.dbg`** source maps via SAF to label the debugger (A) — the homebrew-dev workflow; reuses the desktop loaders | S |

### Per-workstream detail

**A — Mesen2-class debugger (touch).** The single largest item. The desktop debugger depth already
exists (CPU/PPU/APU panels, a full hex editor, RAM search, expression/conditional breakpoints, R/W/X
watchpoints, the CallstackManager + step modes, the MemoryAccessCounter + uninit-read, the PPU
event-viewer heatmap + per-scanline trace + the HD-pack per-pixel inspector — all behind
**`debug-hooks`**). The work is **not** re-deriving the logic but **re-laying it for touch and the
expanded large-screen layout** (the v1.8.8 two-pane layout is the natural host): scrollable hex with
a touch keypad, a watch list, a breakpoints sheet, the heatmap as a pinch-zoom canvas. Ship it **off
by default and off the Play build** (the optional sideload power-user surface the roadmap has always
reserved); it supersedes the long-deferred "egui debugger overlay" with a native Compose UI. Bridge:
a set of **`debug-hooks`-gated** read-mostly getters/setters on `rustynes-mobile` (read RAM/registers,
set/clear breakpoints/watchpoints, step) — mirroring the desktop debugger API, gated exactly like the
desktop `debug-hooks` so the default build is byte-identical. **Effort: XL.**

**B — TAStudio piano-roll (touch).** A touch **TAStudio**: the piano-roll input grid (per-frame button
cells), frame-advance, markers/branches, `.rnmproj` projects — built on the **desktop TAStudio model**
+ the **`.rnm` movie engine already shipped on Android in v1.8.5**. The **`tastudio.*` Lua API** comes
along for free (Lua shipped in v1.8.6). Touch ergonomics: a horizontally-scrolling frame grid, tap-to-
toggle cells, a transport bar, pinch to zoom the timeline. Bridge: the TAStudio model is frontend
state driving the same deterministic input path — expose its control surface through `rustynes-mobile`
(load/save project, set frame input, seek), reusing the v1.8.5 `.rnm` plumbing. **Effort: L.**

**C — Movie-format import/export.** The desktop **`.fm2`/`.bk2`/`.fcm`/`.fmv`/`.vmv` importers + the
`.fm2`/`.bk2` exporters** (hash-verified) are pure Rust and cross-compile unchanged; add a Compose
movie browser + SAF import/export. Lets mobile play back movies authored in FCEUX / BizHawk and share
TASes off-device. Bridge: import-bytes → movie / export-movie → bytes getters (no path, SAF bytes).
Determinism-neutral (movies drive the same input path). **Effort: M.**

**D — HD-Pack Builder (touch).** The **HD-Pack Builder** (ADR 0017) authoring flow on mobile, over the
**`rustynes-hdpack`** shared crate already compiled into the Android build (v1.8.5 extracted it and
taught it `Read + Seek` / raw-bytes loading for SAF). The new surface is the **touch authoring UI**
(capture the current tile set, edit/replace tiles, assign conditions, export the pack via SAF). Output-
only / off-by-default, so the emulation stays byte-identical. Bridge: builder control + export-bytes on
`rustynes-mobile`, forwarding to `rustynes-hdpack`. **Effort: L.**

**E — Audio depth.** Bring the desktop **audio-depth** workstream (ADR 0020) to Android: a **20-band
EQ**, **stereo panning / Schroeder-reverb / crossfeed**, **per-channel mixing** sliders, an **output-
device picker**, and a standalone **NSF / NSFe music player** (load a `.nsf` via SAF, a track list +
transport, no ROM needed — the desktop NSF player). All of it is **frontend DSP** over the emitted
samples (the core's synthesis is untouched), so it is **determinism-neutral** — exactly as on desktop.
Bridge: the audio-DSP config setters + the NSF-player surface on `rustynes-mobile`. **Effort: M.**

**F — Shader / filter breadth.** Extend the v1.8.4 wgpu `SurfaceView` renderer beyond the shipped
WGSL NTSC/CRT/Bisqwit set with the desktop **hqNx / xBRZ** scalers, the **CRT preset bank**, and a
**constrained `.slangp` / `.cgp` import** (the same constrained subset the desktop accepts). Reuses the
shared **`rustynes-gfx-shaders`** crate path (WGSL on Android via wgpu). Presentation-only →
determinism-safe. Bridge: shader-config selection (likely already partly present from v1.8.4).
**Effort: M.**

**G — Cheats + Game Genie.** A Compose **cheat manager**: **Game Genie** codes (the
`rustynes-core/src/genie.rs` runtime overlay, off by default, not in the save-state), **raw-RAM
(GameShark-style)** cheats (`Nes::poke_ram` applied after `run_frame`), and the **Game Genie code DB**,
persisted **per-ROM** (the desktop `[[raw]]` / Game-Genie TOML model, keyed by ROM SHA via the per-game
DB). Empty cheat list = byte-identical, so determinism holds. Bridge: apply-genie / poke-ram setters on
`rustynes-mobile` (genie already core; poke-ram is the documented caller-side overlay). **Effort: M.**

**H — Symbol / source-map debugging.** SAF loaders for **`.sym` / `.mlb` / `.nl`** symbol files and
**ca65/cc65 `.dbg`** source maps, feeding the debugger (A) so addresses show labels and the homebrew
dev sees source context — the desktop symbol/source-map loaders reused, fed raw bytes via SAF. Gated
with A (`debug-hooks`). Bridge: load-symbols-bytes on `rustynes-mobile`. **Effort: S.**

### Verification (v1.8.9)

- **Host CI (authoritative):** every feature is **off-by-default / gated** (`debug-hooks` /
  `emu.write`-class / output-only), so the **default shipped emulation is byte-identical** and
  AccuracyCoin 100% (139/139) holds by construction; the gated surfaces compile + their host-side logic
  reuses the already-tested desktop code; the Android crates cross-compile with the gates on.
- **On-device / AVD (manual):** the debugger panels + hex editor + RAM search + a breakpoint/watchpoint
  hit (A); a piano-roll TAS authored + replayed bit-identically (B) — and **cross-checked against the
  desktop** (`.rnm` portability); a `.fm2`/`.bk2` import playback (C); an HD-pack authored + re-loaded
  (D); the EQ / reverb / panning / per-channel mix + an NSF playthrough (E); each new scaler/preset +
  a `.slangp` import (F); a Game Genie + raw-RAM cheat (G); a symbol file labelling the debugger (H).
- **Cross-platform determinism (unchanged contract):** a TAS authored in the mobile TAStudio replays
  bit-identically on desktop and vice-versa; a cheat-free / gate-off session is byte-identical to
  v1.8.8.

### Critical files / crates (v1.8.9)

- **Android (Kotlin, mostly new):** `Debugger.kt` (panels / hex / RAM search / breakpoints / heatmap),
  `TasStudio.kt` (piano-roll), `Movies.kt` (`.fm2`/`.bk2`/… import-export browser), `HdPackBuilder.kt`,
  `AudioDepth.kt` (EQ / reverb / panning / per-channel) + `NsfPlayer.kt`, `Shaders.kt` (scaler/preset/
  `.slangp` import), `Cheats.kt` (Game Genie + raw-RAM + DB), `Symbols.kt` (`.sym`/`.mlb`/`.nl`/`.dbg`).
- **Shared crates / bridge:** new **`debug-hooks`-gated** debugger getters/setters + the TAStudio,
  movie-IO, HD-pack-builder, audio-DSP, NSF-player, cheat, and symbol surfaces on **`rustynes-mobile`**
  — each reusing the existing desktop/`rustynes-hdpack`/`rustynes-gfx-shaders`/`rustynes-core` logic and
  gated exactly as the desktop equivalents, so the default build is byte-identical. **No chip-stack
  change.**

## The shared bridge is now connectivity-complete — iOS inherits Lua / RA / netplay

By the end of v1.8.6 the shared **`rustynes-mobile`** bridge exposes the **full** mobile surface —
core (ROM/input/run-frame/framebuffer/save-load-state/identity) **plus Lua, RetroAchievements, and
netplay** — over UniFFI. **v1.9.0 iOS (SwiftUI / wgpu→Metal) reuses all of it for free** via the
generated **Swift** bindings: it inherits Lua, RA, and netplay without re-deriving the FFI surface.
The hard `!Send` → `Send` work that those connectivity features required is **already done in shared
crates**: `rustynes-script`'s `ScriptEngine` is now `Send` (mlua `send` + `Arc`/atomic newtypes) and
`rustynes-ra`'s `RaClient` carries the audited `unsafe impl Send`. The std crates iOS consumes
alongside the core are **`rustynes-ra`** (RetroAchievements), **`rustynes-hdpack`** (HD-pack), and
**`rustynes-gfx-shaders`** (the WGSL NTSC/CRT/Bisqwit stack, WGSL → Metal via wgpu) — all
platform-agnostic, all shared. So the iOS plan's "Netplay / RA / Lua deferred" MVP cut can be revisited
upward cheaply: the Rust-side groundwork is no longer iOS-specific work, only the SwiftUI login /
overlay / netplay-UI chrome remains. The bridge having gone connectivity-complete on Android is the
single biggest de-risk for the iOS release.
