# v2.0 R-1 CORE — DMC-DMA phase-coherence rework (user-committed 2026-06-04)

**Status:** committed (user-authorized, multi-session). Branch `feat/v2.0-from-main` (worktree
`RustyNES_v2-from-main`). Builds forward from `a8e482e` (idle-halt); rollback floor `5ceab83`. This is the
deferred deep core named throughout `v2.0-f2-recoupling-effort-2026-06-03.md` — entered ONLY after the
localized + scoped lever search was exhausted (see that doc; 13 levers refuted, both scoped angles dead).

## The committed target (precise, evidence-grounded)

Make R1's DMC-DMA **absolute phase** match default/Mesen so the **abort-window DMC fetch lands at offset
≤2** of the `STA $4015` abort write (where the truncation engages), WITHOUT moving the 6/10's reload DMAs.

Pinned defect (P-0 + P-2, `v2.0-f2-recoupling-effort`):
- R1's DMC DMA lands **~2 CPU cycles LATE** at the abort write (nearest-DMC offset +3/+4 vs default +1/+2)
  → `dmc_abort_pending` never engages (0× vs default 9×) → wrong `CalculateDMADuration` → `0x06`.
- The DMC **period** is Mesen-clean (oracle-confirmed); the divergence is the **absolute phase** of the
  DMC sequence through the abort-test setup (DMASync spin + `$4010`-rate-change + clockslides).
- **No discriminator exists** to scope a local fix: the 6/10 readout cluster (`$2002`/`$2007`) runs its
  DMAs at the SAME rate `0xE` as the aborts, and the only abort-vs-6/10 difference (the `STA $4015` sweep)
  is unknowable at reload-arm time. So the fix must re-derive the phase coherence UNIVERSALLY (land ALL
  R1 DMC DMAs at default's absolute phase), not gate a knob.
- The abort-SCHEDULING arithmetic (`schedule_explicit_dmc_abort_if_needed`) is NOT the mechanism (proven
  inert incl. forced-immediate). The lever is the DMC-DMA **landing cycle** itself.

Why this is hard (the C1-vs-DMA tension): the interleaved `read1`/`idle_tick` `while dmc_dma_pending()`
service that delivers the 6/10 + the C1 trio (`5ceab83`) is the SAME mechanism producing the ~2-cyc abort
phase lag. Prior global phase shifts (`arm-align`, `arm-delay1`, `inline-service`) all broke the 6/10.
The core must shift the abort-window phase by re-deriving coherence such that the 6/10's (landing-tolerant)
DMAs stay correct while the abort's (landing-sensitive) DMA moves to offset ≤2.

## MANDATORY research strategy (continuous — refine AND accomplish; consult whenever blocked/validating)

- **Exemplar emulators:** **Mesen2** (`ref-proj/.../NesCpu.cpp` `ProcessPendingDma`/`RunDma`/
  `StartDmcTransfer`/`StopDmcTransfer`; the `CycleCount&1` get-half + align-cycle; the per-`MemoryRead`
  cadence), **TriCNES** (`/tmp/TriCNES/Emulator.cs` `_6502`/`APU_PutCycle`/`APUAlignment`/`OAMDMA_Aligned`/
  `DMCDMA_Halt`), **ares** (`apu/dmc.cpp` `dmaDelayCounter`), **TetaNES**. Cross-diff our per-cycle
  `halted_addr`/`getcycle` against theirs.
- **`nesdev_wiki/`** (repo root): `DMA.md` (RDY-low-on-next-read, the 4-cycle halt/dummy/align/get model,
  the `$4000-401F` conflict), APU DMC rate table.
- **Hardware sims:** `perfect6502` / `visual2c02` / `phantom2c02` (`/tmp/RustyNES_v2/`) for cycle-exact RDY
  assertion / dummy-read timing when the docs/oracle are ambiguous.
- **The Internet:** nesdev forums, blargg notes, emu-dev write-ups for the DMC-DMA/RDY edge cases.
- **Other branches + full history + code comments:** the refactor branch's R-0/R-1/R-2 docs +
  `r4-cpu-dma`/`mc-apu-subcycle`/`dmc-rdy-stall-exact` flag CODE (structure to lift, NOT its 77.70% calib).
- **All memory files** + the Mesen per-fetch oracle (`/tmp/RustyNES_v2/mesen_dmc.csv`, already built).

## Phased plan (each gated; default-off feature until the no-regress suite is green)

- **C-0 — PIN where the ~2-cyc offset ACCUMULATES (no behavior change):** per-fetch cross-diff of the
  pivot's DMC sequence vs the Mesen oracle (and vs default) THROUGH the abort-test setup (DMASync exit →
  `$4010=$4E` → clockslides → the sweep), to find the exact fetch/event where R1's absolute phase first
  diverges +2 from default. Deliverable: the accumulation point (candidate: the DMASync sweep-vs-stick
  from P-0, or a specific reload during the clockslide) → the minimal coherence change follows from it.
- **C-1 — phase-coherence change behind `mc-r1-dmc-phase-coherence` (default-off):** the actual rework so
  R1's DMC fetch lands at default's absolute phase at the abort. Gate: abort-region nearest-DMC offset
  +3/+4 → +1/+2; 6/10 + C1 + no-wedge + AccuracyCoin non-decreasing.
- **C-2 — abort truncation + OAM overlap ENGAGE:** with the DMC now landing at offset ≤2, the truncation
  (`StopDmcTransfer` in the interleaved `dmc_dma_step`) + `service_dmc_dma_during_oam` finally fire. Gate:
  Explicit + Implicit + DMC+OAM flip; 6/10 + C1 hold.
- **C-3 — `$4015` edge + determinism/save-state + PROMOTE:** `DMA + $4015` falls out; determinism +
  save-state round-trip (the phase-coherence blast radius); 60-ROM oracle byte-identical (visual-proven
  re-baseline ONLY per `feedback_paired_pipeline_oracle_masking`); promote `mc-r1-substrate` + the fix
  toward default, closing C1 + the DMA cluster together (~95-96%).

## C-0 RESULT (2026-06-04) — the per-DMA phase is CORRECT; the failure is CONTROL-FLOW divergence (the DMASync spin)

Cross-diffed the DMC sequence through the abort setup using a COMMON anchor — the `$4010=$4E` rate-change
that starts each abort iteration — measuring the cycle delta from each anchor to the next DMC DMA, default
vs R1 (full-battery `trace_dmc_dma` boot→`$0479`):

```
DEFAULT: #($4010=$4E) writes = 50   delta(anchor -> next DMA) = 44 (ALL 50, exact)
R1:      #($4010=$4E) writes =  2    delta(anchor -> next DMA) = 44 (both, exact)
```

**Decisive finding — TWO facts that re-frame the whole core:**
1. **The DMC's per-fetch phase relative to its OWN setup is CORRECT under R1** — when R1 reaches a
   `$4010=$4E`, the DMA fires exactly 44 cycles later, identical to default. There is **no per-DMA phase
   error** to fix. (This RETIRES the "advance the DMC fetch ~2 cyc" framing of the kickoff target — the
   DMA is NOT firing late relative to its setup.)
2. **R1 executes ~25× FEWER abort iterations** — 2 `$4010=$4E` rate-changes vs default's 50 (and P-2's 16
   vs 114 `STA $4015` sweeps). The abort test's CONTROL FLOW diverges massively under R1: it never runs
   the full sweep that covers the `CalculateDMADuration` answer-key range → wrong result → `0x06`.

**So P-2's "DMC lands ~2 cyc late at the abort" was a SYMPTOM, not the cause:** the abort `STA $4015` is
positioned by the test's own clockslide, and the test is in a DIFFERENT iteration / clockslide phase under
R1 (because it ran fewer iterations) — so the abort lands at offset +3/+4 even though the DMA-vs-its-own-
setup phase is exact. The per-DMA-vs-abort offset is the test's control-flow desync, not a DMC timing bug.

**Root (C-0 → C-1 retarget):** the control-flow divergence traces to the **DMASync spin** (P-0: R1's DMC
STICKS on `$4000` — gap=1 ×64 — vs default's SWEEP, gap 2-8). The DMASync `LDA $4000; BNE` sync is the
test's loop gate; R1's spin syncs at a different iteration / the conflict fires at a different cadence, so
every downstream loop count + clockslide is off, and the test runs 2 iterations where it should run 50.
**C-1 must therefore target the DMASync-spin SYNC behavior — make R1's DMC `$4000`-conflict during the
spin SWEEP like default's (so the spin exits at default's iteration and the test runs the full sweep) —
NOT a per-DMA phase shift.** This is the intrinsic interleaved-service behavior (the `read1`/`idle_tick`
`while dmc_dma_pending()` loop re-halting on the same `$4000` read every span). The C-1 target is now the
sweep-vs-stick, precisely as P-0 first identified — C-0 confirms it is THE root, not a secondary axis.
Data: `/tmp/RustyNES_v2/{dmc,spin}_{def,r1}_0479.csv`.

## C-1 DESIGN (2026-06-04) — the abort has NO localized entry in the interleaved model; first gated attempt + its structural conclusion

Dived into C-1. Read the interleaved service (`Cpu::read1`/`idle_tick` `while bus.dmc_dma_pending()` →
`bus.dmc_dma_step(addr)`), dumped a real DMASync-spin segment default-vs-R1, and traced the abort path.

**Spin per-cycle mechanism is IDENTICAL** — both default and R1 run a 4-cycle span (`r $4000`×3 halt + 1
GET) then the CPU's `R $4000` reads whatever the GET latched into `open_bus` (default segment GET=`$EFFD`
→`$FF`→spin continues; R1 segment GET=`$FFC0`→`$00`→spin exits). No per-cycle divergence in the spin
itself. Combined with C-0 (per-`$4010` DMA phase exact, 44 in both): the DMC TIMING is correct; the
abort test fails by running only ~2 of 50 iterations (fails early on the first abort's wrong duration).

**First gated attempt — abort truncation port — proven STRUCTURALLY INERT (two independent reasons):**
- **(a) the interleaved span is ATOMIC.** `read1`'s `while bus.dmc_dma_pending()` loop consumes the
  ENTIRE 3-4-cyc span before the CPU's read/write proceeds, so `in_dmc_dma` is true only TRANSIENTLY
  inside `read1`. A `STA $4015` write (via `write1`) can NEVER coincide with `in_dmc_dma` → a bus-level
  "detect `$4015`-disable while `in_dmc_dma` → truncate" hook would never fire. (Verified by the code
  structure; this is also correct hardware behavior — the DMA halts the CPU atomically.)
- **(b) the pre-span CANCELLATION path is inert.** On hardware the abort is a PRE-span cancellation:
  `STA $4015` disables DMC, and `schedule_explicit_dmc_abort_if_needed` arms a 1-cyc abort halt
  (`pending_dmc_abort`) instead of the full DMA. PROVEN inert under R1 (the forced-`cuo=1` diagnostic:
  even forcing the immediate-abort path changed nothing) — the function is never called or early-returns
  on `bits_remaining != 1 || sample_buffer.is_none()` (and needs `was_active`) at the `$4015` disable.

**C-1 ROOT (both reasons collapse to one):** the abort requires the DMC's STATE
(`bits_remaining==1`, `sample_buffer.is_some()`, `active`) at the abort-window `$4015` writes to match
default's. Under R1 it does not — the cumulative-coherence gate. There is NO localized truncation/detection
hook that isn't inert; the span can't be truncated mid-flight (atomic, correct) and the pre-span cancel
can't fire (wrong DMC state). **So C-1's real implementation target is precise: make the DMC STATE at the
abort-window `$4015` writes match default's** (so `schedule_explicit_dmc_abort_if_needed` fires + arms the
1-cyc abort halt as it does on default). This is the cumulative coherence — the DMC's `bits_remaining` /
`sample_buffer` / `active` trajectory through the abort-test playback must track default's cycle-for-cycle.

**Next concrete C-1 step (avoids another inert blind attempt):** instrument WHICH precond field diverges
at the abort-window `$4015` disables (is `schedule_explicit_dmc_abort_if_needed` called at all? if so,
does it fail on `bits_remaining` or `sample_buffer`?) — via gated `pub static` atomic counters in
`apu.rs` (the refactor branch's `cutover::DMC_*` pattern, no_std-safe), read from a harness probe. That
pins the exact state divergence the coherence fix must correct, turning C-1 from "match the whole
trajectory" into "correct field X at the `$4015` write." THEN the gated coherence change. (Gated attempt
#1 = abort-phase, built+tested+reverted INERT; the truncation-port attempt is refuted by design above
without a build — both confirm the abort path has no localized entry.)

## C-1 INSTRUMENTED (2026-06-04, `mc-r1-dmc-abort-probe`) — the full causal chain, with hard numbers

Added gated `pub static` atomic counters (`nes_apu::abort_probe`, default-off, byte-identical, clippy-
clean) to pin WHERE the explicit-abort scheduling diverges. Full-battery counts, default (PASS) vs R1+idle
(FAIL):

```
                          DEFAULT   R1+idle
$4015 disable writes        225       163
  ...DMC was_active          76        12     <- FIRST divergence (the DMC has usually already STOPPED)
schedule() calls            76        12
  fail bits_remaining!=1     17         6
  fail sample_buffer none     0         0
reached cycles_until_output 59         6
  cuo in window {1,2,3}       4         0     <- the abort NEVER lands in the engaging window
abort ARMED                  4         0     <- so the truncation never arms -> wrong duration -> 0x06
```

**The full causal chain is now pinned with numbers:** DMASync spin syncs differently (P-0 stick) → the
abort test runs ~25× fewer iterations (C-0: 2 vs 50 `$4010=$4E`) → **the DMC is `active` at only 12 of
the `$4015` disable writes vs default's 76** (it has already finished its sample by the disable) →
`schedule_explicit_dmc_abort_if_needed` runs 12× not 76× → reaches the `cuo` computation 6× not 59× →
lands in the engaging `{1,2,3}` window **0× vs 4×** → the 1-cyc abort halt **never arms (0 vs 4)** → no
truncation → wrong `CalculateDMADuration` → `0x06`.

**The primary blocker is `disable_was_active` (12 vs 76), upstream of the arming.** Confirmed it's
upstream and not independently fixable: the earlier forced-`cuo=1` experiment armed all reached-cuo events
yet was INERT — arming the abort in the wrong iterations doesn't reproduce the answer-key sweep. So C-1
CANNOT be closed by fixing the arming (downstream); it requires the DMC's `active`/timer STATE at the
abort-window `$4015` writes to match default — which requires the test to run the full iteration set —
which requires the DMASync spin to sync at default's iteration. **The chain bottoms out at the DMASync
sync = the intrinsic interleaved-DMC-service behavior (the mining established has no localized fix).**

**C-1 status:** the causal chain is fully instrumented and the root is confirmed (DMASync sync → DMC-
state-at-disable). The probe (`mc-r1-dmc-abort-probe`) is kept as committed diagnostic infrastructure to
measure future attempts (target: `disable_was_active`→76, `armed`→4). The remaining C-1 work — making the
DMASync spin sync at default's iteration — is the deepest part of the core (the per-cycle spin mechanism
is identical, so the divergence is a cumulative phase that lands the `$00`-conflict on a different spin
iteration); it is the substantial multi-session rewrite, now with a precise success metric.

## C-1 ATTEMPT 1 (2026-06-04, `mc-r1-dmc-phase-coherence`) — cuo phase-basis fix: correct-but-INERT; gate is upstream

Took on the core. The probe localized the arming failure to `cycles_until_output = (apu_phase?2:1) +
timer*2` using the LOCKSTEP phase under R1, where the DMC actually clocks on `!put_cycle` (F-2). Fixed it
behind `mc-r1-dmc-phase-coherence`: under `dmc_driven_externally`, `first_apu_clock = !put_cycle ? 2 : 1`.

- **6/10-safe + C1-safe by construction** (the change is ONLY in `schedule_explicit_dmc_abort_if_needed`,
  which runs solely on `$4015`-disable-while-active; the 6/10 readout tests never disable the DMC).
  Confirmed: `scan_dma_abort` 6/10 intact, 4 fails unchanged.
- **But INERT** — `cuo_in_window` stayed 0. The probe shows why: the 6 `reached_cuo` events under R1 all
  have `timer ≥ 2` (→ `cuo ≥ 5`), so toggling `first_apu_clock` between 1 and 2 can't reach `{1,2,3}`
  (which needs `timer ∈ {0,1}`). The phase basis WAS wrong, but fixing it is masked by the upstream gate.

**The upstream gate, pinned at the finest grain:** `active()` = `bytes_remaining > 0`. R1's DMC has
`bytes_remaining == 0` (sample EXHAUSTED) at **93%** of disable writes (151/163) vs default's 66%
(149/225) — i.e. `disable_was_active` 12 vs 76. The Explicit/Implicit abort uses a SHORT non-looping
sample (the implicit-stop edge: `sample_length==1`, rate `0x0E`, non-looping) with a NARROW active window
that the swept `STA $4015` must land inside. Default's sweep hits that window 76×; R1's only 12×, because
the DMC's `bytes_remaining`/timer trajectory relative to the test's swept disables diverges — the
cumulative setup timing (rooted in the DMASync sync) places the sample's active window off the sweep.

**Conclusion (finest-grain, multiply-confirmed):** there is NO localized field/phase fix. The cuo
phase-basis was genuinely wrong and is now corrected (kept, gated, correct-in-principle), but it is inert
until the DMC's `bytes_remaining`/timer TRAJECTORY through the abort-test setup matches default cycle-for-
cycle — the cumulative coherence. Success metric (probe): `disable_was_active` 12→76, then `cuo_in_window`
0→4, then `armed` 0→4. The remaining work is the DMASync-sync cumulative-phase alignment that makes the
whole abort-test playback track default — the substantial multi-session rewrite. The cuo phase-basis fix
is banked behind `mc-r1-dmc-phase-coherence` as the first (necessary, not yet sufficient) coherence piece.

## C-1 ATTEMPT 2 + EVENT-LOG (2026-06-04) — DEFINITIVE: the abort SWEEP LOOP never runs under R1

Applied the same phase-basis fix to the LOAD arm (`dmc_dma_delay`, line 837) under
`mc-r1-dmc-phase-coherence` — also INERT (`disable_was_active` stuck at 12). Two inert phase-fixes in a
row → stopped guessing and built a per-`$4015`-write event log (`abort_probe::EVLOG_*`, ring of cpu_cycle
+ enable + `bytes_remaining` + timer, dumped to CSV via `RUSTYNES_ABORT_EVLOG`). Default vs R1:

```
                  $4015 writes   enables   disables   enable->disable gap clusters
DEFAULT:          493            268       225        2483 (×30), 2914 (×17)  <- ~47 sweep iterations
R1+phase-coh:     222             59       163        NONE (all gaps in the millions)
active disables:  default 76 (timer≈22 ×49)     R1 12 (timer scattered, never the calibrated 22)
```

**DEFINITIVE FINDING:** the Explicit-abort SWEEP LOOP (enable → DMASync+clockslide → disable, repeated
~47× with a fixed ~2483-cyc gap, active disable landing at the calibrated `timer≈22`) **never executes
under R1.** R1 has ZERO of the tight 2483/2914-cyc enable→disable pairs; its 59 enables / 163 disables are
scattered (million-cycle gaps) — a completely different control-flow path. So the phase fixes (cuo,
load-arm) are inert because **the code that would arm the abort never runs** — R1's control flow diverges
UPSTREAM of the sweep, at the DMASync sync (P-0: R1's DMC sticks on `$4000`, so the `LDA $4000; BNE`
sync exits at the wrong iteration / blows the test's timing budget), so the test never reaches the
sweep loop and takes a divergent path. `disable_was_active` 12 vs 76 and `timer≈22`-never-hit are
SYMPTOMS of the sweep loop never running, not independent gates.

**This collapses the entire C-1 chain to ONE root, now proven at the deepest grain: the DMASync-spin
sync.** Every downstream lever (abort scheduling, load arm, cuo phase, truncation) is inert until R1's
DMASync `LDA $4000; BNE` spin syncs at default's iteration — which requires R1's interleaved DMC
`$4000`-conflict to SWEEP the spin with default's cadence (P-0), i.e. the structural rewrite of the
interleaved DMC service so its conflict-during-spin matches default's burst. That is the genuine
multi-week core; it is NOT a phase knob (3 phase knobs now inert: cuo, load-arm, +the earlier abort-phase).

**Banked (all gated, default byte-identical, 6/10-safe, clippy-clean):** `mc-r1-dmc-abort-probe` (counters
+ event log) and `mc-r1-dmc-phase-coherence` (cuo + load-arm phase-basis fixes — correct-in-principle,
necessary-not-sufficient coherence pieces, INERT until the DMASync sync is fixed). Success metric
unchanged (`disable_was_active`→76, `armed`→4); the event log adds a sharper leading metric: **the
2483-cyc enable→disable sweep cluster must appear under R1** (0→~47). The remaining work — making the
interleaved DMC `$4000`-conflict sweep the DMASync spin at default's cadence — is the structural
multi-session rewrite. STOP-CONDITION reached (anti-thrash): 3 inert phase knobs; the root is structural.

## C-1 OPTION-a (2026-06-04) — first-DMASync-spin trace: the divergence is PHASE, not mechanism

Captured the abort-test ENTRY region (not the result window) via a new `RUSTYNES_TRACE_MARGIN` env on
`trace_dma_4015` (margin=50 frames), default vs R1, and isolated the DMC-conflict DMASync syncs (CPU
`LDA $4000` spin clusters that EXIT `$00` = the DMC drove the open-bus conflict):

```
                 DMC-conflict syncs   sync mechanism        entry_mod432 (CPU-vs-DMC phase at spin entry)
DEFAULT:         65                   iters=4, dmc_halts=3   406 (×13 dominant) + spread 172/82/190/424/...
R1+phase-coh:    11                   iters=4, dmc_halts=3   74 (×10 dominant) + 146
```

**FINDING:** the DMASync sync MECHANISM is IDENTICAL under R1 (4 spin iterations, 3 DMC `$4000`-halts,
then the `$00` GET drives the exit) — R1 syncs fine. What differs is the **absolute CPU-vs-DMC PHASE at
the sync**: R1's syncs cluster tightly at `entry_mod432 = 74`, default's dominantly at `406`. The sync is
DMC-conflict-locked, but the CPU instruction stream enters/exits the spin at a DIFFERENT point in the DMC
432-cycle period under R1. Since the abort test's clockslide + `STA $4015` disable is a FIXED cycle offset
from the sync exit, a shifted sync-phase lands the disable at a DIFFERENT DMC timer value → outside the
`timer∈{0,1}` arm window → `armed` 0 → no truncation → `0x06`. (Also: R1 has only 11 syncs vs 65 — it runs
far fewer because, once the first abort iteration mis-measures, the test bails, per the event-log finding.)

**This pins option-a's question: the divergence is a CUMULATIVE CPU-vs-DMC absolute-PHASE offset at the
DMASync sync (NOT a sync-mechanism bug, NOT the span/period — both correct).** The sync mechanism works;
the CPU-vs-DMC phase at which it occurs is offset (cumulative, from R1's cycle accounting upstream of the
abort test). The fix must align R1's CPU-vs-DMC phase to default's at the sync — WITHOUT moving the 6/10's
DMAs (which `arm-align`/`arm-delay1`/`inline-service` proved is the hard constraint). That is the
structural cumulative-phase coherence (the multi-week core), but now with a CONCRETE phase target
(`entry_mod432`: R1 74 → default 406) and a leading metric, not a vague "match the trajectory." Tooling
banked: `RUSTYNES_TRACE_MARGIN` on `trace_dma_4015` (abort-entry capture) + the abort probe/event log.
Data: `/tmp/RustyNES_v2/entry_{def,r1}.csv`.

## C-1 ROOT (2026-06-04) — DEFINITIVE: the interleaved `put_cycle` lock kills the phase WALK the abort sweep needs

Traced the DMASync sync PHASE across the abort region (consecutive syncs, `entry_mod432`), default vs R1:

```
DEFAULT (abort sweep): 370 → 388 → 406 → 424 → 10 → 28 → ... 82 → 100 → 118 → 136 → 154 → 172 → 190
                       = a clean +18 WALK per iteration (regular ~576-stepped cpu_cycle deltas)
R1:                    74, 74, 74, 74, 74, 74, 74, 74, 74, (146), 74
                       = LOCKED at phase 74 (huge irregular gaps — not sweep iterations)
```

**THE ARCHITECTURAL ROOT (definitive, deepest grain):** the Explicit-abort sweep WORKS in default because
the DMASync sync PHASE **WALKS +18 per iteration** through the DMC's 432-cycle period — so the disable
(a fixed clockslide offset from the sync exit) sweeps through the DMC `timer≈0` arm window and arms at the
iterations where it aligns (default `armed` 4×). **R1's interleaved DMC is PHASE-LOCKED to the CPU** — its
GET is gated on `!put_cycle`, and `put_cycle` is CPU-cycle parity, so the DMC-vs-CPU phase is PINNED (sync
always at 74). The phase does NOT walk → the disable never sweeps the `timer≈0` window → `armed` 0 → the
first abort iteration mis-measures → the test bails (the event-log "sweep loop never runs" + the
`disable_was_active` 12-vs-76 are both downstream of this lock).

**This is the fundamental interleaved-model-vs-abort tension, now pinned to the exact mechanism:** the
`put_cycle` get-gate (the `5ceab83` BREAKTHROUGH that delivered the 6/10 + un-wedged the battery + closed
C1) is the SAME mechanism that phase-LOCKS the DMC, killing the +18 phase walk the abort sweep requires.
Default's burst DMC fires on its own timer (parity-independent) → phase walks; R1's interleaved DMC fires
on `!put_cycle` → phase locked. The 6/10 are landing-TOLERANT (one post-sync observation, lock is fine);
the aborts are sweep-SENSITIVE (need the phase to walk, lock kills it). NO localized fix reconciles them:
unlocking the phase (burst-style timer-fired DMC) re-breaks the 6/10 + the wedge (proven: inline-service,
arm-align, arm-delay1); keeping the lock starves the abort sweep.

**C-1 conclusion:** the 4-test abort tail requires the DMC to fire on its OWN timer (phase-WALKING like
default's burst) for the abort sweep, while the 6/10 + C1 require the `put_cycle`-locked interleaved
service. Reconciling these = a DMC service that is timer-fired (walks) AND lands the 6/10's post-sync DMA
correctly AND stays un-wedged AND keeps C1 — i.e. the full master-clock DMC coherence (the genuine
multi-week core the pivot was built to defer). This is NOT reachable by a gated knob on the interleaved
model; it is the architectural redesign. The diagnosis is now COMPLETE at the deepest (mechanism) grain:
**the abort needs a phase WALK; the interleaved `put_cycle` lock forbids it; the lock is load-bearing for
the 6/10/C1.** Leading metric for any redesign: the DMASync sync phase must WALK +18/iter under R1 (it is
locked at 74). All instrumentation banked. Data: `/tmp/RustyNES_v2/entry_{def,r1}.csv`.

# C-1 REDESIGN SKETCH (2026-06-04) — phase-WALKING DMC GET on the R1 substrate (next-session plan, no code yet)

The diagnosis is complete: the abort sweep needs the DMASync-sync phase to WALK +18/iter; R1's interleaved
GET gate (`get = !put_cycle`) phase-LOCKS it; the lock is the `5ceab83` mechanism that gives 6/10 + C1.
This sketch is the architectural design to reconcile them. **No code is written yet — this is the plan.**

## The precise mechanism to change

Two SEPARATE uses of `!put_cycle` exist; only ONE causes the lock:
- **(A) the DMC byte-TIMER clock** — `tick_dmc()` calls `dmc.clock_timer()` when `!put_cycle` (apu.rs
  ~672). This sets WHEN the buffer empties (the fire cycle). It is the `5ceab83` un-wedge fix (recovers
  main's odd-cycle DMC fire-phase the DMASync `$4000` conflict needs). **KEEP — do NOT touch.**
- **(B) the DMA GET gate** — `dmc_dma_step_impl` (bus.rs ~2130) computes `get_cycle = !put_cycle` and only
  performs the sample GET on a get cycle, doing halt re-reads otherwise. This SNAPS the GET to CPU-cycle
  parity → the DMC-vs-CPU phase is pinned (sync locked at 74). **THIS is the lock to remove.**

**Design principle:** make the GET fire at a FIXED offset from the DMA start (the buffer-empty cycle,
which (A) already walks every 432) instead of snapping to the next `!put_cycle`. Then the GET inherits the
walking phase of the buffer-empty → the DMASync sync walks +18/iter like default → the abort sweep arms.
Default's burst (`service_dmc_dma`) does exactly this: `noop_cycles = short?2:3` halt cycles THEN an
UNCONDITIONAL get — a fixed-offset get, not parity-gated. Port that offset model INTO the R1 per-cycle
interleaved access shape (NOT the legacy burst — see "why prior attempts failed").

## Why this differs from the 3 refuted phase levers

- **inline-service (cand. i)** re-enabled default's burst via the LEGACY `tick_one_cpu_cycle` +
  `dma_mc_consumed` — which does NOT reproduce default's lockstep timing under R1's `master_clock`/
  `run_ppu_to` split (worst of both worlds → broke 6/10). The redesign keeps the R1 per-cycle access shape
  (`start_cycle`/`bus.read`/`end_cycle`); it changes ONLY the GET-fire DECISION inside `dmc_dma_step`, not
  the cycle machinery.
- **fixed-span (`mc-r1-dmc-fixed-span`)** fixed the span LENGTH but KEPT the `!put_cycle` GET gate (the
  lock) → still locked. The redesign REMOVES the gate (fixed OFFSET, parity-independent).
- **arm-align/arm-delay1** shifted the ARM cycle (where the buffer-empty is detected) to a fixed parity →
  moved ALL DMAs incl. the 6/10's → broke them. The redesign leaves the ARM (buffer-empty, the walking
  fire cycle) UNTOUCHED; it only changes when, after the arm, the GET fires.

## Phased plan (each gated behind `mc-r1-dmc-phase-walk`, default-off, probe-validated)

- **R1w-0 — DESIGN VALIDATION (trace-only):** confirm via the existing probe + `entry_*` traces that the
  6/10 readout DMAs do NOT depend on the GET-parity-snap specifically (they observe one post-sync DMA; if
  their landing is set by the buffer-empty cycle, not the snap, a fixed-offset GET preserves them). If they
  DO depend on the snap, the redesign needs a per-DMA-kind offset; pin this first.
- **R1w-1 — fixed-offset GET in `dmc_dma_step_impl`:** replace `get_cycle = !put_cycle` with a per-span
  countdown (`noop_cycles = dmc_dma_short()?2:3` halt cycles, then an unconditional GET), seeded at the
  span start (`!self.in_dmc_dma`). Keep the timer clock (A) and the arm untouched. GATE: the R1 DMASync
  sync phase must WALK +18/iter (probe `entry_mod432` no longer locked at 74); 6/10 + C1 + no-wedge hold;
  AccuracyCoin non-decreasing. This is the make-or-break step.
- **R1w-2 — abort arming + truncation:** with the phase walking, the disable sweeps the `timer∈{0,1}`
  window → `cuo_in_window` 0→~4, `armed` 0→~4 (the cuo phase-basis fix from `mc-r1-dmc-phase-coherence`
  now bites). Wire the interleaved abort halt (the 1-cyc `pending_dmc_abort` consume) into `dmc_dma_step`.
  GATE: `disable_was_active` 12→~76, Explicit + Implicit Abort flip; 6/10 + C1 hold.
- **R1w-3 — OAM overlap + `$4015` edge:** `service_dmc_dma_during_oam` under R1 + the `$4015`-read edge.
  GATE: DMC+OAM + `DMA + $4015` flip.
- **R1w-4 — determinism/save-state + PROMOTE:** the GET-offset change alters the per-cycle bus-access
  pattern → re-run the determinism contract + save-state round-trip (the `in_dmc_dma`/span-state is in the
  save-state; the offset countdown is new state to serialize) + 60-ROM oracle (visual-proven re-baseline
  ONLY). Promote `mc-r1-substrate` + the phase-walk fix toward default, closing C1 + DMA together.

## Leading metrics (probe-driven, in order)

1. R1 DMASync sync phase WALKS +18/iter (was: locked at 74) — the make-or-break R1w-1 gate.
2. `disable_was_active` 12 → ~76.
3. `cuo_in_window{1,2,3}` 0 → ~4.
4. `armed` 0 → ~4.
5. Explicit/Implicit/DMC+OAM/`$4015` flip to pass; 6/10 + C1 + no-wedge HOLD throughout.

## Risks / open questions for the redesign

- **6/10 dependence on the snap (highest risk):** if the readout cluster's DMA landing is set by the GET
  parity-snap (not just the buffer-empty), the fixed-offset GET shifts their landing → regression. R1w-0
  must pin this BEFORE R1w-1. Mitigation: a per-DMA-kind offset (load vs reload vs the readout-observed
  DMA), matching default's `short?2:3`.
- **Re-wedge:** the span model change could re-introduce the DMASync hang (the original reason the pivot
  left burst). The timer clock (A) staying put should prevent it (it un-wedged), but R1w-1 must verify the
  battery completes all 139 (no `not_run` regression).
- **Determinism/save-state blast radius:** the GET-offset countdown is new per-cycle state; must be
  seeded deterministically + serialized (R1w-4).
- **Span length vs duration:** the abort measures DMA DURATION; the fixed-offset span (3/4) must match
  default's per-fetch span so `CalculateDMADuration` reads the answer-key values once the abort truncates.

## Banked state for the redesign

`5ceab83` (breakthrough, floor) + `a8e482e` (idle-halt). Gated diagnostic infra ready: `mc-r1-dmc-abort-
probe` (counters + `$4015` event log), `mc-r1-dmc-phase-coherence` (cuo + load-arm phase-basis, the
necessary downstream pieces, inert until the phase walks), `RUSTYNES_TRACE_MARGIN` (abort-entry capture).
Default 94.24% byte-identical. The redesign is R1w-1 (fixed-offset GET) — the one change that flips the
lock to a walk — gated + probe-gated at every step.

## R1w-1 ATTEMPTED + REFUTED (2026-06-04) — the phase-lock is a SYMPTOM, not the cause (causality corrected)

Implemented the fixed-offset GET behind `mc-r1-dmc-phase-walk` (removed the `get = !put_cycle` snap in
`dmc_dma_step_impl`; arm `noop_cycles = short?2:3` halt re-reads at span start, then an UNCONDITIONAL GET;
new `dmc_dma_noop_left` field). Built + measured (make-or-break: does the sync phase WALK?):

- **6/10 HELD, C1 held** (R1w-0 risk did NOT materialize — the readout cluster is unaffected). GOOD.
- **But the sync phase stayed LOCKED at 74** (entry_mod432 still {74:×10, 146:×1}) and the probe was
  byte-identical (disable_was_active 12, armed 0); the aborts did NOT flip. **R1w-1 INERT/REFUTED.**

**Causality CORRECTED (the R1w redesign premise was backwards):** the "phase locked at 74" is NOT caused
by the GET-parity-snap — it is a SYMPTOM of R1 **not running the abort sweep at all.** Default's +18 phase
WALK is produced by the sweep's varying CLOCKSLIDE (47 iterations, each shifting the spin entry +18); R1's
11 syncs are all from a single NON-sweep context, so they share one phase (74). Removing the GET gate
can't make the phase walk, because the walk comes from the test's clockslide loop — which R1 never enters.
(Also: the fixed-offset GET fires at the same cycle the parity-snap did here — both span 3/4 — so it was
inert; the phase lock traces to the ARM/timer-on-`!put_cycle`, but that is moot since the walk is a
sweep-running consequence regardless.)

**The corrected root, re-derived:** R1 BAILS the abort test after ~2 iterations (event log: 0 sweep pairs)
because **iteration 1/2 produces a WRONG DMA duration** — the test detects the mismatch and writes `0x06`.
The phase never walks because the sweep never runs because iteration 1/2 fails. So the question is NOT
"why is the phase locked" (symptom) but **"why does R1's FIRST abort iteration measure a wrong DMA
duration"** — a single, concrete, alignable event (the first `enable → DMASync → disable → CalculateDMA-
Duration`), NOT the cumulative sweep. The answer key opens `04 04 04 04 04 04 ...` (the first ~6 iterations
expect duration 4, NO truncation), so R1's early iteration must be giving a non-4 duration even though no
truncation is expected — i.e. R1's un-aborted DMA SPAN itself is the wrong length at the abort-test load,
OR the duration-measurement instruction timing is off by R1's per-cycle access shape.

**R1w REDIRECT:** the make-or-break failed; do NOT pursue the phase-walk (refuted — it's a symptom).
NEXT = trace ONE abort iteration (the FIRST), aligned on the abort-test entry, default vs R1: the
`$4015`-enable, the load DMA span, the DMASync, the `$4015`-disable, and what `CalculateDMADuration`
measures — to pin why R1's iteration-1 duration ≠ 4. That single-event divergence (not the cumulative
phase) is the real root. The phase-walk change is REVERTED (gated, inert, refuted; tree byte-identical).
`mc-r1-dmc-phase-coherence` + the probe/event-log/`RUSTYNES_TRACE_MARGIN` infra stay banked.

## R1w iteration-trace (2026-06-04) — R1's span is CORRECT (4); the failure is never-arm + broken iteration structure

Traced the abort-entry DMC DMA spans, default vs R1, by the TRUE metric (consecutive `DmaRead` runs, which
tag halt + GET — NOT the `in_dmc` count, which drops R1's GET cycle = the `e6f0271` artifact):

```
                 TRUE span-length hist (DmaRead runs)
DEFAULT:         4:581, 1:368, 3:64, 2:8, 5:1   (4 = normal; 1 = abort-TRUNCATED = answer-key 01/00)
R1:              4:195, 3:16                      (4 = normal — CORRECT, matches default; NO span-1)
```

**FINDINGS (rule span-length OUT):** (1) R1's un-aborted DMA span is **4 — correct, identical to
default** (the old "1-cycle short" was re-confirmed as the `in_dmc`-count artifact). So the iteration-1
"wrong duration" is NOT a span-length bug. (2) R1 has **ZERO span-1 runs** — it never produces a
truncated DMA, because the abort never arms (`armed` 0). So R1's un-truncated durations ARE 4 (matching
the answer key's leading `04 04 04 04 04 04`), but it never produces the `03`/`01`/`00` the later entries
need — AND the enable→DMASync→disable iteration structure is broken (event log: no 2483-gap pairs), so it
bails after ~2 iterations regardless.

**The failure is NOT span length — it is (a) the abort never arming (no truncation → no `01`/`00`) and
(b) the broken iteration structure (the enable→DMASync→disable timing is off so the test bails early).**
Both bottom out at the SAME DMASync/phase coherence already established as the multi-week core: the abort
sweep needs the DMC-vs-CPU phase to advance across iterations (default's clockslide walk), which R1's
master-clock substrate does not reproduce.

**LIMIT OF BUS-TRACE DIAGNOSIS REACHED.** Bus traces have now ruled out every localized cause (span,
period, conflict value, phase-basis, GET gate) and pinned the failure to the cumulative DMASync/iteration
coherence. Pinning the EXACT iteration-1 divergence further requires reading the AccuracyCoin abort test's
own `CalculateDMADuration` + DMASync logic (ROM disassembly) — a DIFFERENT tool than bus-trace diagnosis —
or committing to the master-clock DMC coherence core directly. The bus-observable diagnosis is COMPLETE
and exhaustive; the 4-test tail is the master-clock DMC coherence (multi-week), confirmed from ~6
independent angles (span/period/conflict/phase-basis/GET-lock/iteration-structure). Data:
`/tmp/RustyNES_v2/entry_{def,r1}.csv`.

## OPTION (a) ROM DISASSEMBLY (2026-06-04) — DECISIVE: 3 of 4 fails = one gate, "normal DMA Y must == 4"; R1 measures Y=3

Disassembled the AccuracyCoin abort tests (`/tmp/RustyNES_v2/AccuracyCoin.asm`, MIT). The mechanism:

- **`CalculateDMADuration` (asm:12899):** `Y` = iterations of a loop that reads `LDA $4000` and stalls 560
  cyc/iter (575 total vs the 576-cyc DMA period → the read walks 1 cyc EARLIER per iter), exiting (`BEQ`)
  when `$4000` reads `$00`. So **Y = the iteration at which the walking read first catches the DMC DMA's
  `$00`-conflict cycle** — a ~1-cycle-resolution measure of the DMC GET-conflict's absolute phase.
- **`CheckDMATiming` (asm:12944):** runs ONE normal (un-aborted) looping-rate-`$4E` DMC DMA through
  `CalculateDMADuration`. On default it yields **Y = 4**.
- **THE GATE:** `TEST_DMCDMAPlusOAMDMA` (asm:12963), `TEST_ExplicitDMAAbort` (13086), and
  `TEST_ImplicitDMAAbort` (13157) EACH open with `JSR CheckDMATiming; CPY #4; BNE FAIL` — **before** their
  sweep loops. `TEST_Fail` stores `(ErrorCode<<2)|2`; `ErrorCode` defaults to 1 and the first `INC` is
  AFTER the gate ⇒ a gate failure stores **`0x06`** — exactly the code all three show.

**MEASURED (scan_dma_abort now prints `$50` = DMC+OAM's `STY <$50` CheckDMATiming Y, captured at the
`$0477`-set frame): DEFAULT Y=4, R1+idle Y=3.** So **R1's normal DMC DMA measures Y=3, must be 4** — the
`CPY #4` gate fails, the sweep never runs (⇒ the event-log "no 2483-gap pairs", `disable_was_active` 12,
`armed` 0 are ALL downstream of this single gate), and DMC+OAM + Explicit + Implicit all bail `0x06`.

**THIS UNIFIES 3 OF THE 4 FAILS INTO ONE SINGLE-CYCLE TARGET:** R1's normal-DMA GET-conflict phase is
~1 cycle off (Y=3 vs 4). The 4th fail (`DMA + $4015`, `0x0A` = ErrorCode 2) fails one step later — a
separate, smaller residual. The "multi-week cumulative-coherence" framing is REPLACED by a sharp,
measurable spec: **make R1's CheckDMATiming Y = 4** (shift the normal-DMA `$00`-GET-conflict phase +1
cycle later), verified live by `scan_dma_abort`'s Y line, gated on the 6/10 + C1 holding. Why earlier
phase levers + the phase-walk didn't move Y: they fired the GET at the same cycle / shifted the arm (broke
6/10); the target is specifically the GET-conflict's 1-cycle phase vs the `LDA $4000` read, 6/10-safe.

## OPTION (b) — make CheckDMATiming Y = 4 (the sharp target from (a))

The (b) coherence work is now scoped to one measurable condition. Plan:
- **b-0:** with `scan_dma_abort`'s Y line as the live metric, find the minimal change that moves R1's Y
  3→4 — the DMC GET (`$00`-conflict) must land 1 cycle later relative to the `LDA $4000` read. Candidate
  surfaces (all probe/Y-gated): the GET cycle within `dmc_dma_step_impl`, the idle-vs-read service split
  (idle-halt services the GET one cycle differently), or the open-bus drive timing of the GET.
- **b-1:** once Y=4 holds 6/10-safe + C1-safe + no-wedge, the three gated tests' sweeps RUN; then wire
  the abort truncation (`mc-r1-dmc-phase-coherence` cuo fix + the interleaved abort halt) so the sweep
  durations match the answer keys. Re-check `DMA + $4015` (0x0A) separately.
- **b-2:** determinism/save-state + promote.
Leading metric: **CheckDMATiming Y 3→4** (scan_dma_abort), then the 3 tests flip, then `$4015`.

## OPTION (b)-0 PROGRESS (2026-06-04) — Y=3 is inherent to the interleaved GET-conflict phase

Using `scan_dma_abort`'s live `CheckDMATiming Y` metric (target 3→4):
- **R1 WITHOUT idle-halt: Y = 3** (unchanged) → idle-halt is NOT the cause.
- **R1 under the reverted phase-walk (fixed-offset GET): the aborts stayed `0x06`** ⟹ Y stayed 3 → the
  fixed-offset GET (noop=3 → GET at span-cycle 4) fires at the same cycle as the parity-snap; didn't move Y.

**Interpretation:** the GET is parity-locked to READ cycles (the TriCNES `get = !put_cycle` get=read
constraint), so it can only shift by 2 cycles (get-to-get), never the 1 cycle Y needs. Default's burst GET
is at a FIXED offset (`noop_cycles` then unconditional get), so it can land on the cycle parity R1's
parity-locked GET cannot. The Y=3-vs-4 (1-cycle) gap is the interleaved-GET-parity-lock vs burst-fixed-
offset difference, at the resolution of the `CalculateDMADuration` walking read.

**b-0 NEXT (focused, the decisive measurement):** trace R1 vs default's CheckDMATiming GET cycle (the
`$00`-sample read that drives open-bus) and the catching `LDA $4000` read, to pin the exact 1-cycle
source — is it (i) the GET landing 1 cycle off (parity-lock), (ii) the open-bus `$00` drive being visible
1 cycle late under the interleaved access shape, or (iii) `DMASync_50CyclesRemaining` positioning the read
loop 1 cycle off (same root, upstream). Then the fix targets that exact cycle, Y-gated + 6/10-gated. The
target is now a SINGLE cycle with a live metric — the most tractable the abort tail has ever been.

## b-0 GET-vs-read TRACE (2026-06-04) — (ii) ruled out; the 1-cycle gap is the GET ABSOLUTE PHASE

Traced the `$00`-conflict GET → catching `LDA $4000` read, default vs R1 (targeting `$0477`):
**the offset is IDENTICAL = 1 cycle in BOTH** (the GET drives `open_bus=$00`, the CPU reads it the next
cycle). So **(ii) "open-bus `$00` visible 1 cycle late under the interleaved access shape" is REFUTED** —
the GET→read mechanism is the same. The Y=3-vs-4 gap is therefore the GET's **absolute phase** relative
to the walking-read loop = the ~1-cycle DMC-conflict-phase offset already pinned (entry_mod432 R1 74 vs
default 406) = candidate (i)/(iii) combined: R1's DMC GET-conflict is parity-LOCKED (the `5ceab83`
timer-on-`!put_cycle` un-wedge) to a cycle 1 off default's fixed-offset burst GET, and that same offset
positions both the DMASync read-loop and the CalculateDMADuration catch.

**b-0 CONCLUSION:** the fix is to shift R1's DMC GET-conflict absolute phase by exactly 1 cycle (Y 3→4)
WITHOUT losing the parity-lock's un-wedge (`5ceab83`) or the 6/10. The parity-lock pins the GET to even
get-cycles (shiftable only by 2); default's fixed-offset burst GET lands on the odd cycle in between. So
the 1-cycle fix is structural: either (A) give the GET a fixed offset from the arm that can land on the
in-between parity (the phase-walk model — but it must change the EFFECTIVE GET cycle, which the prior
attempt didn't), or (B) shift the ARM (buffer-empty) parity by 1 for this DMA kind — risking the 6/10
(arm-align did). The target is now a SINGLE cycle (Y 3→4) with a live metric (`scan_dma_abort`'s Y line)
and (ii) eliminated; the remaining axis is the parity-lock-vs-fixed-offset reconciliation at 1-cycle
resolution = the structural core, but maximally narrowed. Data: `/tmp/RustyNES_v2/ck_{def,r1}.csv`.

## b-1 FORM (A) ATTEMPTED + REFUTED (2026-06-04) — shifting the GET off get-parity WEDGES; tension is fundamental at 1 cycle

Implemented form (A) behind `mc-r1-dmc-phase-walk`: fire the interleaved DMC GET on the cycle immediately
after `dmc_halt` clears (the put cycle, 1 cyc earlier — direction: default Y=4 > R1 Y=3 ⟹ R1's GET is 1
late) instead of waiting for the 2nd get-cycle (`do_get = !dmc_halt` vs `get_cycle && !dmc_halt`).

**Result: WEDGE — the entire DMA battery is `(unset)` (no result bytes set, including the 6/10).** Firing
the GET off the get-parity breaks the DMASync `$4000`-conflict mechanism the whole battery hangs on, so it
never reaches the DMA tests. Reverted (tree byte-identical).

**DECISIVE — the tension is fundamental at 1-cycle resolution:** the get-parity GET is LOAD-BEARING for
the un-wedge (`5ceab83`) + the 6/10 + DMASync; the abort's `CheckDMATiming Y==4` needs the GET exactly 1
cycle OFF that parity; **the two are mutually exclusive in the single-GET interleaved model.** With form
(B) variants (shift the arm / inline-service) already proven to break the 6/10 (arm-align, arm-delay1,
inline-service), **ALL localized 1-cycle shifts within the interleaved model either WEDGE (form A) or
break the 6/10 (form B).** No localized 1-cycle fix exists — confirmed empirically at the finest grain.

**Root cause, now fully resolved:** R1's GET is 1 cycle late because the SERVICE starts 1 cycle late (the
arm→`read1`-loop-service latency), where default's burst services INLINE at the buffer-empty read (0
latency) with a fixed-offset GET — giving Y==4 AND the un-wedge simultaneously. Reproducing default's
behavior requires the **inline-at-buffer-empty service model on the R1 substrate** — which the legacy
`tick_one_cpu_cycle` port (inline-service) could not do (it broke lockstep timing). That is the genuine
service-model rewrite (the multi-week core), now with the EXACT success spec (CheckDMATiming Y 3→4, live
via `scan_dma_abort`) + the EXACT mechanism (inline buffer-empty service, fixed-offset GET, reproducing
default's lockstep DMA cycle on R1 without `tick_one_cpu_cycle`).

**FINAL STATE of (a)+(b):** (a) pinned + unified the root (Y must==4, 3 of 4 fails; R1=3) with a live
metric. (b) proved NO localized 1-cycle fix (form A wedges, form B breaks 6/10) ⟹ the inline-buffer-empty
service-model rewrite is required, now exactly specified. The bus/ROM diagnosis is exhaustively complete;
what remains is that one structural rewrite, gated by the live Y metric + the 6/10/C1 suite.

## Gates (EVERY phase — per `feedback_adr0002_ask_before_rollback`: on a stop-condition, REPORT + ASK)

6/10 DMA held (`scan_dma_abort`) · `cpu_interrupts_v2` C1 trio held · no battery wedge (`not_run` ≤ 5) ·
AccuracyCoin pass-rate monotonic non-decreasing from 92.09% · default build byte-identical · determinism
+ save-state intact · 60-ROM oracle byte-identical (visual-proven re-baseline only). Rollback floor
`5ceab83` (breakthrough) + `a8e482e` (idle-halt).

## Banked state / data

Breakthrough `5ceab83` (92.09% / DMA 6/10 / C1 closed / un-wedged) + idle-halt `a8e482e` (6/10-safe,
period 4× tighter). Default 94.24% byte-identical. Diagnostic data (regenerable):
`/tmp/RustyNES_v2/{mesen_dmc,dmc_def_0479,dmc_r1_0479,dmc_r1noidle_0479,spin_def_0479,spin_r1_0479}.csv`.
