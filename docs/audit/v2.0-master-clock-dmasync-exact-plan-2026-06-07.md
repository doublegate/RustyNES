# Master-clock rewrite — focused on DMASync-catch-exactness (2026-06-07)

## The de-risked target (this session's load-bearing result)
The CheckDMATiming 1587-vs-1588 (Y=3 vs 4) gating the 6-test DMA tail is NOT any single
component — span (4, correct), open-bus, byte-timer-rate, and LOAD-span were each
engineered+measured this session and ALL absorbed. ROOT (proven by construction):
`DMASync_50CyclesRemaining` catches the DMA's `$00` then applies a FIXED 350-clockslide
anchor, so any single-component shift moves the catch AND the byte-timer together → the
anchor compensates → Y unchanged. So the rewrite's SOLE make-or-break is:

> **Make the DMASync `$00`-catch master-clock-exact**, so the re-sync can no longer absorb
> the LOAD-span-3 fix already built (`mc-r1-dmc-load-get-entry`, proven to change the span).

This is NOT "rewrite everything and hope" — it is one precise property to achieve, with a
fix already in hand (the load-span-3) that becomes load-bearing once the absorption stops.

## Verification metric (exact, artifact-free)
- `trace_dma_4015 RUSTYNES_FULL_RANGE` on `$0478`: the DMASync LOAD must trace span-3
  (get-start) AND the CheckDMATiming setup window must be 1588 (currently 1587) → Y=4.
- Then the abort cluster (Implicit/Explicit Abort, DMC+OAM) closes via `CPY #4`.

## Stages (umbrella `mc-r1-full-cpu`; canary EVERY stage: nestest 0-diff + C1 trio
## cpu_interrupts_v2 + 6/10 DMA + sacred trio + 60-ROM oracle + ppu_vbl_nmi + determinism)
- A/B/D: DONE (cpu-implied/stack-dummy-reads, OAM-unify 7ae11d2).
- C: indexed-mode index/pointer dummy reads (addr_zp_x/zp_y/ind_x + abs_x/abs_y/zp store
  UNCONDITIONAL dummy; Mesen GetZeroXAddr/GetIndXAddr/GetAbsXAddr).
- **E (make-or-break, the focused target): collapse cpu.cycles + cycles_emitted +
  apu.cpu_cycle + master_clock to ONE counter; replace idle_tick burn-loop with
  `internal_cycle` = a dummy read of the held bus address (every cycle a bus access);
  DELETE dma-cycle-budget. CRITICAL NEW SUB-GOAL: the DMASync `$00`-catch (the DMC GET's
  open-bus drive + the CPU `LDA $4000` sample) must be computed on the SINGLE master clock
  so the catch is exact and does NOT re-sync to absorb the load-span-3 fix. Promote
  `mc-r1-dmc-load-get-entry` once E lands (it is proven to change the load span; it becomes
  Y-moving once the absorption is gone).**
- F: promote to default + serialize master_clock as sole save-state authority + retire the
  #[ignore]'d DMA-tail tests.

## DO-NOT-RETRY (engineered+measured refuted, this session + prior)
reload-span 3->4 (already 4); open-bus deferral (RUSTYNES_PHI_DEFER 1==2 inert; mc-phi-subpos
ALSO breaks battery 139->93); cpu-rate byte-timer (inert); LOAD-span-3 IN ISOLATION (changes
span, absorbed by DMASync re-sync, regresses DMC test 21->18) — it is RIGHT but needs E first;
RW-2 cumulative OAM fold (regresses C1); budget toggle (Y->2 wrong way). The load-span-3 fix
is KEPT as the post-E verification lever, not a standalone.

## Achievable in PARALLEL (not behind the DMASync wall, separate sessions)
BG Serial In (won on dmc-landing-coherence via mc-ppu-subpos — consolidate); $2007 Stress
(sub-dot PPU; reads land ~7 dots early in the idle region, characterized 2026-06-07).

## BREAKTHROUGH (2026-06-07): load-span defer MOVES Y — architectural-limit REFUTED
`mc-r1-dmc-load-get-entry` (both read1 + idle_tick paths, defer when check-time `!put_cycle`
so the load ENTERS on get = span-3) MOVED the Implicit Abort from error 2 (CheckDMATiming
Y!=4 gate) to error 4 (a LATER abort sub-test) — i.e. **Y went 3->4, the CPY #4 gate now
PASSES**. The DMC test moved 21->18. So the "DMASync absorbs / architectural limit"
conclusion (mine this session + 18 months) was WRONG — it stemmed from a parity/targeting
bug (I gated only read1, and with put_cycle instead of !put_cycle; the loop checks parity
BEFORE start_cycle flips it, and the DMASync load fires on the idle/NOP path). With the
correct both-path + !put_cycle defer, Y MOVES. REMAINING to fully close the cluster:
(1) Explicit Abort + DMC+OAM still error 2 — their CheckDMATiming loads not yet covered
(different state); (2) Implicit Abort's later sub-test (error 4) = the abort occurrence/
truncation logic, now reachable past the Y gate. NEXT: extend the defer to all abort
contexts + close the post-gate abort sub-tests. Floor gated/byte-identical; this is the
first lever in 18 months to move Y, validating load-span-3 as THE mechanism.

## M-1 EXECUTION — first experiment (2026-06-07): visibility-delay-alone is INSUFFICIENT
Composed `mc-r1-dmc-reload-visibility-delay` + `mc-r1-dmc-load-get-entry` on `mc-r1-full-cpu`:
93.53%/9 — IDENTICAL to load-span-3 alone (Implicit Abort err 4 = Y-gate passes; Explicit Abort +
DMC+OAM still err 2). ROOT: the current `reload-visibility-delay` arms `pending_dmc_dma_next` at cycle
START (in `tick_with_external` `dmc_step_reload_arm`, apu.rs:823-825) and promotes it same-cycle at END
(`cpu_clock_apu_dmc`, bus.rs:3030-3035). That is NOT the reference's CPU-then-APU order — the byte-timer
`bits->0` still fires at the START within-cycle position, so the reload-DMA arm cycle is unchanged.
PRECISE M-1 IMPLEMENTATION (refined): under a NEW flag (e.g. `mc-r1-dmc-end-tick`), (1) force
`dmc_at_start=false` so the DMC byte-timer clock + delay-arm + reload-arm do NOT run in
`tick_with_external` (START); (2) call `self.apu.tick_dmc()` (the existing END-path: delay-arm →
clock_timer → reload-arm) from `end_cycle`/`cpu_clock_apu_dmc` AFTER the CPU's bus access; (3) the
reload-arm latches `pending_dmc_dma_next`; (4) move the `promote_dmc_pending_next` to the NEXT cycle's
START (`cpu_clock`, before the read1 `while dmc_dma_pending` check) so the arm at cycle N's END is
first-serviced at cycle N+1's read1 (1-cycle delay = reference). Loads stay direct (delay-arm →
`pending_dmc_dma`, get-entry via load-get-entry). VERIFY: Explicit Abort $0479 + DMC+OAM $0477 trace
reload span-4 put-first AND advance past err 2; C1 trio green; nestest 0-diff. NOTE: tick_dmc currently
clocks the byte-timer on `!put_cycle` (apu.rs:967) — confirm that polarity matches the START path's
`apu_phase`-true under the end-tick composition, or the GET phase will shift (the prior F-2 divergence-A).

## M-1 EXECUTION — end-tick variant REFUTED (2026-06-07); reload contexts need M-2+M-3
`mc-r1-dmc-end-tick` (force `dmc_at_start=false`; move byte-timer clock + delay/reload arms to
`tick_dmc()` in `cpu_clock_apu_dmc` END; + visibility-delay + load-get-entry): REGRESSED — Implicit
Abort err 4→2 (the load-span-3 Y-gate WIN was UNDONE), DMC test 18→21. Moving the byte-timer to END
shifts its `bits->0` within-cycle position, which shifts the load's arm cycle and breaks the
load-get-entry span-3. So the byte-timer must stay at START (where the load defer works); the
end-tick is the WRONG M-1. Reverted.

CONCLUSION (two M-1 variants measured): M-1 (within-cycle order, either form) does NOT move the
Explicit Abort / DMC+OAM. Confirmed: those are RELOAD contexts (active DMC at the DMASync `$4015=$10`),
and the load-span-3 lever only fixes the LOAD context (Implicit Abort). The reload contexts' DMASync
`$00`-catch phase is off by 1 and is NOT a within-cycle-order issue — it is the cumulative-counter
exactness (M-2) + the DMASync `$00`-catch on the single counter (M-3). So M-1 is NOT a separable
prerequisite that closes anything on its own; the make-or-break is **M-2 (counter-collapse) + M-3
(DMASync exact)**, with the committed load-span-3 (load context) + visibility-delay (reload arm
invisibility) as the composing levers ON the collapsed counter. REVISED sequencing: go straight to
M-2 (collapse `cycles`/`cycles_emitted`/`apu.cpu_cycle`/`master_clock` to one; delete budget; internal
cycle = held-addr dummy read), then M-3, verifying the reload-context DMASync window = 1588 on the
single counter. M-1's load-span-3 (committed `6cd230a`) and the reload-visibility-delay flag are KEPT
as the levers to enable once the counter is exact — DO NOT add the end-tick.

## HARD-DATA PIN (2026-06-07): Implicit Abort error 4 = Loop3 LOOPING-RELOAD at X=10,11
scan_dma_abort (breakthrough config: mc-r1-full-cpu + load-get-entry) dumped the three Implicit
arrays at the $478-set frame. RESULT:
- $500 (Loop1, 1-byte NON-loop implicit abort) = Key1 EXACTLY ✓
- $520 (Loop2, "1-cycle DMA must NOT occur on a write cycle") = Key2 EXACTLY ✓  ← the
  write-cycle-cancel (mc-r1-dmc-abort-cancel, cpu.rs:918-920) ALREADY WORKS. RED HERRING eliminated.
- $540 (Loop3, the LOOPING reload, $4010=$4E) = [..00(10) 00(11) 04(12) 04(13) 04(14) 04(15)]
  vs Key3 [..04(10) 04(11) 04 04 04 04] → MISMATCH at X=10,11 (00 vs 04).
ErrorCode trajectory: init 1 → Y-gate INC 2 → wait, decoder reports 0x12=(4<<2)|2=error 4. The three
KeyLoops INC after each pass; the fail is KeyLoop3 ($540 vs Key3) → error 4. So:
**Implicit Abort error 4 = the LOOPING-reload DMA at X=10,11 is detectable 2 positions LATE** (RustyNES
measures it starting X=12; HW starts X=10). The implicit 1-cycle abort logic (Loop1/Loop2) is fully
CORRECT. CONCLUSION: the entire abort cluster is UNIFORMLY the reload-GET-phase (Implicit error 4 =
Loop3 looping reload 2-late; Explicit/DMC+OAM error 2 = same reload phase fails the Y-gate). There is
NO separable abort sub-bug to pick off — all of it is the single reload-DMA-phase exactness (M-2+M-3),
which means the make-or-break truly is the reload phase, and the abort-cancel/write-cycle pieces are
already done. Next: trace the Loop3 reload GET at X=10 vs X=12 (trace_dma_4015) to measure the exact
2-position lateness, then target it via the counter-exactness — NOT via a blanket reload-span defer
(refuted) or the write-cycle path (already correct).

## EXHAUSTIVE ELIMINATION (2026-06-07): the make-or-break is M-2 (cumulative-count exactness) ALONE
This turn measured + refuted EVERY non-M-2 candidate for the reload-GET-phase lateness:
1. Reload-arm invisibility (M-1): RustyNES ALREADY arms after the read's `while dmc_dma_pending()`
   check (at start_cycle, post-check) → already serviced next-cycle = the reference invisibility is
   ALREADY satisfied. Visibility-delay + end-tick both inert/regressive (measured). NOT the lever.
2. Byte-timer global phase (mc-r1-dmc-phase-minus1 + load-get-entry): 83.45%/23 — broke SH*, the whole
   DMA cluster, C1. The byte-timer phase is CORRECT GLOBALLY; a global shift over-corrects everything.
   NOT the lever.
3. Write-cycle abort-cancel: ALREADY correct ($520=Key2). NOT the lever.
RESIDUE (by exhaustion): the reload-GET is ~2 positions late ONLY in the reload/abort context, while
globally exact → a CONTEXT-SPECIFIC cumulative-cycle-count drift of ~1-2 in the DMA/reload path. The
sole remaining lever is **M-2: make the cumulative master-cycle count exact across the DMA/burn-loop/
budget accounting** so the reload context stops drifting. Prior RW-2 (partial: remove dma_mc_consumed
fold + inline OAM) regressed C1 — so M-2 must be the FULL "every cycle a real bus access on ONE
counter" version (Mesen `_state.CycleCount` + DummyRead), done with the canary gate, NOT the partial
fold-removal. The C1-sensitivity lives in the `dma_mc_consumed` fold (master_clock += consumed at
end_cycle) — the collapse must preserve the φ2 IRQ sample while unifying the count. This is now the
SINGLE de-risked make-or-break: every other lever is measured-out.

## TRACE EVIDENCE + the C1-safe M-2 constraint (2026-06-07, trace_dma_4015 on $0478 Loop3)
The Implicit Loop3 reload trace shows the reload DMA STRUCTURE is correct: clean 3-cycle spans, each
$FFxx sample GET preceded by put/get halt cycles, `put_cycle` alternating exactly through the span, the
DMASync-conflict GET correctly landing on $4000 driving $40, the key byte $FF50=$00 fetched. So the
reload-GET 2-position lateness ($540[10,11]) is NOT in the reload mechanism (parity + span are exact) —
it is in the DMASync MEASUREMENT ANCHOR (the "50 cycles after RTS" + 350-clockslide), which the
load-get-entry corrected by 1 (the load span) but which retains a residual that compounds to 2 in
Loop3. This is the "DMASync re-sync absorbs single shifts" property → the anchor itself must be
master-clock-exact (M-3), which requires the unified count (M-2).

THE C1-SAFE M-2 CONSTRAINT (the RW-2 regression mechanism, now pinned): RW-2 regressed cpu_interrupts_v2/4
because making DMA cycles advance the unified `master_clock` inline (removing the `dma_mc_consumed`
fold) shifts the POST-DMA instruction's φ2 IRQ sample (`end_cycle::handle_interrupts`). The DMA cycles
themselves do not sample IRQ (CPU halted), but folding their master-clocks inline moves `run_ppu_to`
for the next real cycle → the C1 IRQ/NMI recognition dot moves. So M-2 must satisfy BOTH: (a) the DMA
cycles advance the count the reload-phase/DMASync-anchor reads (for exactness), AND (b) the post-DMA
real cycle's φ2 IRQ sample stays at the same PPU-dot it is on the floor (for C1). The reference
reconciles these because Mesen samples IRQ at a fixed point in `EndCpuCycle` that is invariant to how
many DMA cycles preceded — RustyNES's `dma_mc_consumed` fold makes the post-DMA sample dot DEPEND on
the DMA length. The M-2 design must decouple the IRQ-sample dot from the DMA-cycle count: keep the φ2
sample at `master_clock - PPU_OFFSET` computed from the CURRENT cycle's own pre/post, NOT from the
folded DMA total. This is the precise, C1-safe collapse — not RW-2's blanket fold removal.

## SESSION CONCLUSION (2026-06-07) — make-or-break cornered to NOVEL M-3 open-bus-catch
master_clock is ALREADY exact for the DMC path (dma_mc_consumed fold ~0; DMA cycles run CPU-driven
through start/end_cycle). Parity (put_cycle) is ALREADY exact (RW-1). The reload STRUCTURE is exact
(trace). internal-cycle (idle=bus dummy read) is INERT (byte-identical 93.53%). So the $540[10,11]
2-late residual is NOT in master_clock, parity, the reload span, the byte-timer phase, the arm
visibility, the write-cancel, or the idle-bus-access — ALL measured-out this session.

The residual is the DMASync $00-CATCH SUB-CYCLE: CalculateDMADuration slides `LDA $4000` and catches
when the reload GET drives $00 on the EXTERNAL open bus. The GET's open-bus update vs the LDA's sample
are off by a sub-cycle ONLY at the X=10,11 phase boundary → the catch lands 2 slides late. This is the
M-3 axis (the open-bus φ-position), and it is the SOLE remaining lever — but it requires NOVEL code,
not a flag toggle: mc-phi-subpos (the existing φ2-defer) over-applies and breaks the battery 139->93.

The M-3 implementation (next dedicated session): make the GET drive `external_open_bus` and the
`LDA $4000` read it on the SAME master-clock sub-position the reference uses (Mesen: every Read latches
external open bus; write-only $4000-$401F reads return it; the GET's latch is visible to the very next
bus access), gated so it ONLY affects the abort/DMASync context and does NOT regress the battery the
way mc-phi-subpos did. This is the one piece of genuinely-new code the whole DMA tail reduces to; every
toggle-able lever is now exhausted and committed to this audit chain. Floor byte-identical (93.53%/9).

## CATCH MECHANISM FULLY PINNED (2026-06-07, trace_dma_4015 $4000 read-data analysis)
CalculateDMADuration (asm 12655): `LDY#0; Clockslide_45; Loop: LDA $4000; BEQ End; ClockslideFromWord
560; INY; JMP Loop`. Each iter slides the `LDA $4000` 1 cycle earlier vs the 576-cycle DMA period
(15 overhead + 560 slide = 575). Y = iters until `LDA $4000` reads $00 = the cycle-offset at which the
reload GET drives the $FF50=$00 sample byte onto the EXTERNAL open bus coincident with the CPU read.
TRACE (Implicit Loop3): CPU $4000 reads = $40 ×1465 (its own operand-high $40 / the $4000-conflict GET)
and $00 ×99 (the catch). Catches at cy 48895905, 48898065, 48900675. **$540[10,11] Y=0 means at the
X=10,11 phase the $00 coincidence happens on CalculateDMADuration's FIRST slide read (premature) where
HW has Y=4 (coincidence 4 slides in).** So RustyNES's reload-GET-$00 lands on the external bus ~2-4
cycles too early RELATIVE to the DMASync anchor at exactly the X=10,11 sub-phase — a coincidence-phase
boundary, not a span/parity/count error (all proven exact). The M-3 fix is to shift WHEN the reload
GET's sample byte becomes visible on the external bus to the CPU's $4000 read by the sub-cycle the
reference uses — WITHOUT the global φ2-defer (mc-phi-subpos) that broke the battery. This requires a
TriCNES per-cycle cross-diff at the X=10,11 Loop3 CalculateDMADuration to read off the exact target
sub-cycle, then a context-scoped open-bus-visibility shift. Fully characterized; implementation is the
next dedicated step. Floor byte-identical 93.53%/9; breakthrough 6cd230a preserved.

## REFUTED (2026-06-07): reload-only open-bus-visibility defer (mc-r1-dmc-reload-obus)
Built a RELOAD-ONLY φ-defer (defer the open-bus drive only when `!dmc_dma_is_load()`, so the
DMASync-load anchor stays immediate). RESULT: 92.81%/10 — REGRESSED (added "DMC DMA Bus Conflicts"
error 2) and did NOT fix Implicit Abort (still error 4 / $540[10,11]). LESSON: the reload GET's
external open-bus is load-bearing + ALREADY CORRECT for DMC DMA Bus Conflicts; shifting it breaks that
without touching $540. So $540[10,11] Y=0 is NOT a reload-GET-open-bus-visibility effect either. The
$00 the slide catches at Y=0 must come from the instruction-level bus interaction (operand-low $00
fetch returned when the DMA halts mid-LDA, or the byte-timer firing the reload 2 cycles late ONLY at
the X=10,11 sub-phase). Both are the cumulative master-clock exactness (M-2), context-specific, not a
single open-bus knob. Reverted. NET (this session): the abort cluster's $540[10,11] is conclusively a
context-specific cumulative-count/byte-timer-fire boundary effect — every open-bus + arm + span +
phase + visibility knob is measured-out; only the C1-safe full master-clock collapse (M-2) remains, and
a TriCNES per-cycle cross-diff at X=10,11 Loop3 is the precise tool to pin the exact target before
attempting it. Floor byte-identical 93.53%/9.

## TriCNES CROSS-DIFF ORACLE OBTAINED (2026-06-07) — the M-2 verification target
TriCNES IS runnable (dotnet; /tmp/tricnes-harness prebuilt). Captured the Implicit-Abort region
(cyc ~48.8-48.9M) → saved to `crates/nes-test-harness/golden/tricnes/implicit_abort_region.txt`.
REFERENCE TARGETS:
- At the Loop3 `$4015=$10` enables: TriCNES `dmctimer=48, put=0` (the landmark-lock value; a few are
  46/put=1 or 32/34 at loop boundaries). This is the exact "dmc_timer@lm must lock" metric.
- TriCNES reload GET spacing VARIES in a 450,432,432,576,576,576 repeating pattern (addr constant
  FFC0 = the 1-byte looping sample). RustyNES's trace shows CONSTANT 432 spacing with INCREMENTING
  addrs ($FF52,$FF53,$FF4E,..) → CANDIDATE DIVERGENCE: RustyNES may not be looping the 1-byte sample
  to FFC0 (current_addr increments instead of reloading), OR the regions are mis-anchored (the
  $FF5x addrs may be the DMASync multi-byte sample, not the Loop3 FFC0 reload). MUST landmark-align
  on a `$4010=$4E`+`$4015=$10` pair before concluding.
NEXT (the M-2 fix, now oracle-backed): (1) add `dmc_timer` to trace_dma_4015 columns; (2) landmark-
align RustyNES's Loop3 `$4015` enable to TriCNES's; (3) confirm whether RustyNES's dmctimer@enable
and GET-spacing match 48/put=0 + 450/432/576 — the divergence there IS the 2-cycle source; (4) fix it
(sample-loop-reload-to-FFC0 OR the byte-timer phase) to lock the metric, verify Implicit $540→Key3.
Oracle committed so it survives /tmp wipe.

## CONCRETE M-2 DRIFT OBSERVED via cross-diff (2026-06-07) — sample-restart timing lag
DECISIVE: RustyNES trace shows `$4012=$FF`+`$4013=$00` (→ sample_addr=$FFC0, length 1) at each DMASync,
but the GET FETCHES $FF4E-$FF53 (the OLD multi-byte sample), NOT $FFC0. TriCNES fetches FFC0. Mechanism:
at the `$4015=$10` re-enable, nesdev restarts the sample ONLY if bytes_remaining==0. RustyNES has
bytes_remaining>0 (old sample STILL PLAYING) → no restart → fetches old addrs; TriCNES has
bytes_remaining==0 (old sample FINISHED) → restarts to $FFC0. => **RustyNES's byte-timer sample
PLAYBACK LAGS TriCNES's**: the old sample finishes ~N cycles late, so the restart-to-$FFC0 is delayed,
the wrong sample's bytes play, and the $00-catch byte position (hence CalculateDMADuration Y / $540)
shifts. This is the cumulative byte-timer drift CONCRETELY OBSERVED (not distributionally inferred) —
the exact thing M-2 (cycle-exact byte-timer playback on the unified clock) must fix. The oracle
(`golden/tricnes/implicit_abort_region.txt`, dmctimer@enable=48/put=0) is the lock target. NEXT (M-2
fix, oracle-backed): make RustyNES's per-byte playback advance match TriCNES so bytes_remaining hits 0
at the same cycle → the restart + $FFC0 fetch align → $540 Y locks to Key3. The lag is small per byte
(the 2-cycle boundary at X=10,11) and accumulates over the multi-byte old sample — confirming it is a
cumulative-count exactness fix, NOT a single span/phase knob (all measured-out). Floor byte-identical.

## CORRECTION (2026-06-07): byte-timer PERIOD is identical to TriCNES — drift is start/cumulative, not per-bit
Self-check: RustyNES `timer_period = cpu_period/2 - 1 = 35` → 36 APU-ticks → 72 CPU cyc/bit (rate $E).
TriCNES reload=72, -=2/APU-cycle → 36 decrements → 72 CPU cyc/bit. IDENTICAL. So the prior turn's
"byte-timer playback LAGS" framing is OVERSTATED — there is NO per-bit period error. The observed
sample-restart divergence (RustyNES bytes_remaining>0 vs TriCNES ==0, fetching $FF4E-53 vs FFC0) is
therefore EITHER (a) a cumulative-count / sample-START-cycle drift (the $4015-enable lands at a
different point in the old sample's playback), OR (b) residual trace-region MISALIGNMENT I have not
frame-anchored — the $FF4E-53 addrs may be a different test's multi-byte sample, since the Implicit
test window interleaves DMASync (FFC0) with other samples. HONEST STATUS of the cross-diff: the oracle
is obtained (dmctimer@enable=48/put=0, committed) but a CLEAN frame-anchored alignment (match the exact
Loop3 iteration in both emulators, then compare dmctimer + GET addr/cycle at that single landmark) is
still required before the divergence is proven and the fix targeted. That frame-anchored cross-diff +
the C1-safe cumulative fix is the dedicated M-2 work. Do NOT claim the sample-restart divergence as the
root until frame-anchored. Floor byte-identical 93.53%/9; nestest 0-diff; breakthrough 6cd230a intact.

## ***MAJOR REFRAME (2026-06-07): the reload GET is EXACT; the divergence is the ABORT RE-ENABLE***
Ran the existing Φ-1 cross-diff tooling (scan_dma_abort + mc-r1-dmc-abort-probe), which computes
RustyNES's `$4010=$4E -> first $FFC0 GET` offset + the $FFC0 reload PERIOD vs TriCNES. HARD RESULT:
- NORMAL reload: RustyNES offset = **48 (×50)** = TriCNES 48 EXACTLY; PERIOD = **576 (×128)** = TriCNES
  576 EXACTLY. **The DMC reload GET timing is BYTE-EXACT to TriCNES — NOT mis-timed.**
- ABORT region (disable→re-enable near $4010): TriCNES offsets = 14(×17) + 11(×15); RustyNES = 15(×12)
  + 14(×2). **RustyNES's abort-re-enable GET lands ~3-4 cycles LATE (15 vs 11) and fires FEWER times
  (14 vs 32 non-48 events).**
=> THE 18-MONTH PREMISE ("reload GET span/fire-phase is 1-2 cycles off") IS REFUTED BY DIRECT
CROSS-DIFF: the reload is exact (48/576). The abort cluster fails because the ABORT (the $4015
disable→re-enable timing arming the DMA) lands the GET at offset 15 not 11, AND some abort GETs don't
fire at all. This is why every reload-span/byte-timer-phase lever was inert or regressive — they
targeted the EXACT part. The make-or-break is the **abort re-enable arm cycle** (the
$4015-enable-while-near-$4010 path): RustyNES arms it ~4 cycles late / drops events. NEXT: trace the
abort re-enable in `mc-r1-dmc-abort-cancel` (cpu.rs:790-804, write1 cancel 918-921; apu
dmc_step_delay_arm + the $4015-enable arm 1133-1184) at the offset-11 TriCNES events; align the arm so
RustyNES's offset becomes 11 + all events fire. This is a TARGETED abort-path fix, NOT the C1-risky
counter-collapse. Tooling: Φ-1 offsets (TriCNES=48 normal / 11+14 abort). Floor byte-identical.

## ABORT-LOGIC fixes REFUTED (2026-06-07): the +4 is NOT the disable/abort cycle consumption
Implemented `mc-r1-dmc-mesen-abort` (Mesen StopDmcTransfer): on $4015 disable of an active DMC, (a)
full-cancel when `dmc_need_halt`, then (b) broadened to cancel on ANY `was_active && pending_dmc_dma`.
BOTH byte-identical (93.53%/9) → `pending_dmc_dma` is NOT set at the $4015 disable, so no aborted DMA
runs after it; the abort LOGIC (schedule_explicit_dmc_abort cuo-window + the disable keep-alive) is NOT
the +4 source. Combined with mesen-span + phase-coherence (inert), this exhausts the abort-LOGIC
levers. So the Φ-1 abort-offset +4 (15 vs 11) is EITHER a windowing artifact of the 64-cap/event-order
in FFC0_OFF (the reload is proven EXACT 48/576, so a true reload-timing +4 is impossible) OR a subtler
cumulative effect upstream of the DMASync that no abort-path lever reaches. NEXT: build matched-event
Φ-1 (align the SAME abort sub-context in RustyNES + TriCNES, not aggregate counts) to confirm the +4 is
real before chasing it; if it's a windowing artifact, the abort tests' true failure is elsewhere
(re-examine the Explicit/DMC+OAM Y-gate reload-context DMASync directly). The CERTAIN result stands:
reload byte-exact, abort-logic not the lever. Floor byte-identical.

## MATCHED-EVENT CROSS-DIFF — tooling built, blocked on boot-offset alignment (2026-06-07)
Built the region-gated R4000 ($4000-read) log to compare RustyNES vs TriCNES $00-catches directly
(the actual CalculateDMADuration mechanism). TriCNES reads $4000->$00 exactly 5× over the whole run
(the genuine catches), $40 the other 472×. RustyNES (first-4096, region 48.1-48.5M) read $4000->$00
**84×** — suggestive that RustyNES drives $00 on the external $4000 open-bus FAR more often than TriCNES
(the likely premature-catch / Y=0 source). BUT: BLOCKED — RustyNES and TriCNES have a ~0.19-0.7M-cycle
BOOT OFFSET (RustyNES's Implicit executes at a different absolute cycle than TriCNES's 48.8M window),
and RustyNES's abort-cluster $4000 reads are NOT where frame×29781 predicts (cycle-counter vs
frame-timing mismatch). So a clean matched-event $00-catch comparison needs LANDMARK-RELATIVE alignment
(anchor on a shared ROM write like the Implicit Loop3 $4010=$4E+$4015 pair, compare $4000-read values
at relative cycles), not absolute-window gating. NEXT: (1) add a ROM-landmark anchor to both logs
(RustyNES log_4000_read + TriCNES R4000) keyed on the Nth $4010=$4E in the Implicit test; (2) compare
the $00-catch positions relative to that anchor; (3) if RustyNES catches $00 earlier/more, the fix is
the DMC-GET external-open-bus drive (NOT the reload-obus blanket defer, which regressed — must be the
specific catch the test uses). The 84-vs-5 is the strongest lead but UNCONFIRMED until landmark-aligned.
Floor byte-identical.

## Implicit Loop3 $4000 reads ISOLATED (2026-06-07) — catch mechanism observable; correlation pending
Moved the R4000 caller-gate (bus.rs:2551) to the Implicit Loop3 window (bus.cycle 48.955-49.0M, just
before the $0478 result write at frame 1645). RESULT: 9× $4000->$00 catches + 1200× $40 in Loop3.
Each $00 is a SINGLE read (surrounded by $40) = the per-iteration CalculateDMADuration catch — so the
catch mechanism is directly observable in the correct region (the 84-vs-5 earlier was the wrong/early
window). NEXT (the confirmation + fix, RustyNES-only, no TriCNES alignment needed): correlate each $00
catch to its X iteration (the test writes $540[X] per CalculateDMADuration; X=10,11 are the failing
ones) + count the $40 reads BEFORE each $00 (= the Y value: Y=0 means the FIRST slide read $00 =
premature catch). If X=10,11's catch is Y=0 (premature $00) where Key3 expects Y=4, the bug is RustyNES
driving/retaining $00 on the external $4000 open-bus at the wrong slide — fix the specific DMC-GET
open-bus persistence (NOT the blanket reload-obus defer, which regressed DMC Bus Conflicts). The
caller-gate + R4000_LOG (last-N or wider ring to avoid the first-4096 fill) is the harness for this.
Floor byte-identical; the catch is now in scope, the correlation is the remaining decode step.

## TRACE-CONFIRMED ROOT CAUSE (2026-06-07) — the complete mechanism, finally pinned
trace_dma_4015 around the FIRST $4000=$00 CPU read (cyc 48631356-359):
  48631356  r $4000=$40  in_dmc=1   (DMA halt re-reads $4000)
  48631357  r $4000=$40  in_dmc=1   (DMA halt)
  48631358  r $FFC0=$00            (DMC GET fetches the $00 sample → drives open_bus=$00)
  48631359  R $4000=$00            (resumed CPU LDA $4000 reads open_bus=$00 = THE CATCH, Y=0)
MECHANISM (complete): the catch is the DMC GET's $00 driven onto open_bus and read by the CPU's
IMMEDIATELY-resumed $4000 read (the held address). At X=10,11 the reload DMA COINCIDES with the
measuring LDA $4000 (→ Y=0); at X=12-15 it lands later (→ Y=4). The GET driving open_bus is CORRECT
(reference does the same). The reload PERIOD is exact (576). So the X=10,11 coincidence is the
CUMULATIVE DMA-INSERTION TIMING: the slide code reaches its LDA $4000 a few cycles off relative to the
periodic DMA, because the cumulative count of prior DMA insertions across the 16 iterations drifts.
This is the cumulative-count exactness (M-2) — the SAME axis whose complete fix (the master-clock
collapse) regresses C1 (RW-2: 92→89%). The (in_dmc=0 at the GET row is an end-of-cycle LOGGING
artifact, not a bug; in_dmc is cleared at cycle end after the GET).
CONCLUSION (trace-grounded, final for this session): the abort tests fail because the cumulative
DMA-insertion timing makes the reload GET coincide with the measuring read at X=10,11. The reload, the
span, the open-bus drive, and the abort logic are ALL correct/exact. The ONLY fix is making the
cumulative DMA-insertion count exact WITHOUT shifting the C1 φ2 IRQ-sample dot — the C1-safe master-
clock collapse, the genuine multi-session structural work. No surgical lever exists (all measured-out).
Floor byte-identical 93.53%/9.
