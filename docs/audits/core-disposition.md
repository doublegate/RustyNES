# Core audit — disposition ledger

Report: [`core-audit-report.md`](core-audit-report.md). Verdict vocabulary and
the closing procedure: [`README.md`](README.md). Target release per
[ADR 0041](../adr/0041-hardware-release-is-v3.0.0.md).

**Stale facts in the report:** AccuracyCoin is 144/144, not "141/141" (§1.3). §5.1's premise that "the host frontend persists battery-backed save data to `.sav` files" is **false for the desktop and mobile frontends**, which never read `sram()` (frontend ledger FE-01); the fixes below matter for the libretro core, which hands `sram()` to RetroArch as its `.srm`.
`GEMINI.md` is a symlink to `AGENTS.md`, so the report's citations of it are
citations of `AGENTS.md`.

| Id | Finding (report §) | Verdict | Evidence | Release | PR |
| --- | --- | --- | --- | --- | --- |
| IMP-01 | PPU snapshot `spr_count` unbounded (§2.2) | FIXED | `PpuSnapshotError::InvalidSprCount` rejects `> 8`. Red-first: the v2.7.0 fuzz target crashed the unfixed tree at `ppu.rs:5104` ("len is 8 but the index is 8") in 29,326 runs. Test `a_corrupt_sprite_count_is_rejected_not_indexed`; mutation CAUGHT | v2.7.0 | |
| IMP-02 | APU channel state + BlipBuf unchecked (§2.3, §2.4) | FIXED | `ApuSnapshotError::FieldOutOfRange` bounds 12 register-width fields (duty, step, the three sweep fields, envelope volume/divider/decay, triangle step, DMC rate/bit count/DAC); `InvalidResampler` rejects a zero sample rate, a non-finite or non-positive CPU rate, a rate ratio above one output per CPU cycle (a finite huge ratio hangs exactly like an infinite one), a phase outside `[0, 1)`, and non-finite filter/held values. Tests `every_register_width_field_is_bounded_on_restore`, `resampler_fields_that_would_hang_or_poison_audio_are_rejected`; 18 mutations, all CAUGHT. Fuzz evidence arrived late: the first patch mode could not reach the APU section (the 245,760-byte framebuffer sits in front of it and offsets were 16-bit; review finding on #546). With offsets that skip the framebuffer, the unfixed tree crashed at `triangle.rs:140` ("len is 32 but the index is 131") in 1,193 runs | v2.7.0 | |
| IMP-03 | `SectionIter` unchecked `body_start + len` (§2.5) | FIXED | `checked_add`. The overflow is reachable only where `usize` is 32 bits, so no native test can make it fail; the `thumbv7em` build compiles it but does not execute it. Same change fuses the iterator: a malformed section used to return the same error on every `next()`, an infinite iterator for any caller that skips errors. Test `section_iter_stops_after_a_malformed_section`; both fuse sites mutated, CAUGHT | v2.7.0 | |
| §2.1 | `unsafe_code = "warn"`; add `#![forbid(unsafe_code)]` to the chip crates | FIXED | `#![forbid(unsafe_code)]` in `rustynes-{cpu,ppu,apu,mappers,core}/src/lib.rs`; none of the five contained `unsafe`, so this makes an observation a guarantee | v2.7.0 | |
| IMP-04 | Branchless `set_nz` / `adc` / `cmp_with` (§3.2) | REJECTED (measured) | Branchless `set_nz`: in the v2.7.5 combined ceiling probe, zero; and because a combined bound can hide an offset (review on #553), also run ALONE, twice: the candidate tracked its A/B/A control both times (run 2: +0.09 / +0.24 / +0.26 / +0.10% against control −0.03 / +0.11 / +0.24 / +0.07%). See `docs/performance.md` §v2.7.5 | v2.7.5 | |
| IMP-05 | `#[inline]` + cold-path outlining in `Cpu` (§3.3) | REJECTED (measured) | The premise holds (`read1`, `write1`, `end_cycle`, `idle_tick`, `adc` survive as out-of-line symbols, unlike v2.3.1 G7's `bus.rs` targets), so it got its own candidate: DMA / abort drains outlined `#[cold] #[inline(never)]`, `read1` hinted `#[inline]`. The outlining took; two clean runs (one more voided by load) read the candidate as its own control: zero. See `docs/performance.md` §v2.7.5 | v2.7.5 | |
| IMP-06 | PPU fast-path redundant stores, phase match, branchless palette (§3.4) | REJECTED (measured) / NOT PURSUED (phase match) | The four redundant stores: in the combined ceiling, zero. The branchless palette mirror: its own ceiling (mirroring deleted, 61,440 lookups a frame), zero on two runs. The phase `match` fold has no ceiling and restructures the same function whose store elision measured zero, so it was not pursued. See `docs/performance.md` §v2.7.5. v2.7.6: the four stores measured ALONE, two runs: zero, byte-identical; three of them rewrote fields the fast-path guard already requires to be `true`, so they are replaced by a `debug_assert!` of that invariant (pinned by `fast_render_path_asserts_its_rendering_history`), and `bg_reload_render` keeps its store. | v2.7.5 | |
| IMP-07 | BlipBuf `drain_all` capacity churn + unbounded growth (§3.5) | ADOPTED (capacity) / REJECTED (cap) | Capacity: `drain_all` now replaces the buffer with one of the same capacity. First credited to the combined ceiling's zero, which was wrong: the stock `full_frame` benches restore a snapshot per iteration and never drain audio, so `drain_all` never ran in them (found on #553). Measured on a frame-then-drain bench as an A/B/A: **−0.89% on two runs** (3.924 / 3.916 ms against 3.958 / 3.952 ms; controls −0.52% / +0.22%). Adopted under `ab_check.sh`'s evidence rule; test `drain_all_keeps_room_for_the_next_frame`, red first, mutation caught. Helps `Nes::drain_audio` callers (mobile, wasm) only; audio byte-identical. The audit's "eliminates per-frame allocations" premise is false (a `Vec` returned by value needs a new buffer each call). The 16,384-sample cap: rejected as a behaviour change with no user. See `docs/performance.md` §v2.7.5 | v2.7.5 | |
| §3.1 | Scheduler hotspots A/B/C (divider polling, DMA drain store, per-dot NMI edge sampling) | REJECTED (measured) | All three hotspots in the combined ceiling (divider hard-coded to 12, `take_dma_mc_consumed` removed, `sample_nmi_edge` removed from all three catch-up loops): zero. Premises partly wrong: `div / 2` is a shift, not a division; B's accumulator is zero in production because the core overrides `cpu_clock`, not for the reason given. See `docs/performance.md` §v2.7.5. v2.7.6: A, B and C each measured ALONE, two runs: all zero, all byte-identical. C's `sample_nmi_edge` feeds only `nmi_edge_latch`, read only by the deprecated `poll_nmi`; B checks an accumulator only a unit-test path feeds. Removal of that pre-v2.0.0 machinery goes with the v2.9.0 deprecation decision. | v2.7.5 | |
| §3.5b | Pulse sweep-mute caching | REJECTED (measured) | The mute check deleted from `Pulse::output` in the combined ceiling: zero. See `docs/performance.md` §v2.7.5. v2.7.6: measured ALONE, two runs: zero. | v2.7.5 | |
| §3.6 | `cpu_read_unmapped` virtual call on every cart read | REJECTED (measured) | The `cpu_read_unmapped` call skipped for every cartridge read in the combined ceiling: zero. See `docs/performance.md` §v2.7.5. v2.7.6: measured ALONE, two runs: zero. | v2.7.5 | |
| §4.2 | `Bus`/`CpuBus` naming drift, `LockstepBus` name | FIXED (prose) / DECLINED (alias) | CONFIRMED: `rustynes-core` has no type named `Bus`, `Cpu` has no `tick()`, and `AGENTS.md` described the pre-v2.0.0 design (it also still said the scheduler advances by `tick_one_dot()`, which no longer exists). Fixed by correcting `AGENTS.md` and `docs/scheduler.md`. The suggested `pub type Bus = LockstepBus` alias is declined: a second name for one type is the drift being reported, and renaming `LockstepBus` is an API break for v2.9.0 to weigh | v2.7.5 | |
| §4.3 | Orphaned `ApuBus` trait | FIXED | CONFIRMED: nothing implements or calls `ApuBus`; the bus drives the DMC fetch through `Apu::dmc_dma_pending` / `dmc_dma_addr` / `complete_dmc_dma` (`bus.rs` 3685-3693, 3832-3836). `#[deprecated(since = "2.7.5")]`; the re-export stays under `#[allow(deprecated)]` so the path works until v2.9.0. `docs/scheduler.md` described it as live and is corrected | v2.7.5 | |
| §4.4 | 22 dead methods on `rustynes_cpu::Bus` | FIXED, AND THE LIST WAS WRONG | Checked by the compiler, not by grep: every trait method temporarily `#[deprecated]`, then the workspace and every CI feature set checked. **18 are dead; 5 of the audit's 22 are live**: `cpu_read` / `cpu_write` (the trait's default `read` / `write` and the core's own calls), `on_cpu_cycle` (the default `cpu_clock` a stub bus uses), `dmc_dma_defer_load_entry` and `oam_dma_overlap_cycle` (the core calls both). The audit also missed `internal_data_bus` (dead) and `trace_instr` (live under `cpu-instr-cycle-trace`). (The report's own §1.4 scorecard says 18.) The 18 carry `#[deprecated(since = "2.7.5")]`; `-D warnings` across the workspace is now the proof that nothing calls them | v2.7.5 | |
| §4.5 | CPU open-bus has no decay; Sachen 150/243 `$4101` wipes open-bus high bits | FIXED (Sachen) / NOT A DEFECT (decay) | **Sachen: CONFIRMED** from `nesdev_wiki/INES_Mapper_150.xhtml` (the data register drives D2-D0; D7-D3 float). Fixed with an additive `Mapper::cpu_read_driven_mask` (default `0xFF`), which the bus merges with its latch for `$4020-$5FFF` reads only, keeping PRG fetches free of it. Sachen TCA-01 (143) uses it too: its protection read drives D5-D0, where the old code approximated the top two bits from the address. Bus test `a_sachen_register_read_keeps_the_floating_high_bits`; mutation (drop the merge) CAUGHT. **Decay: NOT A DEFECT**: `nesdev_wiki/Open_bus_behavior.xhtml` describes the CPU data bus as a dynamic latch whose trace capacitance lets it "float in place", and documents no CPU-side decay (decay is documented for the PPU's I/O latch, which is modelled) | v2.7.2 | |
| IMP-12 | `PpuBusAdapter` built per dot in `run_ppu_to` (§4.6) | REJECTED (already measured) | This is v2.3.1's G10, measured and rejected then: no `PpuBusAdapter` symbol survives codegen, and the hoist conflicts with the `self.mapper` borrow. `sample_nmi_edge` (the other half) was deleted in the v2.7.5 combined ceiling: zero. See `docs/performance.md` §v2.7.5 | v2.7.5 | |
| §4.7 | Doc drift: zapper temporal-light default, FDS error text, CPU header | FIXED | All three CONFIRMED: `zapper_temporal_light` documented as default off (it is on since v2.3.6) with a link to the wrong type; `RomError::FdsUnsupported` still said FDS was "planned for v2.2.0" (it shipped in v2.2.0; the error now says the image needs the disk loader and a BIOS, which is what a mobile user opening a `.fds` needs to read); the `cpu.rs` header described the retired `on_cpu_cycle` stepping interface | v2.7.5 | |
| IMP-08 | Bandai FCG EEPROM not exposed via `sram()` (§5.1) | FIXED | CONFIRMED red-first: `crates/rustynes-mappers/tests/battery_sram_exposed.rs` `bandai_fcg_eeprom_is_the_battery_save` failed (0 bytes) on v2.7.0. `sram()`/`sram_mut()` now return the 24C01/24C02 array (128/256 B) for mappers 16 and 159; `&[]` when the board has none. Mutation (drop the override) CAUGHT | v2.7.1 | |
| IMP-09 | Taito X1-005 128-byte RAM not exposed (§5.1) | FIXED | CONFIRMED red-first by `taito_x1_005_ram_is_the_battery_save`, which unlocks the RAM the way a game does (`$A3` to BOTH `$7EF8` and `$7EF9`) and checks both directions: CPU writes reach `sram()`, and a restored `sram_mut()` is what `$7F05` reads. Mutation CAUGHT | v2.7.1 | |
| IMP-10 | TxSROM / TQROM do not forward `sram()` to `inner` (§5.1) | FIXED | CONFIRMED red-first by the generic loop in `crates/rustynes-mappers/tests/battery_sram_exposed.rs` (mappers 118, 119). Both now forward to the wrapped MMC3. Mutations CAUGHT | v2.7.1 | |
| §5.1e | Multicart 15 PRG-RAM not exposed | FIXED | CONFIRMED red-first by the generic loop, but only once it tried a CHR-RAM image: Multicart 15 refuses CHR-ROM, so a CHR-ROM-only loop never constructed it and silently passed. The loop now builds every mapper both ways. Mutation CAUGHT. Note: the CHANGELOG's "battery saves" entry (v2.6.21) concerned the MiSTer RTL, not these mappers | v2.7.1 | |
| IMP-11 | Namco 163 `$C000-$DFFF` nametable banking (§5.2) | FIXED | CONFIRMED, and wider: `$E800` bits 6-7 (CIRAM as CHR-RAM for pages `$E0-$FF`) were masked off too, and `nta` was serialised but never read. Fixed from `nesdev_wiki/INES_Mapper_019.xhtml`. Three unit tests red on `main` (quadrant CIRAM selection, CHR-ROM page as a read-only nametable, CIRAM as CHR-RAM with the `$E800` disables); three compatibility controls pass before and after (power-on layout = header mirroring, `$E800`'s PRG bits undisturbed, a v2 save state loads with CHR-RAM disabled). **Correction (review on #550):** those unit tests called `ppu_read($2000)` directly, a seam the PPU never uses. It fetches nametables through `nametable_fetch` / `nametable_write` / `nametable_address`, which N163 did not override, so in the emulator only the standard layouts worked. The hooks now route CHR-ROM pages and the selected CIRAM page, and the board's CIRAM copy is authoritative so CIRAM-as-CHR sees nametable writes. Three `rustynes-core` tests drive `$2006/$2007` through the PPU and are red on the first cut. Old blobs hold `nta` zeroed, which now means CHR-ROM page 0, so the restore rebuilds it from the header (mutation CAUGHT) | v2.7.2 | |
| §5.3 | MMC5 multi-bank PRG-RAM ignored | FIXED | CONFIRMED (`$5113` stored, never read; RAM-mode `$5114-$5116` pages ignored). Fixed from `nesdev_wiki/MMC5.xhtml`: the RAM is paged by the bank value's low three bits over the wiki's **64 KiB compatible superset**; a 16 KiB window takes A13 from the CPU; `sram()` is the header's declared battery part (ETROM's 16 KiB saves only the first chip). **Correction within v2.7.2:** the first version implemented the wiki's exact per-board table (chip select, open bus where no chip answers). The commercial oracle caught it: *L'Empereur* is ETROM per the wiki's board table, but its NES 2.0 dump declares only the 8 KiB battery chip, so its work-RAM chip floated and the game stopped at its logo. The superset is what the wiki recommends for exactly this unreliability; the superset pages live in a separate block saved as a v5 tail, so older save states load. Tests: every declared size pages all eight values; the under-declared ETROM case; the save size per declared layout; the superset round-trips a save state and a v4 blob loads | v2.7.2 | |
| §5.4 | MMC1 SUROM/SXROM 512 KiB PRG | FIXED | CONFIRMED, and wider than reported: besides the 4-bit PRG mask (`m001_mmc1.rs:168`, whose comment claimed all 5 bits), SOROM/SXROM PRG-RAM banking was missing entirely, and SNROM's bit-4 RAM enable was applied to SUROM, where that line is PRG A18. Fixed from `nesdev_wiki/MMC1.xhtml`. Red-first: holy_mapperel `M1_P512K_CR8K_S8K` / `_S32K`, built from the v0.02 tag, rendered `S*ROM`, `PRG RAM MISSING`, `0300` on `main` and render `SUROM` / `SXROM`, `0000` now; six unit tests, five red on `main`. Mutation (no outer bank) CAUGHT by the ROM net | v2.7.2 | |
| §5.5 | `$6000-$7FFF` returns 0 instead of open bus on discrete boards | FIXED | CONFIRMED and far wider than the four boards named: a sweep over every NES 2.0 mapper number (`tests/prg_ram_window_open_bus.rs`) found **205** board variants reading a made-up `$00` there with no RAM. Fixed at the source: `Mapper::cpu_read_unmapped`'s default now floats `$6000-$7FFF` exactly when `sram()` is empty, which is sound because v2.7.1 made every save-RAM board expose `sram()`. Nineteen overrides that ignored the window took the same rule; seven boards that drive ROM or registers there (40, 42, 50, 212, 238, 305, 306) keep it mapped, found by the test's converse check ("reported unmapped while driving data"). Found on the way: F-08, F-09. The converse check builds boards from iNES headers only; the two with non-save RAM built another way, Vs. DualSystem shared RAM and the NSF player's WRAM, were first floated by the rule and are pinned mapped (`vs_dualsystem_synth`, `nsf::tests::wram_window_is_mapped_and_holds_data`; the NSF case found by review on #550). Mutation (revert the default) CAUGHT | v2.7.2 | |
| §5.6 | `has_hardwired_mirroring` missing on 11+ fixed-mirroring boards | FIXED (10) / REFUTED (2) | Verified per board against its wiki page (`tests/hardwired_mirroring.rs` records the evidence): now `true` on 11, 13, 34, 38, 79, 87, 94, 180, 184, and 70 unless its header says four-screen (the pre-152 encoding of the 1-screen variant). **REFUTED** for 71 (the model decodes Fire Hawk's single-screen bit on every cart) and 152 (a mirroring register); 86, 101 and 140 keep the safe default because their wiki pages state no mirroring. Mutations (revert 87; drop the 70 four-screen exception) CAUGHT | v2.7.2 | |
| §6.2 | "Sanitize" Mesen2/puNES citations in `m085_vrc7.rs`, `m099_vs_system.rs`, `m244_cne_decathlon.rs` | REJECTED FIX | The comments quote Mesen2 **source expressions** (`m085_vrc7.rs:362,947`; `m099_vs_system.rs:135,160-161`; `m244_cne_decathlon.rs:115`), so they are derivation statements. Removing them would be laundering (AGENTS.md NEVER LAUNDER). **Done instead (v2.7.1):** an SPDX + `// Provenance:` header on each file, three §1 rows in `docs/originality-and-provenance.md`, and the `NOTICE` lists; the in-body comments are unchanged. `provenance_record_audit.rs` now fails if a header and its §1 row disagree. **Awaiting maintainer review before merge** — classification is a human judgement, not a tooling result | v2.7.1 | |
| §6.3 | Derived-component attributions complete | NOT A DEFECT | Informational, but the regeneration command it relied on was wrong: `crates/*/src/*.rs` does not descend, and missed `debugger/source_map.rs` and `bin/pgo_trainer.rs` (both recorded in §1). The correct command is `grep -rln "^// Provenance:" crates` (26 files at v2.7.1); `provenance_record_audit.rs` walks the tree | — | |

## Found during triage (not in the report)

| Id | Finding | Verdict | Evidence | Release | PR |
| --- | --- | --- | --- | --- | --- |
| T-01 | Pulse-1 sweep with negate and shift 0 (the `$4001=$08` idiom) computes `per - per - 1 = 0xFFFF` and mutes | FIXED | CONFIRMED by `pulse1_negate_shift0_clamps_to_zero_and_does_not_mute`, red on v2.6.23. Fix: a negated target saturates at zero (NESdev "APU Sweep"). `pulse1_negate_clamp_is_inert_for_nonzero_shift` shows the clamp changes nothing for shift 1-7 over periods 8-`$7FF`. Full `--features test-roms` suite with this fix alone: 2,612 passed, 0 failed, no golden moved (2,622 on the final release tree, which adds the later tests). The v2.8.2 co-sim gate still follows | v2.7.0 | |
| F-01 | Restored `dma_mc_consumed` trips `Cpu::end_cycle`'s structural-zero `debug_assert_eq!` (not in the report) | FIXED | Found by the v2.7.0 fuzz target in 6,848 runs. Dev/test/fuzz builds only: release drains and discards the value, so it is now discarded on restore (bytes still consumed). Test `a_restored_dma_mc_consumed_is_discarded_not_loaded`; mutation CAUGHT | v2.7.0 | |
| F-02 | Restored OAM-DMA byte index `uni_oam_addr` unbounded (not in the report) | FIXED | Found by the fuzz target in 287,867 runs (`bus.rs:3871`, add overflow). An active transfer at 256 or above never completes and overflows the `u16`. Bounds: `<= 255` while active, `<= 256` otherwise. Test `an_out_of_range_oam_dma_index_is_rejected`; both halves mutated, CAUGHT | v2.7.0 | |
| F-03 | Restored PPU raster position unbounded (not in the report) | FIXED | Found by the fuzz target once its base machine had a live APU, in 76,381 runs (`ppu.rs:6078`, `dot += 1` overflow). The per-dot advance wraps only at dot 340 and the pre-render line, so a position past either never produces a frame. `PpuSnapshotError::InvalidRasterPosition`: dot `<= 340`, scanline `-1..=` pre-render line (-1 is the power-on position, which an existing round-trip test caught when the first draft excluded it). Test `an_out_of_range_raster_position_is_rejected`; 4 mutations, all CAUGHT | v2.7.0 | |
| F-04 | Restored PPU fine X and ten sprite-evaluation / OAM-bus counters unbounded (not in the report) | FIXED | The fuzz target found fine X (`ppu.rs:4788`, `0x8000 >> x` shift overflow) in 92,496 runs; the rest were then swept by reading every field `restore` loads rather than left to the fuzzer one run at a time. `PpuSnapshotError::FieldOutOfRange` bounds `x` 7, sprite-eval `n` 63 / `m` 3 / `found` 8 / `sec_idx` 32, OAM-bus address 63 / 3, secondary address 32, overflow counter 3, `oam2_addr` 31, corruption index 32 -- each the range the running PPU keeps it in. One existing round-trip test set the overflow counter to 5, a marker value the PPU cannot reach (it loads 3 and counts down); changed to 2. Test `every_ppu_counter_and_index_is_bounded_on_restore`; 11 mutations, all CAUGHT | v2.7.0 | |
| F-05 | Restored CPU `master_clock` / bus `ppu_clock` pair unchecked across sections (not in the report) | FIXED | Found as a libFuzzer TIMEOUT once patch offsets could reach past the framebuffer: `run_ppu_to` (`bus.rs`) ticks the PPU until `ppu_clock` meets `master_clock`, so a pair far apart ran billions of dots or none (no frame ever completes). The machine keeps them within a CPU cycle (measured: 4 master clocks after a frame); `LockstepBus::check_restored_clocks` rejects more than 1,024, called once both sections are decoded. Test `a_restored_cpu_ppu_clock_skew_is_rejected`; 3 mutations (call removed, bound disabled, off by one), all CAUGHT. The test exercises the "ahead" direction to the bound; `master_clock` is small after a frame, so "behind" is exercised only as far as zero | v2.7.0 | |
| F-06 | Restored `extra_lines_remaining` unbounded under a live overclock knob (review finding on #546, Copilot) | FIXED | CONFIRMED: a `u16::MAX` countdown idles ~65,535 scanlines before the frame resumes -- a stall, not a hang. Clamped (not rejected) to the knob in force, since the knob is not serialized and a larger one may have written a genuine file; at knob 0 the branch is unreachable and the value inert. Test `a_restored_extra_lines_countdown_is_clamped_to_the_live_knob`; mutation CAUGHT | v2.7.0 | |
| F-07 | BMC-FK23C (mapper 176) 32 KiB WRAM not exposed via `sram()` (not in the report) | FIXED | Found by the generic loop in `crates/rustynes-mappers/tests/battery_sram_exposed.rs`, which does not trust a list of boards: FK23C holds battery WRAM at `$6000-$7FFF` and returned `&[]`. Reached by mapper 176 and by mapper 30 with CHR-ROM, which routes to 176 submapper 2. Mutation CAUGHT | v2.7.1 | |
| F-08 | Kaiser KS202 (mapper 56) `$6000-$7FFF` work RAM was read-only (not in the report) | FIXED | Found by `prg_ram_window_open_bus.rs`: the read path served `wram`, but no write path existed, so the game's work RAM stayed zero. `nesdev_wiki/INES_Mapper_056`: "8 KB PRG RAM bank (not battery backed!)". Write path added; `sram()` stays empty by design (no battery), recorded in the battery sweep's exceptions. Mutation CAUGHT | v2.7.2 | |
| F-09 | Kaiser KS7032 (mapper 142) `$6000-$7FFF` modelled as RAM-or-ROM on register 4 bit 2 (not in the report) | OPEN | `nesdev_wiki/INES_Mapper_142` gives that window an 8 KiB switchable PRG-ROM bank selected by register 4. The model reads a zero work RAM there until register 4 bit 2 selects ROM. A modelling divergence rather than open bus; left for a release that can check it against a game. Named in `prg_ram_window_open_bus.rs`'s allow-list so it cannot pass silently | TBD | |
| F-10 | Five boards' documented `$6000-$7FFF` RAM missing from the model (not in the report) | FIXED | Exposed by comparing the commercial-ROM suite on `main` and on the §5.5 change: games on mappers 156, 177, 227, 241 and 245 changed behaviour, and all five wiki pages document RAM there that the model never had (writes vanished; reads were a made-up `$00`, then open bus). Added, 8 KiB each: 156 ("8 KiB RAM"), 177 and 245 (battery-backed, always the save), 241 ("can be battery-backed", the save when the header says so), 227 (only the battery-backed FW-01 variant has it, so it exists only with a battery header). Each saves its RAM as an appended block; a pre-v2.7.2 blob loads with it zeroed (`tests/documented_wram.rs`). The battery sweep's reach rose from 43 to 51 images. **Not done:** the other MMC3 clones whose boards document WRAM (mapper 52's 7-in-1) put board registers in the same window; that overlap needs its own decode | v2.7.2 | |
