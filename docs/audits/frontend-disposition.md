# Frontend audit — disposition ledger

Report: [`frontend-audit-report.md`](frontend-audit-report.md). Verdict vocabulary and
the closing procedure: [`README.md`](README.md). Target release per
[ADR 0041](../adr/0041-hardware-release-is-v3.0.0.md).

Rows are generated from the report's §8 table, so the ids and severities are the
report's own. The report itself retracted a 49th candidate, CON-02, as a false positive.

**Mobile scope (maintainer decision, 2026-09-22):** all tiers are implemented --
the Rust bridge, Kotlin and Swift. What CI and an Android emulator can reach is
verified here; the maintainer runs a per-platform device checklist before the
v2.7.4 PR merges, and anything left unverified is labelled so in the release notes.

| Id | Severity | Finding | Verdict | Evidence | Release | PR |
| --- | --- | --- | --- | --- | --- | --- |
| DESK-01 | High | Egui `textures_delta` dropped on swapchain acquire failure (`Lost`/`Outdated`/`Timeout`), permanently corrupting font atlas and leaking GPU texture memory. | FIXED | Main window: `paint_shell` (uploads `textures_delta.set` at `debugger/mod.rs:3175`) runs only inside the overlay closure (`app.rs:10057`), after `get_current_texture` succeeds (`gfx.rs:1242`); `detached.rs:355-363` uploads first on purpose. **Fixed in v2.7.3:** the overlay-visible main-window path now calls `DebuggerOverlay::upload_shell_textures` before `Gfx` acquires the swapchain, and `free_shell_textures` after the render whatever it returned, the same order as `detached.rs`. The hidden-overlay path (`render_shell`) was not affected: it runs the whole egui pass inside the paint closure, and egui keeps undelivered texture updates until a frame actually runs. **Not under automated test:** the renderer needs a wgpu device and the failure needs a real surface; verified by reading the path against `detached.rs`, and by clippy on every feature combination | v2.7.3 | |
| DESK-02 | Medium | Keycode collision: Famicom microphone (`MICROPHONE_KEY = KeyCode::KeyM`) and menu bar toggle (`default_toggle_menu_bar = "KeyM"`) share the same key. | FIXED | Worse than stated: `input.rs:734` `MICROPHONE_KEY = KeyM`, `config.rs:692-693` menu-bar toggle `KeyM`, **and** `config.rs:496` P2 Select `KeyM` -- three defaults on one key; the comment at `input.rs:730` claiming it avoids controller keys is false **Correction:** the third `KeyM` is **P3** Select (`default_player3`), not P2. Enumerating every default binding found a fourth clash: P2 Select and P3 Right both `KeyL`. **Fixed in v2.7.3:** microphone `M` -> `N`, P3 Select `M` -> `Comma`, P2 Select `L` -> `R`; the menu-bar toggle keeps `M`. `no_two_default_bindings_share_a_key` serialises P1-P4 and the system bindings (so later additions are covered) plus the microphone key; red first with exactly the three clashes. A saved config keeps its keys, so an upgraded user with Four Score and a saved P2 `L` keeps that overlap until rebinding. Also fixed: the README said save-state slots 0-9 were on number keys, but slots are 0-7 and chosen from the menu | v2.7.3 | |
| DESK-03 | Medium | In fast-forward mode, `proxy.send_event(AppEvent::EmuFrame)` floods Winit event loop at thousands of FPS, causing high `self.emu.lock()` contention. | FIXED | CONFIRMED by reading: in fast-forward the emu thread posted `AppEvent::EmuFrame` for every produced frame (`emu_thread.rs` send site), with no coalescing. **Fixed in v2.7.3, fast-forward only:** `EmuControl::should_signal_frame` keeps at most one wakeup pending while fast-forwarding, and `on_emu_frame` clears it first thing. At normal speed every frame still gets its wakeup, because the winit thread drives `RetroAchievements` once per wakeup and RA wants one `do_frame` per frame. Under the old flood the handler read the core as it was when the handler ran, so RA was not frame-exact during fast-forward before this either. `fast_forward_keeps_at_most_one_wakeup_pending`; the "always signal" mutation is CAUGHT. **PR #551 review:** coalescing still cut the number of RA `do_frame` calls under softcore fast-forward, which changes how hit counts advance, so a live RA session (`App.ra` present) turns it off and keeps the pre-v2.7.3 one-wakeup-per-frame behaviour; mutation CAUGHT. The throughput gain is not measured here | v2.7.3 | |
| DESK-04 | High | CPAL stream errors (device disconnect, route change) are ignored, permanently muting audio until application restart. | FIXED | CONFIRMED by reading: `build_stream`'s error callback only printed, and nothing checked stream health, so a fatal error left audio silent until restart (the producer kept pushing until the resync band skipped every batch). **Fixed in v2.7.3:** the callback sets a shared `failed` flag on `DeviceNotAvailable` / `HostUnavailable` / `StreamInvalidated` only (cpal 0.18 `ErrorKind`; `Xrun`, `DeviceChanged`, which is an automatic reroute, and `RealtimeDenied` do not end a stream). `AudioOutput::try_reopen` rebuilds the stream over the SAME `SampleQueue` (so the emu thread's `AudioProducer` needs no re-wiring), on the requested or else the default device, with the original config, retried at most every 2 s from `App::per_frame_host_io`. Tests pin the fatal-kind set and the retry pacing (both mutations CAUGHT). **PR #551 review:** the reopen kept the ORIGINAL device's channels and format on whatever device it found, so a replacement that could not take them failed every retry; `reopen_layout` now keeps the rate and takes the device's layout (three mutations CAUGHT). Stream builds passed `None`, an unbounded backend wait, now 2 s; running the reopen off the winit thread was not done, since a `cpal::Stream` is not `Send` on every backend. agy round 2: a repeated reopen failure is logged once per reason (`a_repeated_reopen_failure_is_logged_once`, mutation CAUGHT). **Not under automated test:** the reopen and the App hook need a real audio device | v2.7.3 | |
| DESK-05 | High | Low latency false underrun oscillation: setting `latency_ms = 20` when OS audio period is 1024 frames trips false underrun muting cycle. | FIXED | **As stated, REFUTED; a real narrower form, FIXED.** A 48 kHz timeline simulation (the real `SampleQueue` and `AudioProducer` with its DRC servo; 60 Hz pushes against device callbacks) measured the audit's case, a 960-sample target and a 1024-frame period, at **0** underruns in 5 s, with or without DRC. The gate holds consumption until more than a callback is buffered, so there is no perpetual oscillation. With DRC and a period of at least twice the target (960 / 2048), the servo held occupancy below one callback: **4** underruns in 5 s, a click about once a second. **Fixed in v2.7.3:** the queue records the largest callback request; the gate opens on at least 2x it, and producers servo and resync against `effective_latency = max(target, 2 x largest callback)`; the ring is at least 16,384. `the_drc_target_is_never_below_two_device_callbacks` (periods 2048 and 4096) fails first. Reverting both parts: CAUGHT. Either part alone suffices in this simulation, so single-part mutations are expected to be not caught; documented at `effective_latency` | v2.7.3 | |
| DESK-06 | Medium | Web canvas `#nes-canvas` has fixed 512x480 dimensions without responsive CSS, overflowing mobile screens. | FIXED | CONFIRMED by reading: `#nes-canvas` was a fixed 512 x 480 with nothing to shrink it. winit also writes the canvas size as INLINE styles (`with_inner_size`), which beat the stylesheet. **Fixed in v2.7.3:** `max-width: calc(100vw - 24px)` (applies against an inline width), `height: auto !important` and `aspect-ratio: 512 / 480`. **Verified by inspection and the wasm clippy / Pages build only; not yet seen on a narrow screen** | v2.7.3 | |
| DESK-07 | Low | Web Audio AudioWorklet Blob URL is never revoked, leaking memory in browser context. | FIXED | CONFIRMED by reading: `install_worklet` never revoked the object URL from `make_worklet_blob_url`. **Fixed in v2.7.3:** it is revoked once the `addModule` promise settles, success or failure (the module has been fetched by then). Wasm clippy for both web features; not observable from a test | v2.7.3 | |
| DESK-08 | Low | Unbounded texture cache accumulation in `BadgeCache` across games. | FIXED | CONFIRMED: nothing ever evicted a `BadgeCache` entry, so every badge texture of every game in a session stayed resident. At about 16 KiB a badge and roughly 100 a game this is a slow leak, which matches Low. **Fixed in v2.7.3:** `BadgeCache::clear()` drops ready and failed entries (dropping a `TextureHandle` frees the texture) and keeps in-flight fetches; it is called from `DebuggerOverlay::clear_rom_bound_analysis`, which runs on every ROM load and close. **PR #551 review:** keeping in-flight entries was itself a leak, since each became a texture for a game no longer loaded; `clear()` now drops everything and a result nobody requested again is discarded on arrival. `a_game_change_releases_every_badge_including_in_flight_ones`, red first; three mutations CAUGHT. The PPU-panel allocation churn in the same report section (a 245 KB `Vec` per frame from `nametable_rgba`) is a performance item for v2.7.5, where changes need a measured A/B | v2.7.3 | |
| AND-01 | High | Surface destruction race: 800ms join timeout causes zombie thread condition; subsequent `surfaceCreated` fails to start render thread, causing permanent black screen. | FIXED | PARTIAL: the black screen is real (a `surfaceDestroyed` join that times out leaves the old thread alive, and `surfaceCreated` refused to start another), the use-after-free is not (`NativeWindow` holds its own reference), and the report's fix -- set the shared `running` flag regardless -- would have revived the old thread bound to the dead surface. **Fixed in v2.7.4:** a per-thread `Worker` token; a new surface always gets a new worker, which joins its predecessor before any native call; only the current worker may consume surface state (checked under the lock that retires the old one). Not under automated test (needs a device); on the maintainer's checklist **Review (#552):** the new worker was published before its thread was assigned, and `wake()` read the unassigned `lateinit` from the emulation thread; the thread is now created before publication | v2.7.4 | |
| AND-02 | High | 60 FPS Compose recomposition storm: updating `frame = reuse.asImageBitmap()` forces full-tree recomposition of `EmulatorScreen` and redundant CPU pixel packing. | FIXED | CONFIRMED on mechanism: the per-frame `frame` read sat in an inline Row/Column/Box chain, so every frame re-ran all of `EmulatorScreen` (on the DEFAULT Bitmap path; the GPU path is opt-in). **Fixed in v2.7.4:** the picture reads it inside `FrameScope`, its own restart scope, and the idle test is a `derivedStateOf` that changes only on open / close. **Deferred:** skipping the per-frame bitmap pack on the GPU path; screenshots, clips and casting read that bitmap, so it needs an on-demand capture path first. Recomposition counts not measured here (device checklist) | v2.7.4 | |
| AND-03 | High | Background emulation and audio leak: `onPause` does not pause `emulator.paused`, coroutine runs at 100% CPU, audio continues playing, and zero AudioFocus handling exists. | FIXED | CONFIRMED: no `onStop`, no `AudioFocus` anywhere. **Fixed in v2.7.4:** emulation pauses in `onStop` and resumes in `onStart` only what it paused (keyed on STOP, not PAUSE, because picture-in-picture calls `onPause` without `onStop`); `AudioPlayer` requests AUDIOFOCUS_GAIN, drops samples while focus is lost, ducks to 0.2, pauses and abandons focus while emulation is paused, re-requests on resume. Not under automated test (device checklist) | v2.7.4 | |
| AND-04 | High | Synchronous main-thread disk I/O and SHA-256 computation in `loadRom` and save-state UI clicks risk ANRs. | FIXED | CONFIRMED, severity overstated (NES ROMs are small; the real ANR risk is a cloud-backed SAF provider blocking the read). **Fixed in v2.7.4:** `loadRom` split into `prepareRom` (read, hash, `.sav` and auto-state loads, grants, recents, library) on Dispatchers.IO and `publishRom` on Main, via one `openRom` for all four entry points; the save-slot sheet's reads and Save / Load / Delete run on IO. The background save in `onPause` stays synchronous on purpose. Exercised on the emulator (the boot-counter run under AND-09 loads through `prepareRom`) | v2.7.4 | |
| AND-05 | High | Non-atomic save writes using `File.writeBytes` risk file truncation and corruption on process kill. | FIXED | CONFIRMED, and wider: `RaProgressStore` and `RomLibrary` had the same truncating write, and `GameLibrary` / `GameConfig` (user data the report did not name) too. **Fixed in v2.7.4:** `writeAtomic` over `androidx.core.util.AtomicFile` for all of them. The app's first JVM tests: `an_interrupted_write_keeps_the_previous_contents` (and no side file left); mutation to a plain write CAUGHT | v2.7.4 | |
| AND-06 | High | Phantom AAudio sink: `Cargo.toml` claims an AAudio sink, but Rust code implements zero audio symbols. Kotlin uses blocking `AudioTrack.write` that conflicts with frame pacing. | FIXED | PARTIAL: the AAudio sink is documentation drift (manifest, module docs, `docs/android.md`, ADR 0024 all claimed it; audio was always Kotlin `AudioTrack`); the pacing "conflict" is a deliberate wall-clock floor, not a conflict. **Fixed in v2.7.4:** the claims corrected, ADR 0024 by dated amendment. No Oboe/AAudio port: nothing requires it | v2.7.4 | |
| AND-07 | Medium | High-frequency touch and analog motion events trigger unthrottled synchronous JNI calls and Rust mutex contention. | FIXED | Mostly REFUTED: touch and motion call in only on a mask CHANGE (`VirtualController.kt:121`, `GamepadManager.kt:462`). The residual -- each change blocked on the bridge mutex `run_frame` holds -- is MOB-01, fixed in the bridge in v2.7.4; no Kotlin change. Severity Low | v2.7.4 | |
| AND-08 | Medium | CPU-spinning `Thread.sleep(2)` in `NesSurfaceView` render loop generates up to 500 wakeups/sec when idle. | FIXED | CONFIRMED (a sleep-poll, not a spin; GPU path only, surface attached only). **Fixed in v2.7.4:** `LockSupport.park` with a one-frame safety timeout; every producer unparks. Not under automated test (device checklist) | v2.7.4 | |
| AND-09 | Medium | Missing standard cartridge battery PRG-RAM (`.sav`) persistence. | FIXED | CONFIRMED (the mobile half of FE-01; the report's `bus.rs:1186` citation is wrong). **Fixed in v2.7.4:** bridge API (MOB-05) + `Battery.kt` with the desktop's rules; loaded before the first frame, compared once a second (write on IO), written on close / dispose / background, and after the swap on a ROM switch. JVM tests pin the rules (5, mutations CAUGHT, one after a first NOT CAUGHT made the test observe which check refused). **Run on the Android emulator:** a boot-counter ROM (INC $6002 at reset) read 1, 2, 3 across three force-stopped launches -- the `.sav` loads before the first frame and persists **Review (#552):** a periodic flush read the RAM before the saver's lock and could write an older copy over the final save; the reader now runs under the lock (JVM test). Savers of one path now share a per-file lock (reopening the running game), and opening a ROM writes the running game's save first | v2.7.4 | |
| AND-10 | Low | Absence of `onTrimMemory` and `onLowMemory` callbacks increases LMK kill score. | WONT-FIX | Absence CONFIRMED, impact REFUTED: implementing `onTrimMemory` / `onLowMemory` does not change the low-memory killer's score, which depends on process state; and there is little to free (no mobile rewind buffer; scratch buffers ~250 KB) | v2.7.4 | |
| IOS-01 | High | CADisplayLink configured for 120 Hz instead of 60 Hz causes 3:2 pull-down micro-judder and doubles GPU/display power consumption. | FIXED | PARTIAL: the 120 Hz request is real; the report's judder mechanism is wrong (`link.timestamp` is vsync-quantised) and its fix (60 Hz) only halves how often the accumulator hitches (every ~10 s instead of ~5 s). **Fixed in v2.7.4:** 60 Hz exactly, and on a ~60 Hz link one console frame per vsync (display sync), the 0.16% slowdown absorbed by the new rate control (IOS-02); the accumulator remains for other refresh rates. Swift, not compiled here (device checklist) | v2.7.4 | |
| IOS-02 | High | Total absence of Dynamic Rate Control (DRC) causes systematic clock drift and buffer dropouts every 30-60 seconds. | FIXED | PARTIAL: no DRC is true; "quickly fills" and "dropouts every 30-60 s" are not (the pacing is wall-clock; drift is ppm-level). What was real: nothing steered the queue (up to a 250 ms cap), and playback started empty. **Fixed in v2.7.4:** host-tested `audio_ring.rs` -- a Hermite resampler steered by fill to a 50 ms target, a start threshold, re-arm after underrun. `rate_control_steers_the_ring_to_its_target`; four mutations CAUGHT **Review (#552):** moving `Ring` into a public module exposed `&self` push / pop on a `Sync` type, so safe code could race two producers (UB). `Ring` is private now; one non-`Clone` `RingTx` and one `RingRx`, both `&mut self`, are the only way in | v2.7.4 | |
| IOS-03 | Medium | Per-sample atomic load/store in real-time CoreAudio callback degrades thread determinism and burns mobile CPU cycles. | FIXED | CONFIRMED mechanically, severity overstated (nanoseconds per sample on ARM64). **Fixed in v2.7.4:** `Ring::pop_into`, one index load and store per chunk, into a buffer allocated once. A cost property: a mutation back is expected not caught, recorded at the site | v2.7.4 | |
| IOS-04 | High | CloudKit uploads and SRAM auto-save on scene backgrounding lack `beginBackgroundTask`, risking termination by iOS. | FIXED | PARTIAL: CloudKit uploads without a background task CONFIRMED; the "SRAM auto-save" half REFUTED -- iOS persisted no SRAM at all (MOB-05). Found alongside: reconcile marked a slot `.synced` when the local copy was newer and never uploaded. **Fixed in v2.7.4:** `BackgroundTask` (always ended, including from the expiration handler, since an unended task gets the app killed) around uploads and the background battery save; a newer local copy is re-uploaded. Swift, not compiled here | v2.7.4 | |
| IOS-05 | High | Virtual control pad positioned over Home Indicator zone without `preferredScreenEdgesDeferringSystemGestures` triggers accidental app minimization. | FIXED | PARTIAL: the deferral APIs were missing; the pad does NOT sit over the indicator (it respects the safe area). **Fixed in v2.7.4:** `.defersSystemGestures(on: .bottom)` + `.persistentSystemOverlays(.hidden)` (iOS 17 floor). Swift, not compiled here | v2.7.4 | |
| IOS-06 | Medium | `GCController` background thread invokes `@MainActor` state without actor isolation, causing data races. | REFUTED | `GCController.handlerQueue` defaults to main and was never changed, so there was no race. Hardening in v2.7.4: the queue is set to main explicitly, since the file's own comment assumed off-main delivery | v2.7.4 | |
| IOS-07 | High | Missing autorelease pool in CADisplayLink 60 Hz frame tick causes 14.7 MB/s memory churn and Jetsam risk. | REFUTED | The link runs on the main run loop, which drains its autorelease pool every iteration; the per-frame buffers are ARC `Data` and Rust `Vec`s, not autoreleased. "Jetsam from 14.7 MB/s" confuses allocation throughput with growth. No change | v2.7.4 | |
| IOS-08 | High | Audio stream death on interruptions (phone calls) and route changes; unhandled `mediaServicesWereResetNotification`. | FIXED | PARTIAL: interruption recovery REFUTED (cpal 0.18 and the app already handle it); media-services reset CONFIRMED (cpal reports `StreamInvalidated`, the app only logged it). Found alongside: an unplugged headset paused the game until a background/foreground cycle. **Fixed in v2.7.4:** the sink flags a fatal stream (`rustynes_ios_audio_is_invalid`); a reset re-configures the session and rebuilds the sink, as does a sink reporting itself dead; route loss is its own pause reason, cleared by the next menu close. A hardware rate change cannot be matched without a new core and is logged. Rust half type-checked on Linux; Swift not compiled here **Review (#552):** a rebuild that failed left `audio` nil and nothing retried it; a lost sink is now retried on the same 2 s throttle | v2.7.4 | |
| IOS-09 | Medium | Surround sound channels erroneously fed only Left channel audio instead of mono center average. | REFUTED | Unreachable: cpal 0.18 always negotiates stereo on iOS, and only the DSP-enabled path fanned out to extra channels. Fixed as hygiene in v2.7.4 (`fan_out` gives extra channels the centre; `surround_channels_get_the_centre`, mutation CAUGHT). The report's desktop citation (`:1057`) is wrong (`:1326`) | v2.7.4 | |
| IOS-10 | Medium | Fragile CloudKit conflict resolution based purely on wall-clock time is vulnerable to clock skew. | FIXED | CONFIRMED and broader: uploads saved with `.allKeys`, so an older save uploaded later replaced a newer one regardless of timestamps, and the per-record save result was discarded. **Fixed in v2.7.4:** fetch the server record, skip if newer, update it and save with `.ifServerRecordUnchanged`, check the per-record result. Swift, not compiled here | v2.7.4 | |
| IOS-11 | High | Complete absence of `ProcessInfo.thermalState` observers and Low Power Mode handling. | FIXED | Absence CONFIRMED, severity Low not High (the core is light; the expensive work is the optional filter passes). **Fixed in v2.7.4:** at `.serious` / `.critical` thermal state the video filter is suspended until the device cools. Swift, not compiled here **Review (#552):** a thermal change re-ran all display settings, re-reading the palette and HD pack from disk; it now applies the filter only | v2.7.4 | |
| MOB-01 | High | Monolithic `self.inner` mutex lock contention: `run_frame` holds mutex for entire frame execution, starving UI thread `set_buttons` calls and causing input lag / ANRs. | FIXED | CONFIRMED: masks inside the frame mutex, so input waited out a frame, a rollback or a script. **Fixed in v2.7.4:** `[AtomicU8; 4]` outside the lock, latched per frame. `input_does_not_wait_for_the_frame_lock` timed out on the old code; mutations CAUGHT; a re-latch after `load_state` measured redundant and removed | v2.7.4 | |
| MOB-02 | High | Raw uncompressed `.nes` ROM buffers bypass `MAX_ROM_BYTES` validation, allowing memory exhaustion and process termination. | FIXED | CONFIRMED (a VALID image padded past 16 MiB loaded; only zip entries were bounded). **Fixed in v2.7.4:** `check_rom_size` in `new` and `load_rom`, red first; mutations CAUGHT including the off-by-one | v2.7.4 | |
| MOB-03 | High | Missing `catch_unwind` on C-ABI and JNI platform shims causes fatal process aborts on any panic. | FIXED | CONFIRMED and WORSE than reported: the mobile libraries were built `panic = "abort"`, so every panic -- including in UniFFI's own wrapped methods -- killed the app. **Fixed in v2.7.4:** `[profile.release-mobile]` (unwind) for Gradle, `android.yml` and the xcframework script; all 6 JNI and 14 C-ABI entries wrapped. Cost measured: +18% native size on arm64, frame time +0.04-0.08% (noise). **Found in review (agy, #552):** unwinding alone did not keep the app open. UniFFI catches a panic, but `run_frame` is not a throwing method, so the generated Swift is `try! rustCall` (checked by generating the bindings) and the Android frame loop does not catch the `InternalException`; the frame paths (`run_frame`, `step_frame`, `np_advance_frame`) now contain panics inside the bridge (`contained_frame`, test `a_panic_mid_frame_freezes_the_game_instead_of_reaching_the_host`, six mutations CAUGHT after one stimulus fix). Other methods keep UniFFI's guard, so a panic there still reaches the host as an exception | v2.7.4 | |
| MOB-04 | High | Synchronous `worker.join()` in `Drop` blocks calling UI thread for 10-30s on unreachable networks. | FIXED | CONFIRMED at both sites. **Fixed in v2.7.4:** `join_or_detach` (100 ms grace). `dropping_the_client_does_not_wait_for_a_silent_relay`: 10.06 s on the old code, 0.30 s now | v2.7.4 | |
| MOB-05 | Medium | Complete absence of battery SRAM (`.sav`) export/import methods in mobile bridge. | FIXED | **Fixed in v2.7.4:** `has_battery` / `battery_ram` / `load_battery_ram` + `MobileError::Battery`; tests red as a compile error, four mutations CAUGHT; persisted by both hosts (AND-09; iOS `BatterySave.swift`) | v2.7.4 | |
| MOB-06 | Medium | Per-frame `Vec<u8>` allocation & UniFFI copy generates ~15 MB/s GC churn across FFI. | DEFERRED (blocked upstream) | CONFIRMED by arithmetic (245,760 bytes x 60 = 14.7 MB/s through UniFFI). A borrowed-framebuffer API is a performance change with a design cost, so it goes to v2.7.5 with the other performance items, where it needs a measured A/B like them **v2.7.5:** not changed. The natural fix is a caller-owned buffer the bridge fills each frame, which needs UniFFI's mutable borrowed bytes (`&mut [u8]`); that exists only on UniFFI's unreleased `main` (the newest release, 0.32.2, has read-only borrowed bytes only). A raw-pointer workaround would add unverifiable `unsafe`, and the cost itself (Android / iOS GC pressure) cannot be measured on this machine. Revisit when UniFFI releases it | v2.7.4 | |
| MOB-07 | Medium | Transparent mutex poison recovery ignores state corruption in netplay and emulation. | FIXED | Became real in v2.7.4: under abort no panic survived to poison anything. **Fixed:** recovery ends netplay, queues `HostWarning::RecoveredFromInternalError` (Kotlin + Swift, EN + ES), clears the poison. Three mutations CAUGHT. **Extended in review (#552):** recovery also freezes the machine -- no further emulated cycle runs on a state the panic may have left half-updated (agy's point: running on could let a confused game overwrite its battery save) -- until a new ROM, a power cycle or a loaded state. The audit's remedy was a power-cycle; freezing keeps the picture and lets the player choose **Review (#552):** neither app drained host warnings outside a movie load, so the recovery message never showed; Android polls every 15 frames, iOS once a second. The Android frame loop also catches `InternalException` from non-frame calls | v2.7.4 | |
| MOB-08 | Medium | Partial state corruption when snapshot restore fails after bus stage. | FIXED | CONFIRMED, and a CORE defect every host had: `restore_inner` applied the bus sections before checking the CPU section. **Fixed in v2.7.4:** a loud restore snapshots first and rolls back on failure (quiet restores, the hot path, skip it). Red first; three mutations CAUGHT | v2.7.4 | |
| MOB-09 | Medium | Synchronous blocking DNS resolution in `np_join` and `to_nat_config` blocks UI thread. | FIXED | PARTIAL: Android already called on Dispatchers.IO; the TURN lookup ran under the frame lock (all hosts) and iOS called on the main actor. **Fixed in v2.7.4:** the lookup before the lock (not under test: DNS cannot be made slow deterministically); iOS calls detached **Review (#552):** the detached completions were not tied to the game; a stale one (game closed or swapped, or Leave pressed) now ends its own session and publishes nothing, and only one join / host runs at a time so that `npLeave` cannot hit a newer session | v2.7.4 | |
| SEC-01 | High | HD-pack PNG decompression bomb: `output_buffer_size()` called without clamping dimensions allows crafted packs to cause OOM crash. | FIXED | `rustynes-hdpack/src/hdpack.rs:869` allocates `output_buffer_size()` with no dimension clamp; png 0.18 `Limits` reserve one line only; `(w * h * 4)` is also a `u32` multiply that can overflow. **Fixed in v2.7.3:** `decode_png` checks the header dimensions before sizing any buffer: at most 16384 per side and 4096 x 4096 pixels in total (64 MiB RGBA). The RGBA size is computed in `usize`. The report's suggested test (a huge header returns `None`) passes on v2.7.2 too, because Linux overcommit lets the zeroed buffer allocate lazily and the truncated image then fails to decode. So the pinned test uses a VALID 4097 x 4096 PNG (a 17 KiB file), which v2.7.2 decodes in full and v2.7.3 refuses. Removing the guard reproduces v2.7.2 (mutation CAUGHT). **PR #551 review:** the cap bounded one image, not their number; both loaders now charge each decoded image to a 1 GiB pack budget, checked from the header before allocating. `a_pack_is_bounded_in_total_not_only_per_image` and `a_folder_pack_is_bounded_too`; four mutations CAUGHT (check, charge, zip wiring, folder wiring). Not red-first on v2.7.2: a failing loader test there would have to allocate past 1 GiB, the thing being prevented **v2.7.5 (agy on #551):** the budget was charged from the header before the pixels decoded, and a decode that then failed never refunded it; `decode_png` now refunds (red-first `a_png_that_fails_after_its_header_refunds_the_budget`: 0 left of 16,384 before the fix; mutation CAUGHT) | v2.7.3 | |
| SEC-02 | High | Missing Lua memory limit allows scripts to consume unbounded system memory. | FIXED | No `set_memory_limit` anywhere in `crates/rustynes-script/src/`. **Fixed in v2.7.3:** `lua.set_memory_limit(64 MiB)` at VM creation. `a_script_cannot_allocate_past_the_memory_limit` fails first: a 128 MiB `string.rep` succeeded on v2.7.2. Removing the limit: CAUGHT. **PR #551 review:** `emu.log` / `print` queued lines in host memory, outside that limit, with no bound; now 8,192 lines a frame and 4 KiB a line, in both backends. `the_host_log_is_bounded`, red first; both caps' mutations CAUGHT | v2.7.3 | |
| SEC-03 | Medium | Lua debug hook budget error is caught by `pcall`, allowing malicious or buggy scripts to hang the emulator thread while holding `self.emu` lock. | FIXED | CONFIRMED red-first: `a_budget_abort_cannot_be_caught` hung until killed by a 45 s timeout on v2.7.2. The hook raises an ordinary Lua error, and `pcall` / `xpcall` / `coroutine.resume` catch it. **Fixed in v2.7.3:** a per-frame `budget_tripped` flag, set by the hook and cleared with the counter. The sandbox replaces the three catchers with wrappers that re-raise once it is set; the originals and the flag reader are captured as upvalues no script can reach. Every enclosing catcher is a wrapper, so the abort escapes any nesting, and ordinary errors are still caught (`pcall_still_catches_ordinary_errors`). Mutations CAUGHT (by hang): no guards, flag never set. **PR #551 review:** the six host-fired entries (`reset`, `spriteZeroHit`, `codeBreak`, the three TAStudio callbacks) ran Lua with no budget at all, a gap older than this release; and the trip flag outlived the hook, so a later callback's `pcall` re-raised a stale abort. `with_budget` now wraps all six and `disarm_hook` clears the flag. `a_host_fired_callback_is_under_the_budget` hung on the old code, `a_budget_abort_does_not_leak_into_the_next_callback` failed on it; unwrapping `fire_reset` is CAUGHT (hang). Dropping `disarm_hook`'s clear alone is expected not caught, since `arm_hook` clears too; recorded at the site. Fixing this exposed FE-02 | v2.7.3 | |
| SEC-04 | Medium | SSRF and unbounded response memory in host HTTP IPC (`CommCmd::Http`). | FIXED | Two halves, different verdicts. **Unbounded buffering: REFUTED.** ureq 3.4.2 `Body::read_to_string` stops at `MAX_BODY_SIZE` (10 MB), and an over-limit body is an `Err`, which `http_call` maps to an empty body. The limit is now explicit (`HTTP_BODY_LIMIT`), so a ureq default change cannot lift it; that line is inert by construction, and a mutation of it is expected to be not caught. **SSRF: CONFIRMED.** Any URL was fetched, loopback and link-local included. **Fixed in v2.7.3, per the maintainer's policy choice:** public by default; inward addresses (loopback, RFC 1918, CGNAT, link-local incl. `169.254.169.254`, unspecified, broadcast, multicast, documentation, IPv6 ULA / link-local, IPv4-mapped forms) only for hosts in `RUSTYNES_COMM_HTTP_ALLOW`. The check is a `GuardedResolver` in ureq's resolver, so it sees the addresses actually connected to and DNS rebinding gains nothing. `max_redirects(0)` is set, because ureq followed up to 10 redirects, a bypass the report did not mention. ADR 0016 amended. Tests use local listeners: loopback refused with zero connections, an allowlisted host reached, a 302 returned and its target never contacted. Mutations CAUGHT: resolver check skipped, redirects followed, allowlist ignored, loopback treated as public. **PR #551 review:** `2001:db8::/32` and the IPv4-compatible form (`::127.0.0.1`, which `to_ipv4_mapped` does not unwrap) were public; both now refused, red first, both mutations CAUGHT. ureq also took a proxy from `HTTP_PROXY` / `ALL_PROXY`, and through a CONNECT proxy the resolver checks the proxy, not the target; the agent now sets `proxy(None)` (`an_environment_proxy_is_not_used`, red first). An over-limit body was discarded yet reported with its real 2xx status, which agy round 3 flagged as indistinguishable from an empty response; it is now `status = 0`, like any transport failure (`a_body_over_the_limit_is_a_transport_failure`, red first) | v2.7.3 | |
| SEC-05 | Medium | Synchronous `recorder.stop()` waits on `ffmpeg` process on Winit UI thread, freezing interface. | FIXED | CONFIRMED by reading: `handle_av_record_toggle` took the recorder out under a brief lock, then called `recorder.stop()` on the winit thread, and `stop` waits for ffmpeg to mux (`Command::status`). **Fixed in v2.7.3:** `stop` runs on an `av-finalize` thread; `App::poll_av_finalize` reports the result from `about_to_wait` (so it arrives while paused too) and is joined on exit so a quit does not abandon ffmpeg half-way. A new recording cannot start while one is finishing: the temp and staging files are named after the output path, so the same path would write over what ffmpeg reads. **Not under automated test:** the App needs a window; the existing `av_record` tests still pass with `--features av-record` | v2.7.3 | |
| SEC-06 | Medium | FDS disk saves, RetroAchievements progress, and TAS `.rnm` movies use bare `std::fs::write` instead of `write_atomic`. | FIXED | CONFIRMED, and wider than reported: besides the FDS save (`emu.rs`), the RA blob and movies, the screenshot, SRT, history-clip, fm2/bk2 export and TAStudio-project writes in `app.rs`, the memory-compare export and both `hdpack_builder.rs` writes were bare. All now use `write_atomic`. **Review on #548 (Copilot) found two more truncating writers the first guard could not see**, because it matched `fs::write(` alone: the PPU viewer CHR PNG export (`File::create`, now encoded in memory + `write_atomic`) and ffmpeg's `-y` output for A/V, GIF and WAV recordings (now encoded to a `*.rustynes-partial.<ext>` sibling and renamed over the target only on exit 0). Guard `crates/rustynes-frontend/tests/no_bare_fs_write.rs` now matches `fs::write(`, `File::create(` and `OpenOptions::new(`, with a (file, call, reason) allow-list of five and a stale-entry check. Mutations CAUGHT: the FDS bare write, a reintroduced PNG `File::create`, and a failed encode that publishes anyway (`a_failed_encode_leaves_the_previous_recording_untouched`, run with `--features av-record`, which CI lints but does not test) | v2.7.1 | #548 |
| CON-01 | Low | Missing compile-time guard for `wasm-canvas` vs `wasm-winit` conflict. | FIXED | CONFIRMED: `wasm-winit` is a default feature, and each web frontend declares a `#[wasm_bindgen(start)]`, so `--features wasm-canvas` without `--no-default-features` enabled both and failed late, at wasm-bindgen. **Fixed in v2.7.3:** a `compile_error!` when both are on for wasm32, naming the right invocation. Verified: `clippy --target wasm32 --features wasm-canvas` now stops with that message, and both CI invocations stay clean. The report's second guard (neither feature) was not added: such a build only produces a library with no start function, which is harmless, and would break an ad-hoc `--no-default-features` check | v2.7.3 | |
| CON-03 | Low | Degenerate sample rate ratio causes excessive memory allocation in `resample_linear`. | FIXED | CONFIRMED: `decode_ogg_to_mono` took the source rate from the OGG header (0 clamped to 1) and `resample_linear` sized its output by it, so a 1 Hz header expanded the decoded track 48,000-fold at a 48 kHz output. **Fixed in v2.7.3:** a declared rate outside 8 kHz to 384 kHz makes the track inert (`None`), which bounds the expansion at 48x. `a_track_with_an_absurd_source_rate_is_refused` pins the bounds. The call in `decode_ogg_to_mono` is not driven by a test (it would need a crafted OGG header) | v2.7.3 | |
| CON-04 | Low | Corrupted `config.toml` silently discarded on load without `.bak` preservation. | FIXED | CONFIRMED red-first by `an_unparseable_config_is_preserved_before_defaults_replace_it`: the next save overwrote the user's file with defaults. On a parse failure `load_or_default` now writes `<path>.corrupt.bak` (atomically) before returning defaults, and logs either outcome; the migration `.bak` write is atomic and logs its failure. **Review on #548 (Copilot) found the same loss one branch earlier:** `read_to_string` rejected invalid UTF-8 before the parser ran, and that path skipped the copy. The file is now read as bytes and a non-UTF-8 one is preserved raw; pinned red-first by `a_config_that_is_not_utf8_is_preserved_too` | v2.7.1 | #548 |
| CON-05 | Low | Dead/unwired `host` field in `RetroAchievementsConfig`. | FIXED | CONFIRMED: `RetroAchievementsConfig.host` was written to every saved config and read by nothing; `rustynes_ra::RaConfig` carries no host by design. **Fixed in v2.7.3 by removal**, not wiring: pointing the login token at a configurable server is a feature with its own security questions. Old files with `host = ...` still load (no `deny_unknown_fields`; serde ignores the key), and the next save drops it. `the_dead_ra_host_setting_is_dropped_but_still_loads` | v2.7.3 | |

## Found during triage (not in the report)

| Id | Severity | Finding | Verdict | Evidence | Release | PR |
| --- | --- | --- | --- | --- | --- | --- |
| FE-01 | High | The **desktop** frontend never persists cartridge battery RAM | FIXED | Found while checking the v2.7.1 claim that "the frontend writes `sram()` to `.sav`": no code in `rustynes-frontend` calls `Nes::sram()`/`sram_mut()` (only the libretro core and the test harness do), and the only save sidecar the desktop writes is the FDS `.fds.sav`. A game's in-cartridge save survives only inside a save state. The core audit (§5.1) states the opposite as its premise. Mobile has the same gap as AND-09 / MOB-05. Not fixed in v2.7.1. **Placed in v2.7.3 by the maintainer (2026-09-23)**, with the desktop items; mobile stays with AND-09 / MOB-05 in v2.7.4 **Fixed in v2.7.3:** `battery_save.rs` loads `<data_dir>/battery/<rom_sha256>.sav` into `sram_mut()` at ROM load, under the lock that installs the `Nes`, and only when the new additive `Nes::has_battery()` (header flags 6 bit 1) is set. It writes through `write_atomic` when the live bytes differ from the last write: compared once a second, and forced at ROM switch, close and exit. No core dirty flag was added. An unreadable or wrong-size file is neither loaded nor overwritten. Five tests, each mutation CAUGHT: the load, the battery gate, the once-a-period throttle, the equality check, and the wrong-size refusal. **PR #551 review:** the size is now checked from metadata before any read (`a_wrong_size_save_is_refused_without_reading_it`, a symlink to `/dev/zero`: the read-first mutant was OOM-killed, CAUGHT); agy round 3: the read after it goes through `take(expected + 1)`, so a file that grows in between cannot allocate past that (`the_save_read_is_bounded`, mutation CAUGHT; the race itself is not reproducible in a test); the periodic write runs with the emulator lock released (`due_write` / `written`); an unusable save is reported in the status bar; so is the first failed write of a run (agy round 2; `a_failing_write_is_reported_once_per_run_of_failures`, both mutations CAUGHT). The App call sites need a window and are not under test. Known limitation: power-on movies inherit a loaded `.sav` (documented in `docs/frontend.md`) | v2.7.3 | |
| FE-02 | High | A coroutine ran with **no** instruction budget at all (not in the report; found while fixing SEC-03) | FIXED | `coroutine.resume(coroutine.create(function() while true do end end))` hung with no `pcall` involved. mlua's `set_hook` binds the callback to the main thread; when Lua copies the hook into a coroutine, mlua's `hook_proc` finds no callback for that thread and calls `lua_sethook(state, None)`, removing it (read in mlua 0.12 `state/raw.rs`). Both install sites (`arm_hook` for `load`, and the per-frame hook) now use `set_global_hook`, which dispatches through one shared callback that Lua 5.4's `lua_newthread` copies. Pinned by `a_coroutine_is_under_the_budget_too` (the `load` path) and `a_runaway_frame_callback_is_stopped` (the `onFrame` path). Reverting either site to `set_hook`: CAUGHT (hang) | v2.7.3 | |
| FE-03 | High | Raw FFI renderer / sink handles dereferenced as `&mut` from several threads, and freed while in use (report §2.1; not in the §8 table, so no row was generated) | REFUTED | Traced every caller for v2.7.4. Android: all six JNI calls on a handle come from its one `nes-gl` render thread, and the handle is a local of `renderLoop`. iOS: every call is on the main thread (the display link is on the main run loop; the models are `@MainActor`). So the `&mut` never aliases and destroy never overlaps a render. It rests on host convention, so both shims now document the invariant, and the Android fix for AND-01 keeps it (a new worker joins its predecessor before any native call) | v2.7.4 | |
