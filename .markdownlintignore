# Reference documents (external design specs)
ref-docs/

# External reference emulator clones (also gitignored) — never our content
ref-proj/

# Generated / build output
target/

# Vendored / upstream content (not authored here — keep pristine, don't lint)
crates/rustynes-test-harness/golden/tricnes/
tests/roms/audio-tests/UPSTREAM_README.md
tests/roms/holy_mapperel/README.md

# Frozen archived docs / to-dos — exempt going forward (lint living docs only)
docs/archive/
to-dos/archive/

# Release-plan copies (v1.0.0 synthesis .. v1.6.0 + the engine-lineage v2.x
# plans), the v1.7.0 .. v2.0.0 forward plans, and the per-release research dives
# that fed them — reference, not maintained prose, kept exactly as authored
# (like ref-docs/).
to-dos/plans/

# Harvested mobile-monetization reference set (from the rustynes-monetization
# scaffold, 2026-06-23) — reference design docs kept largely as authored, with a
# reconciliation banner in the index. Reference, not maintained prose (like ref-docs/).
docs/monetization/
