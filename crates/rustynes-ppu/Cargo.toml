[package]
name = "rustynes-ppu"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "NES 2C02 PPU (Picture Processing Unit) emulation"
keywords = ["nes", "ppu", "2C02", "emulator", "graphics"]
categories = ["emulators", "graphics"]

[dependencies]
bitflags.workspace = true
log.workspace = true

[dev-dependencies]
proptest.workspace = true

[features]
default = ["std"]
std = []
serde = ["dep:serde"]

[dependencies.serde]
version = "1.0"
optional = true
features = ["derive"]

[lints]
workspace = true
